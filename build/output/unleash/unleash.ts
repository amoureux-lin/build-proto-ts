// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: unleash/unleash.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { PlayerInfo, RoomInfo } from "./game_common_room";

export const protobufPackage = "unleash.v1";

/** 木头人玩家游戏状态 */
export enum UnleashPlayerStatus {
  /** RPS_HALTING - 站住中 */
  RPS_HALTING = 0,
  /** RPS_RUNNING - 开跑中 */
  RPS_RUNNING = 1,
  /** RPS_DEAD - 被娃娃打死 */
  RPS_DEAD = 2,
  /** RPS_WIN - 获胜 */
  RPS_WIN = 3,
  /** RPS_LOSE - 失败 */
  RPS_LOSE = 4,
  UNRECOGNIZED = -1,
}

export function unleashPlayerStatusFromJSON(object: any): UnleashPlayerStatus {
  switch (object) {
    case 0:
    case "RPS_HALTING":
      return UnleashPlayerStatus.RPS_HALTING;
    case 1:
    case "RPS_RUNNING":
      return UnleashPlayerStatus.RPS_RUNNING;
    case 2:
    case "RPS_DEAD":
      return UnleashPlayerStatus.RPS_DEAD;
    case 3:
    case "RPS_WIN":
      return UnleashPlayerStatus.RPS_WIN;
    case 4:
    case "RPS_LOSE":
      return UnleashPlayerStatus.RPS_LOSE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UnleashPlayerStatus.UNRECOGNIZED;
  }
}

export function unleashPlayerStatusToJSON(object: UnleashPlayerStatus): string {
  switch (object) {
    case UnleashPlayerStatus.RPS_HALTING:
      return "RPS_HALTING";
    case UnleashPlayerStatus.RPS_RUNNING:
      return "RPS_RUNNING";
    case UnleashPlayerStatus.RPS_DEAD:
      return "RPS_DEAD";
    case UnleashPlayerStatus.RPS_WIN:
      return "RPS_WIN";
    case UnleashPlayerStatus.RPS_LOSE:
      return "RPS_LOSE";
    case UnleashPlayerStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** 木头人游戏客户端配置 */
export interface UnleashConfig {
  /** 奔跑速度 n像素/秒 暂定120像素 */
  speed: number;
  /** 最大游戏时长/秒 */
  maxGameDuration: number;
}

/** 游戏信息 */
export interface GameInfo {
  /** 游戏配置 */
  config:
    | UnleashConfig
    | undefined;
  /** 游戏状态 0 等待开始 1 奔跑中 2 暂停(娃娃杀人) 3 胜负已分 4 已派奖 */
  status: number;
  /** 当前下注币值 */
  betAmount: number;
  /** 币值列表 */
  scores: number[];
  /** 第一次开跑时间戳/秒 */
  firstBeginTime: number;
  /** 本阶段允许开跑时间戳/秒 */
  stepBeginTime: number;
  /** 当前娃娃转头完成时间戳/秒 */
  stepPauseTime: number;
  /** 获胜者id。0 表示没有获胜者 */
  winnerId: number;
  /** 获胜者赢取的金币 */
  winCoin: number;
  /** 死亡者id （选出胜者前已经被打死的玩家） */
  deadIds: number[];
  /** 当前阶段是否已警告 */
  stepWarned: boolean;
  /** 胜者奖金 */
  prize: number;
  /** 游戏结束时间戳/秒 */
  endGameTime: number;
  /** 第一名玩家id */
  firstPlayerId: number;
}

/** 参战玩家信息 */
export interface UnleashPlayerInfo {
  playerInfo:
    | PlayerInfo
    | undefined;
  /** 玩家状态 */
  playerStatus: UnleashPlayerStatus;
  /** 本次开跑时的距离 */
  playerDistance: number;
}

/**
 * 进入房间请求
 * MessageType: UNLEASH_JOIN_ROOM_REQ
 */
export interface JoinRoomReq {
  roomId: number;
}

/**
 * 进入房间响应
 * MessageType: UNLEASH_JOIN_ROOM_RES
 */
export interface JoinRoomRes {
  /** 房间信息 */
  roomInfo:
    | RoomInfo
    | undefined;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 房间内所有玩家数量 */
  playersCount: number;
  /** 自己的玩家信息 */
  self:
    | UnleashPlayerInfo
    | undefined;
  /** 房间内观战玩家列表 */
  watchers: PlayerInfo[];
  /** 麦位列表 */
  speakers: PlayerInfo[];
  /** 落座玩家列表 */
  players: UnleashPlayerInfo[];
}

/**
 * 开始游戏请求
 * MessageType: UNLEASH_START_GAME_REQ
 */
export interface StartGameReq {
}

/**
 * 开始游戏响应
 * MessageType: UNLEASH_START_GAME_RES
 */
export interface StartGameRes {
}

/**
 * 开始游戏广播(全员广播)
 * MessageType: UNLEASH_START_GAME_BROADCAST
 */
export interface StartGameBroadcast {
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 参战玩家列表 */
  players: UnleashPlayerInfo[];
  /** 本次开跑时间戳/秒 */
  stepBeginTime: number;
}

/**
 * 娃娃转头完成广播(全员广播)
 * MessageType: UNLEASH_STEP_PAUSE_BROADCAST
 */
export interface StepPauseBroadcast {
}

/**
 * 下一阶段安全开跑广播(全员广播)
 * MessageType: UNLEASH_NEXT_STEP_BROADCAST
 */
export interface NextStepBroadcast {
}

/**
 * 结算完成后重置游戏请求
 * MessageType: UNLEASH_RESET_GAME_REQ
 */
export interface ResetGameReq {
}

/**
 * 结算完成后重置游戏响应
 * MessageType: UNLEASH_RESET_GAME_RES
 */
export interface ResetGameRes {
}

/**
 * 结算完成后重置游戏广播（全员广播）重新回到房间界面
 * MessageType: UNLEASH_RESET_GAME_BROADCAST
 */
export interface ResetGameBroadcast {
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 自己的玩家信息 */
  self:
    | UnleashPlayerInfo
    | undefined;
  /** 参战玩家列表 */
  players: UnleashPlayerInfo[];
}

/**
 * 开跑请求
 * MessageType: UNLEASH_RUN_REQ
 */
export interface RunReq {
}

/**
 * 开跑响应
 * MessageType: UNLEASH_RUN_RES
 */
export interface RunRes {
  /** 站住时的距离 */
  playerDistance: number;
}

/**
 * 开跑广播
 * MessageType: UNLEASH_RUN_BROADCAST
 */
export interface RunBroadcast {
  /** 玩家id */
  playerId: number;
  /** 站住时的距离 */
  playerDistance: number;
}

/**
 * 站住请求
 * MessageType: UNLEASH_HALT_REQ
 */
export interface HaltReq {
}

/**
 * 站住响应
 * MessageType: UNLEASH_HALT_RES
 */
export interface HaltRes {
  /** 站住时的距离 */
  playerDistance: number;
}

/**
 * 站住广播
 * MessageType: UNLEASH_HALT_BROADCAST
 */
export interface HaltBroadcast {
  /** 玩家id */
  playerId: number;
  /** 站住时的距离 */
  playerDistance: number;
}

/**
 * 第一次开跑广播
 * MessageType: UNLEASH_FIRST_RUN_BROADCAST
 */
export interface FirstRunBroadcast {
}

/**
 * 警告广播
 * MessageType: UNLEASH_WARN_BROADCAST
 */
export interface WarnBroadcast {
}

/**
 * 被娃娃打死失败广播（全员广播）
 * MessageType: UNLEASH_DEAD_BROADCAST
 */
export interface DeadBroadcast {
  /** 死亡玩家列表 */
  deadPlayers: UnleashPlayerInfo[];
  /** 第一名玩家id */
  firstPlayerId: number;
}

/**
 * 获胜广播(全员广播)
 * MessageType: UNLEASH_WIN_BROADCAST
 */
export interface WinBroadcast {
  /** 玩家id */
  playerId: number;
  /** 获胜者赢取的金币 */
  winCoin: number;
  /** 胜利玩家的奔跑距离 */
  playerDistance: number;
}

/**
 * 游戏结算广播（全员广播）
 * MessageType: UNLEASH_PRIZE_BROADCAST
 */
export interface PrizeBroadcast {
  /** 获胜者user_id */
  winerId: number;
  /** 获胜者赢取的金币 */
  winCoin: number;
  /** 获胜者完成用时/毫秒 */
  winCompleteMilliseconds: number;
  /** 参战玩家列表 */
  players: UnleashPlayerInfo[];
}

/**
 * 获取玩家Unleash游戏历史记录请求
 * MessageType: UNLEASH_GET_PLAYER_HISTORY_REQ
 */
export interface GetUnleashPlayerHistoryReq {
  startTime: number;
  endTime: number;
  page: number;
}

export interface UnleashPlayerDetail {
  rank: string;
  nickname: string;
  percentage: string;
  avatar: string;
  isSelf: boolean;
  userId: number;
}

/** 玩家Unleash游戏历史记录 */
export interface UnleashPlayerHistory {
  gameCode: string;
  endTime: number;
  result: number;
  timeRecord: string;
  cost: number;
  reward: number;
  players: UnleashPlayerDetail[];
}

/**
 * 获取玩家Unleash游戏历史记录响应
 * MessageType: UNLEASH_GET_PLAYER_HISTORY_RES
 */
export interface GetUnleashPlayerHistoryRes {
  unleashPlayerHistory: UnleashPlayerHistory[];
}

function createBaseUnleashConfig(): UnleashConfig {
  return { speed: 0, maxGameDuration: 0 };
}

export const UnleashConfig = {
  encode(message: UnleashConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.speed !== 0) {
      writer.uint32(8).int32(message.speed);
    }
    if (message.maxGameDuration !== 0) {
      writer.uint32(16).int32(message.maxGameDuration);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnleashConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnleashConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.speed = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.maxGameDuration = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnleashConfig {
    return {
      speed: isSet(object.speed) ? globalThis.Number(object.speed) : 0,
      maxGameDuration: isSet(object.maxGameDuration) ? globalThis.Number(object.maxGameDuration) : 0,
    };
  },

  toJSON(message: UnleashConfig): unknown {
    const obj: any = {};
    if (message.speed !== 0) {
      obj.speed = Math.round(message.speed);
    }
    if (message.maxGameDuration !== 0) {
      obj.maxGameDuration = Math.round(message.maxGameDuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnleashConfig>, I>>(base?: I): UnleashConfig {
    return UnleashConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnleashConfig>, I>>(object: I): UnleashConfig {
    const message = createBaseUnleashConfig();
    message.speed = object.speed ?? 0;
    message.maxGameDuration = object.maxGameDuration ?? 0;
    return message;
  },
};

function createBaseGameInfo(): GameInfo {
  return {
    config: undefined,
    status: 0,
    betAmount: 0,
    scores: [],
    firstBeginTime: 0,
    stepBeginTime: 0,
    stepPauseTime: 0,
    winnerId: 0,
    winCoin: 0,
    deadIds: [],
    stepWarned: false,
    prize: 0,
    endGameTime: 0,
    firstPlayerId: 0,
  };
}

export const GameInfo = {
  encode(message: GameInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.config !== undefined) {
      UnleashConfig.encode(message.config, writer.uint32(10).fork()).ldelim();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.betAmount !== 0) {
      writer.uint32(24).int32(message.betAmount);
    }
    writer.uint32(34).fork();
    for (const v of message.scores) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.firstBeginTime !== 0) {
      writer.uint32(40).int64(message.firstBeginTime);
    }
    if (message.stepBeginTime !== 0) {
      writer.uint32(48).int64(message.stepBeginTime);
    }
    if (message.stepPauseTime !== 0) {
      writer.uint32(56).int64(message.stepPauseTime);
    }
    if (message.winnerId !== 0) {
      writer.uint32(64).int64(message.winnerId);
    }
    if (message.winCoin !== 0) {
      writer.uint32(72).int64(message.winCoin);
    }
    writer.uint32(82).fork();
    for (const v of message.deadIds) {
      writer.int64(v);
    }
    writer.ldelim();
    if (message.stepWarned !== false) {
      writer.uint32(88).bool(message.stepWarned);
    }
    if (message.prize !== 0) {
      writer.uint32(96).int64(message.prize);
    }
    if (message.endGameTime !== 0) {
      writer.uint32(104).int64(message.endGameTime);
    }
    if (message.firstPlayerId !== 0) {
      writer.uint32(112).int64(message.firstPlayerId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.config = UnleashConfig.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.betAmount = reader.int32();
          continue;
        case 4:
          if (tag === 32) {
            message.scores.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.scores.push(reader.int32());
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.firstBeginTime = longToNumber(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.stepBeginTime = longToNumber(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.stepPauseTime = longToNumber(reader.int64() as Long);
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.winnerId = longToNumber(reader.int64() as Long);
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.winCoin = longToNumber(reader.int64() as Long);
          continue;
        case 10:
          if (tag === 80) {
            message.deadIds.push(longToNumber(reader.int64() as Long));

            continue;
          }

          if (tag === 82) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.deadIds.push(longToNumber(reader.int64() as Long));
            }

            continue;
          }

          break;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.stepWarned = reader.bool();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.prize = longToNumber(reader.int64() as Long);
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.endGameTime = longToNumber(reader.int64() as Long);
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.firstPlayerId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameInfo {
    return {
      config: isSet(object.config) ? UnleashConfig.fromJSON(object.config) : undefined,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      betAmount: isSet(object.betAmount) ? globalThis.Number(object.betAmount) : 0,
      scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => globalThis.Number(e)) : [],
      firstBeginTime: isSet(object.firstBeginTime) ? globalThis.Number(object.firstBeginTime) : 0,
      stepBeginTime: isSet(object.stepBeginTime) ? globalThis.Number(object.stepBeginTime) : 0,
      stepPauseTime: isSet(object.stepPauseTime) ? globalThis.Number(object.stepPauseTime) : 0,
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      winCoin: isSet(object.winCoin) ? globalThis.Number(object.winCoin) : 0,
      deadIds: globalThis.Array.isArray(object?.deadIds) ? object.deadIds.map((e: any) => globalThis.Number(e)) : [],
      stepWarned: isSet(object.stepWarned) ? globalThis.Boolean(object.stepWarned) : false,
      prize: isSet(object.prize) ? globalThis.Number(object.prize) : 0,
      endGameTime: isSet(object.endGameTime) ? globalThis.Number(object.endGameTime) : 0,
      firstPlayerId: isSet(object.firstPlayerId) ? globalThis.Number(object.firstPlayerId) : 0,
    };
  },

  toJSON(message: GameInfo): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = UnleashConfig.toJSON(message.config);
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.betAmount !== 0) {
      obj.betAmount = Math.round(message.betAmount);
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => Math.round(e));
    }
    if (message.firstBeginTime !== 0) {
      obj.firstBeginTime = Math.round(message.firstBeginTime);
    }
    if (message.stepBeginTime !== 0) {
      obj.stepBeginTime = Math.round(message.stepBeginTime);
    }
    if (message.stepPauseTime !== 0) {
      obj.stepPauseTime = Math.round(message.stepPauseTime);
    }
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.winCoin !== 0) {
      obj.winCoin = Math.round(message.winCoin);
    }
    if (message.deadIds?.length) {
      obj.deadIds = message.deadIds.map((e) => Math.round(e));
    }
    if (message.stepWarned !== false) {
      obj.stepWarned = message.stepWarned;
    }
    if (message.prize !== 0) {
      obj.prize = Math.round(message.prize);
    }
    if (message.endGameTime !== 0) {
      obj.endGameTime = Math.round(message.endGameTime);
    }
    if (message.firstPlayerId !== 0) {
      obj.firstPlayerId = Math.round(message.firstPlayerId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameInfo>, I>>(base?: I): GameInfo {
    return GameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameInfo>, I>>(object: I): GameInfo {
    const message = createBaseGameInfo();
    message.config = (object.config !== undefined && object.config !== null)
      ? UnleashConfig.fromPartial(object.config)
      : undefined;
    message.status = object.status ?? 0;
    message.betAmount = object.betAmount ?? 0;
    message.scores = object.scores?.map((e) => e) || [];
    message.firstBeginTime = object.firstBeginTime ?? 0;
    message.stepBeginTime = object.stepBeginTime ?? 0;
    message.stepPauseTime = object.stepPauseTime ?? 0;
    message.winnerId = object.winnerId ?? 0;
    message.winCoin = object.winCoin ?? 0;
    message.deadIds = object.deadIds?.map((e) => e) || [];
    message.stepWarned = object.stepWarned ?? false;
    message.prize = object.prize ?? 0;
    message.endGameTime = object.endGameTime ?? 0;
    message.firstPlayerId = object.firstPlayerId ?? 0;
    return message;
  },
};

function createBaseUnleashPlayerInfo(): UnleashPlayerInfo {
  return { playerInfo: undefined, playerStatus: 0, playerDistance: 0 };
}

export const UnleashPlayerInfo = {
  encode(message: UnleashPlayerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerInfo !== undefined) {
      PlayerInfo.encode(message.playerInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.playerStatus !== 0) {
      writer.uint32(16).int32(message.playerStatus);
    }
    if (message.playerDistance !== 0) {
      writer.uint32(24).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnleashPlayerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnleashPlayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerInfo = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.playerStatus = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnleashPlayerInfo {
    return {
      playerInfo: isSet(object.playerInfo) ? PlayerInfo.fromJSON(object.playerInfo) : undefined,
      playerStatus: isSet(object.playerStatus) ? unleashPlayerStatusFromJSON(object.playerStatus) : 0,
      playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0,
    };
  },

  toJSON(message: UnleashPlayerInfo): unknown {
    const obj: any = {};
    if (message.playerInfo !== undefined) {
      obj.playerInfo = PlayerInfo.toJSON(message.playerInfo);
    }
    if (message.playerStatus !== 0) {
      obj.playerStatus = unleashPlayerStatusToJSON(message.playerStatus);
    }
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnleashPlayerInfo>, I>>(base?: I): UnleashPlayerInfo {
    return UnleashPlayerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnleashPlayerInfo>, I>>(object: I): UnleashPlayerInfo {
    const message = createBaseUnleashPlayerInfo();
    message.playerInfo = (object.playerInfo !== undefined && object.playerInfo !== null)
      ? PlayerInfo.fromPartial(object.playerInfo)
      : undefined;
    message.playerStatus = object.playerStatus ?? 0;
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBaseJoinRoomReq(): JoinRoomReq {
  return { roomId: 0 };
}

export const JoinRoomReq = {
  encode(message: JoinRoomReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomReq {
    return { roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0 };
  },

  toJSON(message: JoinRoomReq): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomReq>, I>>(base?: I): JoinRoomReq {
    return JoinRoomReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomReq>, I>>(object: I): JoinRoomReq {
    const message = createBaseJoinRoomReq();
    message.roomId = object.roomId ?? 0;
    return message;
  },
};

function createBaseJoinRoomRes(): JoinRoomRes {
  return {
    roomInfo: undefined,
    gameInfo: undefined,
    playersCount: 0,
    self: undefined,
    watchers: [],
    speakers: [],
    players: [],
  };
}

export const JoinRoomRes = {
  encode(message: JoinRoomRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomInfo !== undefined) {
      RoomInfo.encode(message.roomInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(18).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(24).int32(message.playersCount);
    }
    if (message.self !== undefined) {
      UnleashPlayerInfo.encode(message.self, writer.uint32(34).fork()).ldelim();
    }
    for (const v of message.watchers) {
      PlayerInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    for (const v of message.speakers) {
      PlayerInfo.encode(v!, writer.uint32(50).fork()).ldelim();
    }
    for (const v of message.players) {
      UnleashPlayerInfo.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomInfo = RoomInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.self = UnleashPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.watchers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.speakers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.players.push(UnleashPlayerInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRes {
    return {
      roomInfo: isSet(object.roomInfo) ? RoomInfo.fromJSON(object.roomInfo) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
      self: isSet(object.self) ? UnleashPlayerInfo.fromJSON(object.self) : undefined,
      watchers: globalThis.Array.isArray(object?.watchers)
        ? object.watchers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      speakers: globalThis.Array.isArray(object?.speakers)
        ? object.speakers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => UnleashPlayerInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: JoinRoomRes): unknown {
    const obj: any = {};
    if (message.roomInfo !== undefined) {
      obj.roomInfo = RoomInfo.toJSON(message.roomInfo);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    if (message.self !== undefined) {
      obj.self = UnleashPlayerInfo.toJSON(message.self);
    }
    if (message.watchers?.length) {
      obj.watchers = message.watchers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.speakers?.length) {
      obj.speakers = message.speakers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => UnleashPlayerInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomRes>, I>>(base?: I): JoinRoomRes {
    return JoinRoomRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomRes>, I>>(object: I): JoinRoomRes {
    const message = createBaseJoinRoomRes();
    message.roomInfo = (object.roomInfo !== undefined && object.roomInfo !== null)
      ? RoomInfo.fromPartial(object.roomInfo)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.playersCount = object.playersCount ?? 0;
    message.self = (object.self !== undefined && object.self !== null)
      ? UnleashPlayerInfo.fromPartial(object.self)
      : undefined;
    message.watchers = object.watchers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.speakers = object.speakers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.players = object.players?.map((e) => UnleashPlayerInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStartGameReq(): StartGameReq {
  return {};
}

export const StartGameReq = {
  encode(_: StartGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameReq {
    return {};
  },

  toJSON(_: StartGameReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameReq>, I>>(base?: I): StartGameReq {
    return StartGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameReq>, I>>(_: I): StartGameReq {
    const message = createBaseStartGameReq();
    return message;
  },
};

function createBaseStartGameRes(): StartGameRes {
  return {};
}

export const StartGameRes = {
  encode(_: StartGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameRes {
    return {};
  },

  toJSON(_: StartGameRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameRes>, I>>(base?: I): StartGameRes {
    return StartGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameRes>, I>>(_: I): StartGameRes {
    const message = createBaseStartGameRes();
    return message;
  },
};

function createBaseStartGameBroadcast(): StartGameBroadcast {
  return { gameInfo: undefined, players: [], stepBeginTime: 0 };
}

export const StartGameBroadcast = {
  encode(message: StartGameBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.players) {
      UnleashPlayerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.stepBeginTime !== 0) {
      writer.uint32(24).int64(message.stepBeginTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.players.push(UnleashPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.stepBeginTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartGameBroadcast {
    return {
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => UnleashPlayerInfo.fromJSON(e))
        : [],
      stepBeginTime: isSet(object.stepBeginTime) ? globalThis.Number(object.stepBeginTime) : 0,
    };
  },

  toJSON(message: StartGameBroadcast): unknown {
    const obj: any = {};
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => UnleashPlayerInfo.toJSON(e));
    }
    if (message.stepBeginTime !== 0) {
      obj.stepBeginTime = Math.round(message.stepBeginTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameBroadcast>, I>>(base?: I): StartGameBroadcast {
    return StartGameBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameBroadcast>, I>>(object: I): StartGameBroadcast {
    const message = createBaseStartGameBroadcast();
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.players = object.players?.map((e) => UnleashPlayerInfo.fromPartial(e)) || [];
    message.stepBeginTime = object.stepBeginTime ?? 0;
    return message;
  },
};

function createBaseStepPauseBroadcast(): StepPauseBroadcast {
  return {};
}

export const StepPauseBroadcast = {
  encode(_: StepPauseBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StepPauseBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStepPauseBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StepPauseBroadcast {
    return {};
  },

  toJSON(_: StepPauseBroadcast): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StepPauseBroadcast>, I>>(base?: I): StepPauseBroadcast {
    return StepPauseBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StepPauseBroadcast>, I>>(_: I): StepPauseBroadcast {
    const message = createBaseStepPauseBroadcast();
    return message;
  },
};

function createBaseNextStepBroadcast(): NextStepBroadcast {
  return {};
}

export const NextStepBroadcast = {
  encode(_: NextStepBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): NextStepBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNextStepBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): NextStepBroadcast {
    return {};
  },

  toJSON(_: NextStepBroadcast): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<NextStepBroadcast>, I>>(base?: I): NextStepBroadcast {
    return NextStepBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NextStepBroadcast>, I>>(_: I): NextStepBroadcast {
    const message = createBaseNextStepBroadcast();
    return message;
  },
};

function createBaseResetGameReq(): ResetGameReq {
  return {};
}

export const ResetGameReq = {
  encode(_: ResetGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResetGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ResetGameReq {
    return {};
  },

  toJSON(_: ResetGameReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ResetGameReq>, I>>(base?: I): ResetGameReq {
    return ResetGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResetGameReq>, I>>(_: I): ResetGameReq {
    const message = createBaseResetGameReq();
    return message;
  },
};

function createBaseResetGameRes(): ResetGameRes {
  return {};
}

export const ResetGameRes = {
  encode(_: ResetGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResetGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ResetGameRes {
    return {};
  },

  toJSON(_: ResetGameRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ResetGameRes>, I>>(base?: I): ResetGameRes {
    return ResetGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResetGameRes>, I>>(_: I): ResetGameRes {
    const message = createBaseResetGameRes();
    return message;
  },
};

function createBaseResetGameBroadcast(): ResetGameBroadcast {
  return { gameInfo: undefined, self: undefined, players: [] };
}

export const ResetGameBroadcast = {
  encode(message: ResetGameBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.self !== undefined) {
      UnleashPlayerInfo.encode(message.self, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.players) {
      UnleashPlayerInfo.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ResetGameBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResetGameBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.self = UnleashPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.players.push(UnleashPlayerInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResetGameBroadcast {
    return {
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      self: isSet(object.self) ? UnleashPlayerInfo.fromJSON(object.self) : undefined,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => UnleashPlayerInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ResetGameBroadcast): unknown {
    const obj: any = {};
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.self !== undefined) {
      obj.self = UnleashPlayerInfo.toJSON(message.self);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => UnleashPlayerInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ResetGameBroadcast>, I>>(base?: I): ResetGameBroadcast {
    return ResetGameBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ResetGameBroadcast>, I>>(object: I): ResetGameBroadcast {
    const message = createBaseResetGameBroadcast();
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.self = (object.self !== undefined && object.self !== null)
      ? UnleashPlayerInfo.fromPartial(object.self)
      : undefined;
    message.players = object.players?.map((e) => UnleashPlayerInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRunReq(): RunReq {
  return {};
}

export const RunReq = {
  encode(_: RunReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RunReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RunReq {
    return {};
  },

  toJSON(_: RunReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<RunReq>, I>>(base?: I): RunReq {
    return RunReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunReq>, I>>(_: I): RunReq {
    const message = createBaseRunReq();
    return message;
  },
};

function createBaseRunRes(): RunRes {
  return { playerDistance: 0 };
}

export const RunRes = {
  encode(message: RunRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerDistance !== 0) {
      writer.uint32(8).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RunRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunRes {
    return { playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0 };
  },

  toJSON(message: RunRes): unknown {
    const obj: any = {};
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunRes>, I>>(base?: I): RunRes {
    return RunRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunRes>, I>>(object: I): RunRes {
    const message = createBaseRunRes();
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBaseRunBroadcast(): RunBroadcast {
  return { playerId: 0, playerDistance: 0 };
}

export const RunBroadcast = {
  encode(message: RunBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.playerDistance !== 0) {
      writer.uint32(16).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RunBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0,
    };
  },

  toJSON(message: RunBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunBroadcast>, I>>(base?: I): RunBroadcast {
    return RunBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunBroadcast>, I>>(object: I): RunBroadcast {
    const message = createBaseRunBroadcast();
    message.playerId = object.playerId ?? 0;
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBaseHaltReq(): HaltReq {
  return {};
}

export const HaltReq = {
  encode(_: HaltReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HaltReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHaltReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HaltReq {
    return {};
  },

  toJSON(_: HaltReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HaltReq>, I>>(base?: I): HaltReq {
    return HaltReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HaltReq>, I>>(_: I): HaltReq {
    const message = createBaseHaltReq();
    return message;
  },
};

function createBaseHaltRes(): HaltRes {
  return { playerDistance: 0 };
}

export const HaltRes = {
  encode(message: HaltRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerDistance !== 0) {
      writer.uint32(8).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HaltRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHaltRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HaltRes {
    return { playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0 };
  },

  toJSON(message: HaltRes): unknown {
    const obj: any = {};
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HaltRes>, I>>(base?: I): HaltRes {
    return HaltRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HaltRes>, I>>(object: I): HaltRes {
    const message = createBaseHaltRes();
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBaseHaltBroadcast(): HaltBroadcast {
  return { playerId: 0, playerDistance: 0 };
}

export const HaltBroadcast = {
  encode(message: HaltBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.playerDistance !== 0) {
      writer.uint32(16).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HaltBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHaltBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HaltBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0,
    };
  },

  toJSON(message: HaltBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HaltBroadcast>, I>>(base?: I): HaltBroadcast {
    return HaltBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HaltBroadcast>, I>>(object: I): HaltBroadcast {
    const message = createBaseHaltBroadcast();
    message.playerId = object.playerId ?? 0;
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBaseFirstRunBroadcast(): FirstRunBroadcast {
  return {};
}

export const FirstRunBroadcast = {
  encode(_: FirstRunBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): FirstRunBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFirstRunBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FirstRunBroadcast {
    return {};
  },

  toJSON(_: FirstRunBroadcast): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FirstRunBroadcast>, I>>(base?: I): FirstRunBroadcast {
    return FirstRunBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FirstRunBroadcast>, I>>(_: I): FirstRunBroadcast {
    const message = createBaseFirstRunBroadcast();
    return message;
  },
};

function createBaseWarnBroadcast(): WarnBroadcast {
  return {};
}

export const WarnBroadcast = {
  encode(_: WarnBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WarnBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWarnBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): WarnBroadcast {
    return {};
  },

  toJSON(_: WarnBroadcast): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<WarnBroadcast>, I>>(base?: I): WarnBroadcast {
    return WarnBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WarnBroadcast>, I>>(_: I): WarnBroadcast {
    const message = createBaseWarnBroadcast();
    return message;
  },
};

function createBaseDeadBroadcast(): DeadBroadcast {
  return { deadPlayers: [], firstPlayerId: 0 };
}

export const DeadBroadcast = {
  encode(message: DeadBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.deadPlayers) {
      UnleashPlayerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.firstPlayerId !== 0) {
      writer.uint32(16).int64(message.firstPlayerId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DeadBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeadBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.deadPlayers.push(UnleashPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.firstPlayerId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeadBroadcast {
    return {
      deadPlayers: globalThis.Array.isArray(object?.deadPlayers)
        ? object.deadPlayers.map((e: any) => UnleashPlayerInfo.fromJSON(e))
        : [],
      firstPlayerId: isSet(object.firstPlayerId) ? globalThis.Number(object.firstPlayerId) : 0,
    };
  },

  toJSON(message: DeadBroadcast): unknown {
    const obj: any = {};
    if (message.deadPlayers?.length) {
      obj.deadPlayers = message.deadPlayers.map((e) => UnleashPlayerInfo.toJSON(e));
    }
    if (message.firstPlayerId !== 0) {
      obj.firstPlayerId = Math.round(message.firstPlayerId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeadBroadcast>, I>>(base?: I): DeadBroadcast {
    return DeadBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeadBroadcast>, I>>(object: I): DeadBroadcast {
    const message = createBaseDeadBroadcast();
    message.deadPlayers = object.deadPlayers?.map((e) => UnleashPlayerInfo.fromPartial(e)) || [];
    message.firstPlayerId = object.firstPlayerId ?? 0;
    return message;
  },
};

function createBaseWinBroadcast(): WinBroadcast {
  return { playerId: 0, winCoin: 0, playerDistance: 0 };
}

export const WinBroadcast = {
  encode(message: WinBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.winCoin !== 0) {
      writer.uint32(16).int64(message.winCoin);
    }
    if (message.playerDistance !== 0) {
      writer.uint32(32).int64(message.playerDistance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WinBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWinBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.winCoin = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.playerDistance = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WinBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      winCoin: isSet(object.winCoin) ? globalThis.Number(object.winCoin) : 0,
      playerDistance: isSet(object.playerDistance) ? globalThis.Number(object.playerDistance) : 0,
    };
  },

  toJSON(message: WinBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.winCoin !== 0) {
      obj.winCoin = Math.round(message.winCoin);
    }
    if (message.playerDistance !== 0) {
      obj.playerDistance = Math.round(message.playerDistance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WinBroadcast>, I>>(base?: I): WinBroadcast {
    return WinBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WinBroadcast>, I>>(object: I): WinBroadcast {
    const message = createBaseWinBroadcast();
    message.playerId = object.playerId ?? 0;
    message.winCoin = object.winCoin ?? 0;
    message.playerDistance = object.playerDistance ?? 0;
    return message;
  },
};

function createBasePrizeBroadcast(): PrizeBroadcast {
  return { winerId: 0, winCoin: 0, winCompleteMilliseconds: 0, players: [] };
}

export const PrizeBroadcast = {
  encode(message: PrizeBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.winerId !== 0) {
      writer.uint32(8).int64(message.winerId);
    }
    if (message.winCoin !== 0) {
      writer.uint32(16).int64(message.winCoin);
    }
    if (message.winCompleteMilliseconds !== 0) {
      writer.uint32(40).int64(message.winCompleteMilliseconds);
    }
    for (const v of message.players) {
      UnleashPlayerInfo.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PrizeBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrizeBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.winerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.winCoin = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.winCompleteMilliseconds = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.players.push(UnleashPlayerInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrizeBroadcast {
    return {
      winerId: isSet(object.winerId) ? globalThis.Number(object.winerId) : 0,
      winCoin: isSet(object.winCoin) ? globalThis.Number(object.winCoin) : 0,
      winCompleteMilliseconds: isSet(object.winCompleteMilliseconds)
        ? globalThis.Number(object.winCompleteMilliseconds)
        : 0,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => UnleashPlayerInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PrizeBroadcast): unknown {
    const obj: any = {};
    if (message.winerId !== 0) {
      obj.winerId = Math.round(message.winerId);
    }
    if (message.winCoin !== 0) {
      obj.winCoin = Math.round(message.winCoin);
    }
    if (message.winCompleteMilliseconds !== 0) {
      obj.winCompleteMilliseconds = Math.round(message.winCompleteMilliseconds);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => UnleashPlayerInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrizeBroadcast>, I>>(base?: I): PrizeBroadcast {
    return PrizeBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrizeBroadcast>, I>>(object: I): PrizeBroadcast {
    const message = createBasePrizeBroadcast();
    message.winerId = object.winerId ?? 0;
    message.winCoin = object.winCoin ?? 0;
    message.winCompleteMilliseconds = object.winCompleteMilliseconds ?? 0;
    message.players = object.players?.map((e) => UnleashPlayerInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetUnleashPlayerHistoryReq(): GetUnleashPlayerHistoryReq {
  return { startTime: 0, endTime: 0, page: 0 };
}

export const GetUnleashPlayerHistoryReq = {
  encode(message: GetUnleashPlayerHistoryReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.startTime !== 0) {
      writer.uint32(8).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.page !== 0) {
      writer.uint32(24).int32(message.page);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUnleashPlayerHistoryReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUnleashPlayerHistoryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.startTime = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.page = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUnleashPlayerHistoryReq {
    return {
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
    };
  },

  toJSON(message: GetUnleashPlayerHistoryReq): unknown {
    const obj: any = {};
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUnleashPlayerHistoryReq>, I>>(base?: I): GetUnleashPlayerHistoryReq {
    return GetUnleashPlayerHistoryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUnleashPlayerHistoryReq>, I>>(object: I): GetUnleashPlayerHistoryReq {
    const message = createBaseGetUnleashPlayerHistoryReq();
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    message.page = object.page ?? 0;
    return message;
  },
};

function createBaseUnleashPlayerDetail(): UnleashPlayerDetail {
  return { rank: "", nickname: "", percentage: "", avatar: "", isSelf: false, userId: 0 };
}

export const UnleashPlayerDetail = {
  encode(message: UnleashPlayerDetail, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.rank !== "") {
      writer.uint32(10).string(message.rank);
    }
    if (message.nickname !== "") {
      writer.uint32(18).string(message.nickname);
    }
    if (message.percentage !== "") {
      writer.uint32(26).string(message.percentage);
    }
    if (message.avatar !== "") {
      writer.uint32(34).string(message.avatar);
    }
    if (message.isSelf !== false) {
      writer.uint32(40).bool(message.isSelf);
    }
    if (message.userId !== 0) {
      writer.uint32(48).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnleashPlayerDetail {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnleashPlayerDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.rank = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nickname = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.percentage = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.avatar = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.isSelf = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnleashPlayerDetail {
    return {
      rank: isSet(object.rank) ? globalThis.String(object.rank) : "",
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      percentage: isSet(object.percentage) ? globalThis.String(object.percentage) : "",
      avatar: isSet(object.avatar) ? globalThis.String(object.avatar) : "",
      isSelf: isSet(object.isSelf) ? globalThis.Boolean(object.isSelf) : false,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: UnleashPlayerDetail): unknown {
    const obj: any = {};
    if (message.rank !== "") {
      obj.rank = message.rank;
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.percentage !== "") {
      obj.percentage = message.percentage;
    }
    if (message.avatar !== "") {
      obj.avatar = message.avatar;
    }
    if (message.isSelf !== false) {
      obj.isSelf = message.isSelf;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnleashPlayerDetail>, I>>(base?: I): UnleashPlayerDetail {
    return UnleashPlayerDetail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnleashPlayerDetail>, I>>(object: I): UnleashPlayerDetail {
    const message = createBaseUnleashPlayerDetail();
    message.rank = object.rank ?? "";
    message.nickname = object.nickname ?? "";
    message.percentage = object.percentage ?? "";
    message.avatar = object.avatar ?? "";
    message.isSelf = object.isSelf ?? false;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseUnleashPlayerHistory(): UnleashPlayerHistory {
  return { gameCode: "", endTime: 0, result: 0, timeRecord: "", cost: 0, reward: 0, players: [] };
}

export const UnleashPlayerHistory = {
  encode(message: UnleashPlayerHistory, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameCode !== "") {
      writer.uint32(10).string(message.gameCode);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.result !== 0) {
      writer.uint32(24).int32(message.result);
    }
    if (message.timeRecord !== "") {
      writer.uint32(34).string(message.timeRecord);
    }
    if (message.cost !== 0) {
      writer.uint32(40).int64(message.cost);
    }
    if (message.reward !== 0) {
      writer.uint32(48).int64(message.reward);
    }
    for (const v of message.players) {
      UnleashPlayerDetail.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UnleashPlayerHistory {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnleashPlayerHistory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameCode = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.result = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.timeRecord = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.cost = longToNumber(reader.int64() as Long);
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.reward = longToNumber(reader.int64() as Long);
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.players.push(UnleashPlayerDetail.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnleashPlayerHistory {
    return {
      gameCode: isSet(object.gameCode) ? globalThis.String(object.gameCode) : "",
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
      timeRecord: isSet(object.timeRecord) ? globalThis.String(object.timeRecord) : "",
      cost: isSet(object.cost) ? globalThis.Number(object.cost) : 0,
      reward: isSet(object.reward) ? globalThis.Number(object.reward) : 0,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => UnleashPlayerDetail.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UnleashPlayerHistory): unknown {
    const obj: any = {};
    if (message.gameCode !== "") {
      obj.gameCode = message.gameCode;
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    if (message.timeRecord !== "") {
      obj.timeRecord = message.timeRecord;
    }
    if (message.cost !== 0) {
      obj.cost = Math.round(message.cost);
    }
    if (message.reward !== 0) {
      obj.reward = Math.round(message.reward);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => UnleashPlayerDetail.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnleashPlayerHistory>, I>>(base?: I): UnleashPlayerHistory {
    return UnleashPlayerHistory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnleashPlayerHistory>, I>>(object: I): UnleashPlayerHistory {
    const message = createBaseUnleashPlayerHistory();
    message.gameCode = object.gameCode ?? "";
    message.endTime = object.endTime ?? 0;
    message.result = object.result ?? 0;
    message.timeRecord = object.timeRecord ?? "";
    message.cost = object.cost ?? 0;
    message.reward = object.reward ?? 0;
    message.players = object.players?.map((e) => UnleashPlayerDetail.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetUnleashPlayerHistoryRes(): GetUnleashPlayerHistoryRes {
  return { unleashPlayerHistory: [] };
}

export const GetUnleashPlayerHistoryRes = {
  encode(message: GetUnleashPlayerHistoryRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.unleashPlayerHistory) {
      UnleashPlayerHistory.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetUnleashPlayerHistoryRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUnleashPlayerHistoryRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.unleashPlayerHistory.push(UnleashPlayerHistory.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUnleashPlayerHistoryRes {
    return {
      unleashPlayerHistory: globalThis.Array.isArray(object?.unleashPlayerHistory)
        ? object.unleashPlayerHistory.map((e: any) => UnleashPlayerHistory.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUnleashPlayerHistoryRes): unknown {
    const obj: any = {};
    if (message.unleashPlayerHistory?.length) {
      obj.unleashPlayerHistory = message.unleashPlayerHistory.map((e) => UnleashPlayerHistory.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUnleashPlayerHistoryRes>, I>>(base?: I): GetUnleashPlayerHistoryRes {
    return GetUnleashPlayerHistoryRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUnleashPlayerHistoryRes>, I>>(object: I): GetUnleashPlayerHistoryRes {
    const message = createBaseGetUnleashPlayerHistoryRes();
    message.unleashPlayerHistory = object.unleashPlayerHistory?.map((e) => UnleashPlayerHistory.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
