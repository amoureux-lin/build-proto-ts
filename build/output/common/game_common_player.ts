// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: common/game_common_player.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs";
import { PlayerInfo } from "./game_common_room";

export const protobufPackage = "common.v1";

/**
 * 服务器维护中广播
 * MessageType: COMMON_SERVER_CLOSED_BROADCAST
 */
export interface ServerClosedBroadcast {
}

/**
 * 玩家离线广播
 * MessageType: COMMON_OFFLINE_BROADCAST
 */
export interface UserOfflineBroadcast {
  bcUid: number;
  userId: number;
  player: PlayerInfo | undefined;
}

/**
 * 玩家加入房间广播
 * MessageType: COMMON_JOIN_ROOM_BROADCAST
 */
export interface JoinRoomBroadcast {
  bcUid: number;
  userId: number;
  player: PlayerInfo | undefined;
  playersCount: number;
}

/**
 * 玩家退出房间请求
 * MessageType: COMMON_PLAYER_EXIT_ROOM_REQ
 */
export interface PlayerExitRoomReq {
}

/**
 * 玩家退出房间返回
 * MessageType: COMMON_PLAYER_EXIT_ROOM_RES
 */
export interface PlayerExitRoomRes {
}

/**
 * 玩家离开房间广播
 * MessageType: COMMON_LEAVE_ROOM_BROADCAST
 */
export interface LeaveRoomBroadcast {
  bcUid: number;
  userId: number;
  player: PlayerInfo | undefined;
  playersCount: number;
}

/**
 * 玩家坐下请求
 * MessageType: COMMON_PLAYER_SIT_DOWN_REQ
 */
export interface PlayerSitDownReq {
  seat: number;
}

/**
 * 玩家坐下返回
 * MessageType: COMMON_PLAYER_SIT_DOWN_RES
 */
export interface PlayerSitDownRes {
  seat: number;
  /** 等待玩家准备倒计时的结束时间，单位秒。为0表示没有开启倒计时 */
  waitReadyExpiredTime: number;
}

/**
 * 玩家坐下广播
 * MessageType: COMMON_SIT_DOWN_BROADCAST
 */
export interface SitDownBroadcast {
  bcUid: number;
  userId: number;
  player:
    | PlayerInfo
    | undefined;
  /** 等待玩家准备倒计时的结束时间，单位秒。为0表示没有开启倒计时 */
  waitReadyExpiredTime: number;
}

/**
 * 玩家站起请求
 * MessageType: COMMON_PLAYER_STAND_UP_REQ
 */
export interface PlayerStandUpReq {
}

/**
 * 玩家站起返回
 * MessageType: COMMON_PLAYER_STAND_UP_RES
 */
export interface PlayerStandUpRes {
}

/**
 * 玩家站起广播
 * MessageType: COMMON_STAND_UP_BROADCAST
 */
export interface StandUpBroadcast {
  bcUid: number;
  userId: number;
  player: PlayerInfo | undefined;
}

/**
 * 玩家准备游戏请求
 * MessageType: COMMON_PLAYER_READY_GAME_REQ
 */
export interface PlayerReadyGameReq {
  ready: boolean;
}

/**
 * 玩家准备游戏返回
 * MessageType: COMMON_PLAYER_READY_GAME_RES
 */
export interface PlayerReadyGameRes {
  ready: boolean;
  /** 等待玩家准备倒计时的结束时间，单位秒。为0表示没有开启倒计时 */
  waitReadyExpiredTime: number;
}

/**
 * 玩家准备广播
 * MessageType: COMMON_READY_BROADCAST
 */
export interface ReadyBroadcast {
  bcUid: number;
  userId: number;
  player: PlayerInfo | undefined;
  isReady: boolean;
  /** 等待玩家准备倒计时的结束时间，单位秒。为0表示没有开启倒计时 */
  waitReadyExpiredTime: number;
}

/**
 * 玩家信息变化通用类，以后有玩家信息变化直接加字段，有值就是有变化，没值会返回类型默认值
 * MessageType: COMMON_USER_UPDATE_BROADCAST
 */
export interface UserInfoUpdateBroadcast {
  userId: number;
  coins: number;
}

/**
 * 获取玩家历史记录概要请求
 * MessageType: COMMON_GET_HISTORY_SUMMARY_REQ
 */
export interface GetHistorySummaryReq {
  /** 用户ID */
  userId: number;
  /** 游戏ID */
  gameId: number;
  /** 开始时间，Unix时间戳（秒） */
  startTime: number;
  /** 结束时间，Unix时间戳（秒） */
  endTime: number;
}

/**
 * 获取玩家历史记录概要响应
 * MessageType: COMMON_GET_HISTORY_SUMMARY_RES
 */
export interface GetHistorySummaryRes {
  /** 有数据的日期列表，按日期升序排列 */
  dateSummaries: HistoryDateSummary[];
}

/** 历史记录日期概要 */
export interface HistoryDateSummary {
  /** 日期，格式: YYYYMMDD，例如 20231225 */
  date: number;
}

function createBaseServerClosedBroadcast(): ServerClosedBroadcast {
  return {};
}

export const ServerClosedBroadcast = {
  encode(_: ServerClosedBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerClosedBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerClosedBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ServerClosedBroadcast {
    return {};
  },

  toJSON(_: ServerClosedBroadcast): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerClosedBroadcast>, I>>(base?: I): ServerClosedBroadcast {
    return ServerClosedBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerClosedBroadcast>, I>>(_: I): ServerClosedBroadcast {
    const message = createBaseServerClosedBroadcast();
    return message;
  },
};

function createBaseUserOfflineBroadcast(): UserOfflineBroadcast {
  return { bcUid: 0, userId: 0, player: undefined };
}

export const UserOfflineBroadcast = {
  encode(message: UserOfflineBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(24).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UserOfflineBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserOfflineBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3:
          if (tag !== 24) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserOfflineBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
    };
  },

  toJSON(message: UserOfflineBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserOfflineBroadcast>, I>>(base?: I): UserOfflineBroadcast {
    return UserOfflineBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserOfflineBroadcast>, I>>(object: I): UserOfflineBroadcast {
    const message = createBaseUserOfflineBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    return message;
  },
};

function createBaseJoinRoomBroadcast(): JoinRoomBroadcast {
  return { bcUid: 0, userId: 0, player: undefined, playersCount: 0 };
}

export const JoinRoomBroadcast = {
  encode(message: JoinRoomBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(32).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(24).int32(message.playersCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 4:
          if (tag !== 32) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
    };
  },

  toJSON(message: JoinRoomBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomBroadcast>, I>>(base?: I): JoinRoomBroadcast {
    return JoinRoomBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomBroadcast>, I>>(object: I): JoinRoomBroadcast {
    const message = createBaseJoinRoomBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    message.playersCount = object.playersCount ?? 0;
    return message;
  },
};

function createBasePlayerExitRoomReq(): PlayerExitRoomReq {
  return {};
}

export const PlayerExitRoomReq = {
  encode(_: PlayerExitRoomReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerExitRoomReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerExitRoomReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PlayerExitRoomReq {
    return {};
  },

  toJSON(_: PlayerExitRoomReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerExitRoomReq>, I>>(base?: I): PlayerExitRoomReq {
    return PlayerExitRoomReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerExitRoomReq>, I>>(_: I): PlayerExitRoomReq {
    const message = createBasePlayerExitRoomReq();
    return message;
  },
};

function createBasePlayerExitRoomRes(): PlayerExitRoomRes {
  return {};
}

export const PlayerExitRoomRes = {
  encode(_: PlayerExitRoomRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerExitRoomRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerExitRoomRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PlayerExitRoomRes {
    return {};
  },

  toJSON(_: PlayerExitRoomRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerExitRoomRes>, I>>(base?: I): PlayerExitRoomRes {
    return PlayerExitRoomRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerExitRoomRes>, I>>(_: I): PlayerExitRoomRes {
    const message = createBasePlayerExitRoomRes();
    return message;
  },
};

function createBaseLeaveRoomBroadcast(): LeaveRoomBroadcast {
  return { bcUid: 0, userId: 0, player: undefined, playersCount: 0 };
}

export const LeaveRoomBroadcast = {
  encode(message: LeaveRoomBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(32).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(24).int32(message.playersCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LeaveRoomBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLeaveRoomBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 4:
          if (tag !== 32) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LeaveRoomBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
    };
  },

  toJSON(message: LeaveRoomBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveRoomBroadcast>, I>>(base?: I): LeaveRoomBroadcast {
    return LeaveRoomBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveRoomBroadcast>, I>>(object: I): LeaveRoomBroadcast {
    const message = createBaseLeaveRoomBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    message.playersCount = object.playersCount ?? 0;
    return message;
  },
};

function createBasePlayerSitDownReq(): PlayerSitDownReq {
  return { seat: 0 };
}

export const PlayerSitDownReq = {
  encode(message: PlayerSitDownReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.seat !== 0) {
      writer.uint32(8).int32(message.seat);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerSitDownReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerSitDownReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.seat = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerSitDownReq {
    return { seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0 };
  },

  toJSON(message: PlayerSitDownReq): unknown {
    const obj: any = {};
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerSitDownReq>, I>>(base?: I): PlayerSitDownReq {
    return PlayerSitDownReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerSitDownReq>, I>>(object: I): PlayerSitDownReq {
    const message = createBasePlayerSitDownReq();
    message.seat = object.seat ?? 0;
    return message;
  },
};

function createBasePlayerSitDownRes(): PlayerSitDownRes {
  return { seat: 0, waitReadyExpiredTime: 0 };
}

export const PlayerSitDownRes = {
  encode(message: PlayerSitDownRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.seat !== 0) {
      writer.uint32(8).int32(message.seat);
    }
    if (message.waitReadyExpiredTime !== 0) {
      writer.uint32(32).int64(message.waitReadyExpiredTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerSitDownRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerSitDownRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.seat = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.waitReadyExpiredTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerSitDownRes {
    return {
      seat: isSet(object.seat) ? globalThis.Number(object.seat) : 0,
      waitReadyExpiredTime: isSet(object.waitReadyExpiredTime) ? globalThis.Number(object.waitReadyExpiredTime) : 0,
    };
  },

  toJSON(message: PlayerSitDownRes): unknown {
    const obj: any = {};
    if (message.seat !== 0) {
      obj.seat = Math.round(message.seat);
    }
    if (message.waitReadyExpiredTime !== 0) {
      obj.waitReadyExpiredTime = Math.round(message.waitReadyExpiredTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerSitDownRes>, I>>(base?: I): PlayerSitDownRes {
    return PlayerSitDownRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerSitDownRes>, I>>(object: I): PlayerSitDownRes {
    const message = createBasePlayerSitDownRes();
    message.seat = object.seat ?? 0;
    message.waitReadyExpiredTime = object.waitReadyExpiredTime ?? 0;
    return message;
  },
};

function createBaseSitDownBroadcast(): SitDownBroadcast {
  return { bcUid: 0, userId: 0, player: undefined, waitReadyExpiredTime: 0 };
}

export const SitDownBroadcast = {
  encode(message: SitDownBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(24).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    if (message.waitReadyExpiredTime !== 0) {
      writer.uint32(32).int64(message.waitReadyExpiredTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SitDownBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSitDownBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3:
          if (tag !== 24) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.waitReadyExpiredTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SitDownBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
      waitReadyExpiredTime: isSet(object.waitReadyExpiredTime) ? globalThis.Number(object.waitReadyExpiredTime) : 0,
    };
  },

  toJSON(message: SitDownBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    if (message.waitReadyExpiredTime !== 0) {
      obj.waitReadyExpiredTime = Math.round(message.waitReadyExpiredTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SitDownBroadcast>, I>>(base?: I): SitDownBroadcast {
    return SitDownBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SitDownBroadcast>, I>>(object: I): SitDownBroadcast {
    const message = createBaseSitDownBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    message.waitReadyExpiredTime = object.waitReadyExpiredTime ?? 0;
    return message;
  },
};

function createBasePlayerStandUpReq(): PlayerStandUpReq {
  return {};
}

export const PlayerStandUpReq = {
  encode(_: PlayerStandUpReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerStandUpReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerStandUpReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PlayerStandUpReq {
    return {};
  },

  toJSON(_: PlayerStandUpReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerStandUpReq>, I>>(base?: I): PlayerStandUpReq {
    return PlayerStandUpReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerStandUpReq>, I>>(_: I): PlayerStandUpReq {
    const message = createBasePlayerStandUpReq();
    return message;
  },
};

function createBasePlayerStandUpRes(): PlayerStandUpRes {
  return {};
}

export const PlayerStandUpRes = {
  encode(_: PlayerStandUpRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerStandUpRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerStandUpRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PlayerStandUpRes {
    return {};
  },

  toJSON(_: PlayerStandUpRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerStandUpRes>, I>>(base?: I): PlayerStandUpRes {
    return PlayerStandUpRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerStandUpRes>, I>>(_: I): PlayerStandUpRes {
    const message = createBasePlayerStandUpRes();
    return message;
  },
};

function createBaseStandUpBroadcast(): StandUpBroadcast {
  return { bcUid: 0, userId: 0, player: undefined };
}

export const StandUpBroadcast = {
  encode(message: StandUpBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(24).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StandUpBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStandUpBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3:
          if (tag !== 24) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StandUpBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
    };
  },

  toJSON(message: StandUpBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StandUpBroadcast>, I>>(base?: I): StandUpBroadcast {
    return StandUpBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StandUpBroadcast>, I>>(object: I): StandUpBroadcast {
    const message = createBaseStandUpBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    return message;
  },
};

function createBasePlayerReadyGameReq(): PlayerReadyGameReq {
  return { ready: false };
}

export const PlayerReadyGameReq = {
  encode(message: PlayerReadyGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ready !== false) {
      writer.uint32(8).bool(message.ready);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerReadyGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerReadyGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ready = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerReadyGameReq {
    return { ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false };
  },

  toJSON(message: PlayerReadyGameReq): unknown {
    const obj: any = {};
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerReadyGameReq>, I>>(base?: I): PlayerReadyGameReq {
    return PlayerReadyGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerReadyGameReq>, I>>(object: I): PlayerReadyGameReq {
    const message = createBasePlayerReadyGameReq();
    message.ready = object.ready ?? false;
    return message;
  },
};

function createBasePlayerReadyGameRes(): PlayerReadyGameRes {
  return { ready: false, waitReadyExpiredTime: 0 };
}

export const PlayerReadyGameRes = {
  encode(message: PlayerReadyGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ready !== false) {
      writer.uint32(8).bool(message.ready);
    }
    if (message.waitReadyExpiredTime !== 0) {
      writer.uint32(16).int64(message.waitReadyExpiredTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerReadyGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerReadyGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ready = reader.bool();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.waitReadyExpiredTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerReadyGameRes {
    return {
      ready: isSet(object.ready) ? globalThis.Boolean(object.ready) : false,
      waitReadyExpiredTime: isSet(object.waitReadyExpiredTime) ? globalThis.Number(object.waitReadyExpiredTime) : 0,
    };
  },

  toJSON(message: PlayerReadyGameRes): unknown {
    const obj: any = {};
    if (message.ready !== false) {
      obj.ready = message.ready;
    }
    if (message.waitReadyExpiredTime !== 0) {
      obj.waitReadyExpiredTime = Math.round(message.waitReadyExpiredTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerReadyGameRes>, I>>(base?: I): PlayerReadyGameRes {
    return PlayerReadyGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerReadyGameRes>, I>>(object: I): PlayerReadyGameRes {
    const message = createBasePlayerReadyGameRes();
    message.ready = object.ready ?? false;
    message.waitReadyExpiredTime = object.waitReadyExpiredTime ?? 0;
    return message;
  },
};

function createBaseReadyBroadcast(): ReadyBroadcast {
  return { bcUid: 0, userId: 0, player: undefined, isReady: false, waitReadyExpiredTime: 0 };
}

export const ReadyBroadcast = {
  encode(message: ReadyBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.bcUid !== 0) {
      writer.uint32(32).int64(message.bcUid);
    }
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.player !== undefined) {
      PlayerInfo.encode(message.player, writer.uint32(18).fork()).ldelim();
    }
    if (message.isReady !== false) {
      writer.uint32(24).bool(message.isReady);
    }
    if (message.waitReadyExpiredTime !== 0) {
      writer.uint32(40).int64(message.waitReadyExpiredTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ReadyBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReadyBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 4:
          if (tag !== 32) {
            break;
          }

          message.bcUid = longToNumber(reader.int64() as Long);
          continue;
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.player = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isReady = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.waitReadyExpiredTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReadyBroadcast {
    return {
      bcUid: isSet(object.bcUid) ? globalThis.Number(object.bcUid) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      player: isSet(object.player) ? PlayerInfo.fromJSON(object.player) : undefined,
      isReady: isSet(object.isReady) ? globalThis.Boolean(object.isReady) : false,
      waitReadyExpiredTime: isSet(object.waitReadyExpiredTime) ? globalThis.Number(object.waitReadyExpiredTime) : 0,
    };
  },

  toJSON(message: ReadyBroadcast): unknown {
    const obj: any = {};
    if (message.bcUid !== 0) {
      obj.bcUid = Math.round(message.bcUid);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.player !== undefined) {
      obj.player = PlayerInfo.toJSON(message.player);
    }
    if (message.isReady !== false) {
      obj.isReady = message.isReady;
    }
    if (message.waitReadyExpiredTime !== 0) {
      obj.waitReadyExpiredTime = Math.round(message.waitReadyExpiredTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadyBroadcast>, I>>(base?: I): ReadyBroadcast {
    return ReadyBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadyBroadcast>, I>>(object: I): ReadyBroadcast {
    const message = createBaseReadyBroadcast();
    message.bcUid = object.bcUid ?? 0;
    message.userId = object.userId ?? 0;
    message.player = (object.player !== undefined && object.player !== null)
      ? PlayerInfo.fromPartial(object.player)
      : undefined;
    message.isReady = object.isReady ?? false;
    message.waitReadyExpiredTime = object.waitReadyExpiredTime ?? 0;
    return message;
  },
};

function createBaseUserInfoUpdateBroadcast(): UserInfoUpdateBroadcast {
  return { userId: 0, coins: 0 };
}

export const UserInfoUpdateBroadcast = {
  encode(message: UserInfoUpdateBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.coins !== 0) {
      writer.uint32(16).int64(message.coins);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): UserInfoUpdateBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserInfoUpdateBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.coins = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserInfoUpdateBroadcast {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      coins: isSet(object.coins) ? globalThis.Number(object.coins) : 0,
    };
  },

  toJSON(message: UserInfoUpdateBroadcast): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.coins !== 0) {
      obj.coins = Math.round(message.coins);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserInfoUpdateBroadcast>, I>>(base?: I): UserInfoUpdateBroadcast {
    return UserInfoUpdateBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserInfoUpdateBroadcast>, I>>(object: I): UserInfoUpdateBroadcast {
    const message = createBaseUserInfoUpdateBroadcast();
    message.userId = object.userId ?? 0;
    message.coins = object.coins ?? 0;
    return message;
  },
};

function createBaseGetHistorySummaryReq(): GetHistorySummaryReq {
  return { userId: 0, gameId: 0, startTime: 0, endTime: 0 };
}

export const GetHistorySummaryReq = {
  encode(message: GetHistorySummaryReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.gameId !== 0) {
      writer.uint32(16).int32(message.gameId);
    }
    if (message.startTime !== 0) {
      writer.uint32(24).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(32).int64(message.endTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetHistorySummaryReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetHistorySummaryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.gameId = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.startTime = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetHistorySummaryReq {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      gameId: isSet(object.gameId) ? globalThis.Number(object.gameId) : 0,
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
    };
  },

  toJSON(message: GetHistorySummaryReq): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.gameId !== 0) {
      obj.gameId = Math.round(message.gameId);
    }
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHistorySummaryReq>, I>>(base?: I): GetHistorySummaryReq {
    return GetHistorySummaryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHistorySummaryReq>, I>>(object: I): GetHistorySummaryReq {
    const message = createBaseGetHistorySummaryReq();
    message.userId = object.userId ?? 0;
    message.gameId = object.gameId ?? 0;
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    return message;
  },
};

function createBaseGetHistorySummaryRes(): GetHistorySummaryRes {
  return { dateSummaries: [] };
}

export const GetHistorySummaryRes = {
  encode(message: GetHistorySummaryRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.dateSummaries) {
      HistoryDateSummary.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetHistorySummaryRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetHistorySummaryRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.dateSummaries.push(HistoryDateSummary.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetHistorySummaryRes {
    return {
      dateSummaries: globalThis.Array.isArray(object?.dateSummaries)
        ? object.dateSummaries.map((e: any) => HistoryDateSummary.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetHistorySummaryRes): unknown {
    const obj: any = {};
    if (message.dateSummaries?.length) {
      obj.dateSummaries = message.dateSummaries.map((e) => HistoryDateSummary.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHistorySummaryRes>, I>>(base?: I): GetHistorySummaryRes {
    return GetHistorySummaryRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHistorySummaryRes>, I>>(object: I): GetHistorySummaryRes {
    const message = createBaseGetHistorySummaryRes();
    message.dateSummaries = object.dateSummaries?.map((e) => HistoryDateSummary.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHistoryDateSummary(): HistoryDateSummary {
  return { date: 0 };
}

export const HistoryDateSummary = {
  encode(message: HistoryDateSummary, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.date !== 0) {
      writer.uint32(8).int32(message.date);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): HistoryDateSummary {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistoryDateSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.date = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HistoryDateSummary {
    return { date: isSet(object.date) ? globalThis.Number(object.date) : 0 };
  },

  toJSON(message: HistoryDateSummary): unknown {
    const obj: any = {};
    if (message.date !== 0) {
      obj.date = Math.round(message.date);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HistoryDateSummary>, I>>(base?: I): HistoryDateSummary {
    return HistoryDateSummary.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistoryDateSummary>, I>>(object: I): HistoryDateSummary {
    const message = createBaseHistoryDateSummary();
    message.date = object.date ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
