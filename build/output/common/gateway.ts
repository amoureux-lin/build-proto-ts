// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: common/gateway.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "gateway.v1";

/**
 * ConnectedMessage 连接成功消息
 * MessageType: GATEWAY_CONNECTED (1)
 */
export interface ConnectedMessage {
  connectionId: string;
  userId: number;
  serverTime: Date | undefined;
}

/**
 * PingMessage Ping消息（心跳+延迟测量）
 * MessageType: GATEWAY_PING_REQ (2)
 */
export interface PingMessage {
  /** 发送时间戳 */
  timestamp:
    | Date
    | undefined;
  /** 客户端标识（可选） */
  clientId: string;
}

/**
 * PongMessage Pong响应消息（心跳确认+延迟信息）
 * MessageType: GATEWAY_PONG_RES (3)
 */
export interface PongMessage {
  /** 响应时间戳 */
  timestamp:
    | Date
    | undefined;
  /** 对应的ping时间戳 */
  pingTimestamp:
    | Date
    | undefined;
  /** 往返时间（毫秒） */
  roundTripTimeMs: number;
}

/**
 * TestMessage 测试消息
 * MessageType: GATEWAY_TEST (4)
 */
export interface TestMessage {
  /** 测试文本内容 */
  message: string;
  /** 测试数字 */
  number: number;
  /** 时间戳 */
  timestamp:
    | Date
    | undefined;
  /** 是否需要回显 */
  echoBack: boolean;
}

function createBaseConnectedMessage(): ConnectedMessage {
  return { connectionId: "", userId: 0, serverTime: undefined };
}

export const ConnectedMessage = {
  encode(message: ConnectedMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.connectionId !== "") {
      writer.uint32(10).string(message.connectionId);
    }
    if (message.userId !== 0) {
      writer.uint32(16).int64(message.userId);
    }
    if (message.serverTime !== undefined) {
      Timestamp.encode(toTimestamp(message.serverTime), writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConnectedMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectedMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.connectionId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.serverTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectedMessage {
    return {
      connectionId: isSet(object.connectionId) ? globalThis.String(object.connectionId) : "",
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      serverTime: isSet(object.serverTime) ? fromJsonTimestamp(object.serverTime) : undefined,
    };
  },

  toJSON(message: ConnectedMessage): unknown {
    const obj: any = {};
    if (message.connectionId !== "") {
      obj.connectionId = message.connectionId;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.serverTime !== undefined) {
      obj.serverTime = message.serverTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectedMessage>, I>>(base?: I): ConnectedMessage {
    return ConnectedMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectedMessage>, I>>(object: I): ConnectedMessage {
    const message = createBaseConnectedMessage();
    message.connectionId = object.connectionId ?? "";
    message.userId = object.userId ?? 0;
    message.serverTime = object.serverTime ?? undefined;
    return message;
  },
};

function createBasePingMessage(): PingMessage {
  return { timestamp: undefined, clientId: "" };
}

export const PingMessage = {
  encode(message: PingMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PingMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingMessage {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      clientId: isSet(object.clientId) ? globalThis.String(object.clientId) : "",
    };
  },

  toJSON(message: PingMessage): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PingMessage>, I>>(base?: I): PingMessage {
    return PingMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PingMessage>, I>>(object: I): PingMessage {
    const message = createBasePingMessage();
    message.timestamp = object.timestamp ?? undefined;
    message.clientId = object.clientId ?? "";
    return message;
  },
};

function createBasePongMessage(): PongMessage {
  return { timestamp: undefined, pingTimestamp: undefined, roundTripTimeMs: 0 };
}

export const PongMessage = {
  encode(message: PongMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).ldelim();
    }
    if (message.pingTimestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.pingTimestamp), writer.uint32(18).fork()).ldelim();
    }
    if (message.roundTripTimeMs !== 0) {
      writer.uint32(24).int64(message.roundTripTimeMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PongMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePongMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pingTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.roundTripTimeMs = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PongMessage {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      pingTimestamp: isSet(object.pingTimestamp) ? fromJsonTimestamp(object.pingTimestamp) : undefined,
      roundTripTimeMs: isSet(object.roundTripTimeMs) ? globalThis.Number(object.roundTripTimeMs) : 0,
    };
  },

  toJSON(message: PongMessage): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.pingTimestamp !== undefined) {
      obj.pingTimestamp = message.pingTimestamp.toISOString();
    }
    if (message.roundTripTimeMs !== 0) {
      obj.roundTripTimeMs = Math.round(message.roundTripTimeMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PongMessage>, I>>(base?: I): PongMessage {
    return PongMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PongMessage>, I>>(object: I): PongMessage {
    const message = createBasePongMessage();
    message.timestamp = object.timestamp ?? undefined;
    message.pingTimestamp = object.pingTimestamp ?? undefined;
    message.roundTripTimeMs = object.roundTripTimeMs ?? 0;
    return message;
  },
};

function createBaseTestMessage(): TestMessage {
  return { message: "", number: 0, timestamp: undefined, echoBack: false };
}

export const TestMessage = {
  encode(message: TestMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.number !== 0) {
      writer.uint32(16).int32(message.number);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(26).fork()).ldelim();
    }
    if (message.echoBack !== false) {
      writer.uint32(32).bool(message.echoBack);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TestMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.number = reader.int32();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.echoBack = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestMessage {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      number: isSet(object.number) ? globalThis.Number(object.number) : 0,
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      echoBack: isSet(object.echoBack) ? globalThis.Boolean(object.echoBack) : false,
    };
  },

  toJSON(message: TestMessage): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.number !== 0) {
      obj.number = Math.round(message.number);
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.echoBack !== false) {
      obj.echoBack = message.echoBack;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TestMessage>, I>>(base?: I): TestMessage {
    return TestMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestMessage>, I>>(object: I): TestMessage {
    const message = createBaseTestMessage();
    message.message = object.message ?? "";
    message.number = object.number ?? 0;
    message.timestamp = object.timestamp ?? undefined;
    message.echoBack = object.echoBack ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
