// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: color/color.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { PlayerInfo, RoomInfo } from "./game_common_room";

export const protobufPackage = "color.v1";

/** color游戏配置信息 */
export interface ColorGameConfig {
  /** 下注列表 */
  betList: number[];
}

/** color玩家信息 */
export interface ColorPlayerInfo {
  /** 玩家信息 */
  playerInfo:
    | PlayerInfo
    | undefined;
  /** 玩家在每个颜色的打码信息 */
  betInfos: BetInfo[];
}

/** 打码信息 */
export interface BetInfo {
  /** 颜色区 */
  area: number;
  /** 打码量 */
  betAmount: number;
  /** 赔率 */
  odds: number;
  /** 赔付金额 */
  payoutAmount: number;
}

/** 结算玩家信息 */
export interface SettlePlayer {
  /** 玩家id */
  playerId: number;
  /** 每个颜色区的赢的金额 */
  winInfos: BetInfo[];
}

/** 游戏信息 */
export interface GameInfo {
  /** 游戏状态 0 等待开始 1.打码中 2.骰子掉落 3.结算 */
  status: number;
  /** 状态结束时间 */
  statusEndTime: number;
  /** 牌局号 */
  roundNum: string;
  /** 币值列表 */
  scores: number[];
  /** 每个颜色区的打码信息 */
  betInfos: BetInfo[];
  /** 骰子信息 */
  diceInfos: number[];
  /** 结算玩家列表 */
  settlePlayers: SettlePlayer[];
  /** 玩家打码上限 */
  playerBetLimit: number[];
}

/**
 * 进入房间请求
 * MessageType: COLOR_JOIN_ROOM_REQ
 */
export interface JoinRoomReq {
  roomId: number;
}

/**
 * 进入房间响应
 * MessageType: COLOR_JOIN_ROOM_RES
 */
export interface JoinRoomRes {
  /** 房间信息 */
  roomInfo:
    | RoomInfo
    | undefined;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 房间内所有玩家数量 */
  playersCount: number;
  /** 所有玩家信息 */
  players: ColorPlayerInfo[];
  /** 玩家自己的信息 */
  self: ColorPlayerInfo | undefined;
}

/**
 * 开始游戏请求
 * MessageType: COLOR_START_GAME_REQ
 */
export interface StartGameReq {
}

/**
 * 开始游戏响应
 * MessageType: COLOR_START_GAME_RES
 */
export interface StartGameRes {
}

/**
 * 游戏状态变化广播
 * MessageType: COLOR_GAME_STATE_CHANGE_BROADCAST
 */
export interface GameStateChangeBroadcast {
  /** 游戏信息 */
  gameInfo: GameInfo | undefined;
}

/**
 * 打码请求
 * MessageType: COLOR_PLAYER_BET_REQ
 */
export interface BetReq {
  /** 打码请求列表 */
  betInfos: BetInfo[];
}

/**
 * 打码响应
 * MessageType: COLOR_PLAYER_BET_RES
 */
export interface BetRes {
}

/**
 * 打码广播
 * MessageType: COLOR_PLAYER_BET_BROADCAST
 */
export interface BetBroadcast {
  /** 打码玩家信息 */
  betPlayer:
    | ColorPlayerInfo
    | undefined;
  /** 打码请求列表 */
  betInfos: BetInfo[];
  roundNum: string;
}

/**
 * 撤销打码
 * MessageType: COLOR_PLAYER_CANCEL_BET_REQ
 */
export interface CancelBetReq {
}

/**
 * 撤销打码响应
 * MessageType: COLOR_PLAYER_CANCEL_BET_RES
 */
export interface CancelBetRes {
}

/**
 * 撤销打码广播
 * MessageType: COLOR_PLAYER_CANCEL_BET_BROADCAST
 */
export interface CancelBetBroadcast {
  /** 撤销打码玩家信息 */
  betPlayer:
    | ColorPlayerInfo
    | undefined;
  /** 撤销打码请求列表 */
  betInfos: BetInfo[];
}

/**
 * 获取骰子统计信息
 * MessageType: COLOR_GET_DICE_STATS_REQ
 */
export interface GetDiceStatsReq {
}

export interface DiceResult {
  /** 骰子点数(1-6) */
  dice: number[];
  /** 各点数对应的赔付倍数 */
  odds: { [key: number]: number };
  /** 时间戳 */
  timestamp: number;
}

export interface DiceResult_OddsEntry {
  key: number;
  value: number;
}

/**
 * 获取骰子统计信息响应
 * MessageType: COLOR_GET_DICE_STATS_RES
 */
export interface GetDiceStatsRes {
  /** 各点数出现概率(百分比) */
  diceProbabilities: { [key: number]: number };
  /** 最近8次结果 */
  recentResults: DiceResult[];
}

export interface GetDiceStatsRes_DiceProbabilitiesEntry {
  key: number;
  value: number;
}

/**
 * 获取玩家EColor游戏历史记录请求
 * MessageType: COLOR_GET_PLAYER_HISTORY_REQ
 */
export interface GetColorPlayerHistoryReq {
  startTime: number;
  endTime: number;
  pageSize: number;
}

/** 玩家EColor游戏历史记录 */
export interface ColorPlayerHistory {
  /** Game Code */
  gameCode: string;
  /** Time */
  endTime: number;
  /** Game Result */
  diceInfos: number[];
  /** Colors Cost Reward */
  betInfos: BetInfo[];
  /** Total Cost */
  cost: number;
  /** Total Reward */
  reward: number;
  /** Win/Lost */
  result: number;
}

/**
 * 获取玩家EColor游戏历史记录响应
 * MessageType: COLOR_GET_PLAYER_HISTORY_RES
 */
export interface GetColorPlayerHistoryRes {
  colorPlayerHistory: ColorPlayerHistory[];
}

function createBaseColorGameConfig(): ColorGameConfig {
  return { betList: [] };
}

export const ColorGameConfig = {
  encode(message: ColorGameConfig, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.betList) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ColorGameConfig {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseColorGameConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.betList.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.betList.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ColorGameConfig {
    return {
      betList: globalThis.Array.isArray(object?.betList) ? object.betList.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: ColorGameConfig): unknown {
    const obj: any = {};
    if (message.betList?.length) {
      obj.betList = message.betList.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ColorGameConfig>, I>>(base?: I): ColorGameConfig {
    return ColorGameConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ColorGameConfig>, I>>(object: I): ColorGameConfig {
    const message = createBaseColorGameConfig();
    message.betList = object.betList?.map((e) => e) || [];
    return message;
  },
};

function createBaseColorPlayerInfo(): ColorPlayerInfo {
  return { playerInfo: undefined, betInfos: [] };
}

export const ColorPlayerInfo = {
  encode(message: ColorPlayerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerInfo !== undefined) {
      PlayerInfo.encode(message.playerInfo, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ColorPlayerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseColorPlayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerInfo = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ColorPlayerInfo {
    return {
      playerInfo: isSet(object.playerInfo) ? PlayerInfo.fromJSON(object.playerInfo) : undefined,
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ColorPlayerInfo): unknown {
    const obj: any = {};
    if (message.playerInfo !== undefined) {
      obj.playerInfo = PlayerInfo.toJSON(message.playerInfo);
    }
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ColorPlayerInfo>, I>>(base?: I): ColorPlayerInfo {
    return ColorPlayerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ColorPlayerInfo>, I>>(object: I): ColorPlayerInfo {
    const message = createBaseColorPlayerInfo();
    message.playerInfo = (object.playerInfo !== undefined && object.playerInfo !== null)
      ? PlayerInfo.fromPartial(object.playerInfo)
      : undefined;
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBetInfo(): BetInfo {
  return { area: 0, betAmount: 0, odds: 0, payoutAmount: 0 };
}

export const BetInfo = {
  encode(message: BetInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.area !== 0) {
      writer.uint32(8).int32(message.area);
    }
    if (message.betAmount !== 0) {
      writer.uint32(16).int32(message.betAmount);
    }
    if (message.odds !== 0) {
      writer.uint32(24).int32(message.odds);
    }
    if (message.payoutAmount !== 0) {
      writer.uint32(32).int32(message.payoutAmount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BetInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBetInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.area = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.betAmount = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.odds = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.payoutAmount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BetInfo {
    return {
      area: isSet(object.area) ? globalThis.Number(object.area) : 0,
      betAmount: isSet(object.betAmount) ? globalThis.Number(object.betAmount) : 0,
      odds: isSet(object.odds) ? globalThis.Number(object.odds) : 0,
      payoutAmount: isSet(object.payoutAmount) ? globalThis.Number(object.payoutAmount) : 0,
    };
  },

  toJSON(message: BetInfo): unknown {
    const obj: any = {};
    if (message.area !== 0) {
      obj.area = Math.round(message.area);
    }
    if (message.betAmount !== 0) {
      obj.betAmount = Math.round(message.betAmount);
    }
    if (message.odds !== 0) {
      obj.odds = Math.round(message.odds);
    }
    if (message.payoutAmount !== 0) {
      obj.payoutAmount = Math.round(message.payoutAmount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BetInfo>, I>>(base?: I): BetInfo {
    return BetInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BetInfo>, I>>(object: I): BetInfo {
    const message = createBaseBetInfo();
    message.area = object.area ?? 0;
    message.betAmount = object.betAmount ?? 0;
    message.odds = object.odds ?? 0;
    message.payoutAmount = object.payoutAmount ?? 0;
    return message;
  },
};

function createBaseSettlePlayer(): SettlePlayer {
  return { playerId: 0, winInfos: [] };
}

export const SettlePlayer = {
  encode(message: SettlePlayer, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    for (const v of message.winInfos) {
      BetInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SettlePlayer {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettlePlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.winInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SettlePlayer {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      winInfos: globalThis.Array.isArray(object?.winInfos) ? object.winInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: SettlePlayer): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.winInfos?.length) {
      obj.winInfos = message.winInfos.map((e) => BetInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SettlePlayer>, I>>(base?: I): SettlePlayer {
    return SettlePlayer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SettlePlayer>, I>>(object: I): SettlePlayer {
    const message = createBaseSettlePlayer();
    message.playerId = object.playerId ?? 0;
    message.winInfos = object.winInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGameInfo(): GameInfo {
  return {
    status: 0,
    statusEndTime: 0,
    roundNum: "",
    scores: [],
    betInfos: [],
    diceInfos: [],
    settlePlayers: [],
    playerBetLimit: [],
  };
}

export const GameInfo = {
  encode(message: GameInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.statusEndTime !== 0) {
      writer.uint32(16).int64(message.statusEndTime);
    }
    if (message.roundNum !== "") {
      writer.uint32(26).string(message.roundNum);
    }
    writer.uint32(34).fork();
    for (const v of message.scores) {
      writer.int32(v);
    }
    writer.ldelim();
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    writer.uint32(50).fork();
    for (const v of message.diceInfos) {
      writer.int32(v);
    }
    writer.ldelim();
    for (const v of message.settlePlayers) {
      SettlePlayer.encode(v!, writer.uint32(58).fork()).ldelim();
    }
    writer.uint32(66).fork();
    for (const v of message.playerBetLimit) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.statusEndTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.roundNum = reader.string();
          continue;
        case 4:
          if (tag === 32) {
            message.scores.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.scores.push(reader.int32());
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag === 48) {
            message.diceInfos.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.diceInfos.push(reader.int32());
            }

            continue;
          }

          break;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.settlePlayers.push(SettlePlayer.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag === 64) {
            message.playerBetLimit.push(reader.int32());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.playerBetLimit.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameInfo {
    return {
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      statusEndTime: isSet(object.statusEndTime) ? globalThis.Number(object.statusEndTime) : 0,
      roundNum: isSet(object.roundNum) ? globalThis.String(object.roundNum) : "",
      scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => globalThis.Number(e)) : [],
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
      diceInfos: globalThis.Array.isArray(object?.diceInfos)
        ? object.diceInfos.map((e: any) => globalThis.Number(e))
        : [],
      settlePlayers: globalThis.Array.isArray(object?.settlePlayers)
        ? object.settlePlayers.map((e: any) => SettlePlayer.fromJSON(e))
        : [],
      playerBetLimit: globalThis.Array.isArray(object?.playerBetLimit)
        ? object.playerBetLimit.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: GameInfo): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.statusEndTime !== 0) {
      obj.statusEndTime = Math.round(message.statusEndTime);
    }
    if (message.roundNum !== "") {
      obj.roundNum = message.roundNum;
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => Math.round(e));
    }
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    if (message.diceInfos?.length) {
      obj.diceInfos = message.diceInfos.map((e) => Math.round(e));
    }
    if (message.settlePlayers?.length) {
      obj.settlePlayers = message.settlePlayers.map((e) => SettlePlayer.toJSON(e));
    }
    if (message.playerBetLimit?.length) {
      obj.playerBetLimit = message.playerBetLimit.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameInfo>, I>>(base?: I): GameInfo {
    return GameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameInfo>, I>>(object: I): GameInfo {
    const message = createBaseGameInfo();
    message.status = object.status ?? 0;
    message.statusEndTime = object.statusEndTime ?? 0;
    message.roundNum = object.roundNum ?? "";
    message.scores = object.scores?.map((e) => e) || [];
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    message.diceInfos = object.diceInfos?.map((e) => e) || [];
    message.settlePlayers = object.settlePlayers?.map((e) => SettlePlayer.fromPartial(e)) || [];
    message.playerBetLimit = object.playerBetLimit?.map((e) => e) || [];
    return message;
  },
};

function createBaseJoinRoomReq(): JoinRoomReq {
  return { roomId: 0 };
}

export const JoinRoomReq = {
  encode(message: JoinRoomReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomReq {
    return { roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0 };
  },

  toJSON(message: JoinRoomReq): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomReq>, I>>(base?: I): JoinRoomReq {
    return JoinRoomReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomReq>, I>>(object: I): JoinRoomReq {
    const message = createBaseJoinRoomReq();
    message.roomId = object.roomId ?? 0;
    return message;
  },
};

function createBaseJoinRoomRes(): JoinRoomRes {
  return { roomInfo: undefined, gameInfo: undefined, playersCount: 0, players: [], self: undefined };
}

export const JoinRoomRes = {
  encode(message: JoinRoomRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomInfo !== undefined) {
      RoomInfo.encode(message.roomInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(18).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(24).int32(message.playersCount);
    }
    for (const v of message.players) {
      ColorPlayerInfo.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.self !== undefined) {
      ColorPlayerInfo.encode(message.self, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomInfo = RoomInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.players.push(ColorPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.self = ColorPlayerInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRes {
    return {
      roomInfo: isSet(object.roomInfo) ? RoomInfo.fromJSON(object.roomInfo) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => ColorPlayerInfo.fromJSON(e))
        : [],
      self: isSet(object.self) ? ColorPlayerInfo.fromJSON(object.self) : undefined,
    };
  },

  toJSON(message: JoinRoomRes): unknown {
    const obj: any = {};
    if (message.roomInfo !== undefined) {
      obj.roomInfo = RoomInfo.toJSON(message.roomInfo);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => ColorPlayerInfo.toJSON(e));
    }
    if (message.self !== undefined) {
      obj.self = ColorPlayerInfo.toJSON(message.self);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomRes>, I>>(base?: I): JoinRoomRes {
    return JoinRoomRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomRes>, I>>(object: I): JoinRoomRes {
    const message = createBaseJoinRoomRes();
    message.roomInfo = (object.roomInfo !== undefined && object.roomInfo !== null)
      ? RoomInfo.fromPartial(object.roomInfo)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.playersCount = object.playersCount ?? 0;
    message.players = object.players?.map((e) => ColorPlayerInfo.fromPartial(e)) || [];
    message.self = (object.self !== undefined && object.self !== null)
      ? ColorPlayerInfo.fromPartial(object.self)
      : undefined;
    return message;
  },
};

function createBaseStartGameReq(): StartGameReq {
  return {};
}

export const StartGameReq = {
  encode(_: StartGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameReq {
    return {};
  },

  toJSON(_: StartGameReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameReq>, I>>(base?: I): StartGameReq {
    return StartGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameReq>, I>>(_: I): StartGameReq {
    const message = createBaseStartGameReq();
    return message;
  },
};

function createBaseStartGameRes(): StartGameRes {
  return {};
}

export const StartGameRes = {
  encode(_: StartGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameRes {
    return {};
  },

  toJSON(_: StartGameRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameRes>, I>>(base?: I): StartGameRes {
    return StartGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameRes>, I>>(_: I): StartGameRes {
    const message = createBaseStartGameRes();
    return message;
  },
};

function createBaseGameStateChangeBroadcast(): GameStateChangeBroadcast {
  return { gameInfo: undefined };
}

export const GameStateChangeBroadcast = {
  encode(message: GameStateChangeBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameStateChangeBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStateChangeBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStateChangeBroadcast {
    return { gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined };
  },

  toJSON(message: GameStateChangeBroadcast): unknown {
    const obj: any = {};
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStateChangeBroadcast>, I>>(base?: I): GameStateChangeBroadcast {
    return GameStateChangeBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStateChangeBroadcast>, I>>(object: I): GameStateChangeBroadcast {
    const message = createBaseGameStateChangeBroadcast();
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    return message;
  },
};

function createBaseBetReq(): BetReq {
  return { betInfos: [] };
}

export const BetReq = {
  encode(message: BetReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BetReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBetReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BetReq {
    return {
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: BetReq): unknown {
    const obj: any = {};
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BetReq>, I>>(base?: I): BetReq {
    return BetReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BetReq>, I>>(object: I): BetReq {
    const message = createBaseBetReq();
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBetRes(): BetRes {
  return {};
}

export const BetRes = {
  encode(_: BetRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BetRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBetRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): BetRes {
    return {};
  },

  toJSON(_: BetRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<BetRes>, I>>(base?: I): BetRes {
    return BetRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BetRes>, I>>(_: I): BetRes {
    const message = createBaseBetRes();
    return message;
  },
};

function createBaseBetBroadcast(): BetBroadcast {
  return { betPlayer: undefined, betInfos: [], roundNum: "" };
}

export const BetBroadcast = {
  encode(message: BetBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.betPlayer !== undefined) {
      ColorPlayerInfo.encode(message.betPlayer, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.roundNum !== "") {
      writer.uint32(26).string(message.roundNum);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BetBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBetBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.betPlayer = ColorPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.roundNum = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BetBroadcast {
    return {
      betPlayer: isSet(object.betPlayer) ? ColorPlayerInfo.fromJSON(object.betPlayer) : undefined,
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
      roundNum: isSet(object.roundNum) ? globalThis.String(object.roundNum) : "",
    };
  },

  toJSON(message: BetBroadcast): unknown {
    const obj: any = {};
    if (message.betPlayer !== undefined) {
      obj.betPlayer = ColorPlayerInfo.toJSON(message.betPlayer);
    }
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    if (message.roundNum !== "") {
      obj.roundNum = message.roundNum;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BetBroadcast>, I>>(base?: I): BetBroadcast {
    return BetBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BetBroadcast>, I>>(object: I): BetBroadcast {
    const message = createBaseBetBroadcast();
    message.betPlayer = (object.betPlayer !== undefined && object.betPlayer !== null)
      ? ColorPlayerInfo.fromPartial(object.betPlayer)
      : undefined;
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    message.roundNum = object.roundNum ?? "";
    return message;
  },
};

function createBaseCancelBetReq(): CancelBetReq {
  return {};
}

export const CancelBetReq = {
  encode(_: CancelBetReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CancelBetReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelBetReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CancelBetReq {
    return {};
  },

  toJSON(_: CancelBetReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelBetReq>, I>>(base?: I): CancelBetReq {
    return CancelBetReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelBetReq>, I>>(_: I): CancelBetReq {
    const message = createBaseCancelBetReq();
    return message;
  },
};

function createBaseCancelBetRes(): CancelBetRes {
  return {};
}

export const CancelBetRes = {
  encode(_: CancelBetRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CancelBetRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelBetRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CancelBetRes {
    return {};
  },

  toJSON(_: CancelBetRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelBetRes>, I>>(base?: I): CancelBetRes {
    return CancelBetRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelBetRes>, I>>(_: I): CancelBetRes {
    const message = createBaseCancelBetRes();
    return message;
  },
};

function createBaseCancelBetBroadcast(): CancelBetBroadcast {
  return { betPlayer: undefined, betInfos: [] };
}

export const CancelBetBroadcast = {
  encode(message: CancelBetBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.betPlayer !== undefined) {
      ColorPlayerInfo.encode(message.betPlayer, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CancelBetBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelBetBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.betPlayer = ColorPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelBetBroadcast {
    return {
      betPlayer: isSet(object.betPlayer) ? ColorPlayerInfo.fromJSON(object.betPlayer) : undefined,
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: CancelBetBroadcast): unknown {
    const obj: any = {};
    if (message.betPlayer !== undefined) {
      obj.betPlayer = ColorPlayerInfo.toJSON(message.betPlayer);
    }
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelBetBroadcast>, I>>(base?: I): CancelBetBroadcast {
    return CancelBetBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelBetBroadcast>, I>>(object: I): CancelBetBroadcast {
    const message = createBaseCancelBetBroadcast();
    message.betPlayer = (object.betPlayer !== undefined && object.betPlayer !== null)
      ? ColorPlayerInfo.fromPartial(object.betPlayer)
      : undefined;
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetDiceStatsReq(): GetDiceStatsReq {
  return {};
}

export const GetDiceStatsReq = {
  encode(_: GetDiceStatsReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDiceStatsReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDiceStatsReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetDiceStatsReq {
    return {};
  },

  toJSON(_: GetDiceStatsReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDiceStatsReq>, I>>(base?: I): GetDiceStatsReq {
    return GetDiceStatsReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDiceStatsReq>, I>>(_: I): GetDiceStatsReq {
    const message = createBaseGetDiceStatsReq();
    return message;
  },
};

function createBaseDiceResult(): DiceResult {
  return { dice: [], odds: {}, timestamp: 0 };
}

export const DiceResult = {
  encode(message: DiceResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.dice) {
      writer.int32(v);
    }
    writer.ldelim();
    Object.entries(message.odds).forEach(([key, value]) => {
      DiceResult_OddsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).ldelim();
    });
    if (message.timestamp !== 0) {
      writer.uint32(24).int64(message.timestamp);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DiceResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiceResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.dice.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.dice.push(reader.int32());
            }

            continue;
          }

          break;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = DiceResult_OddsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.odds[entry2.key] = entry2.value;
          }
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.timestamp = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiceResult {
    return {
      dice: globalThis.Array.isArray(object?.dice) ? object.dice.map((e: any) => globalThis.Number(e)) : [],
      odds: isObject(object.odds)
        ? Object.entries(object.odds).reduce<{ [key: number]: number }>((acc, [key, value]) => {
          acc[globalThis.Number(key)] = Number(value);
          return acc;
        }, {})
        : {},
      timestamp: isSet(object.timestamp) ? globalThis.Number(object.timestamp) : 0,
    };
  },

  toJSON(message: DiceResult): unknown {
    const obj: any = {};
    if (message.dice?.length) {
      obj.dice = message.dice.map((e) => Math.round(e));
    }
    if (message.odds) {
      const entries = Object.entries(message.odds);
      if (entries.length > 0) {
        obj.odds = {};
        entries.forEach(([k, v]) => {
          obj.odds[k] = Math.round(v);
        });
      }
    }
    if (message.timestamp !== 0) {
      obj.timestamp = Math.round(message.timestamp);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiceResult>, I>>(base?: I): DiceResult {
    return DiceResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiceResult>, I>>(object: I): DiceResult {
    const message = createBaseDiceResult();
    message.dice = object.dice?.map((e) => e) || [];
    message.odds = Object.entries(object.odds ?? {}).reduce<{ [key: number]: number }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[globalThis.Number(key)] = globalThis.Number(value);
      }
      return acc;
    }, {});
    message.timestamp = object.timestamp ?? 0;
    return message;
  },
};

function createBaseDiceResult_OddsEntry(): DiceResult_OddsEntry {
  return { key: 0, value: 0 };
}

export const DiceResult_OddsEntry = {
  encode(message: DiceResult_OddsEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== 0) {
      writer.uint32(8).int32(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DiceResult_OddsEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiceResult_OddsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.key = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiceResult_OddsEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: DiceResult_OddsEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiceResult_OddsEntry>, I>>(base?: I): DiceResult_OddsEntry {
    return DiceResult_OddsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiceResult_OddsEntry>, I>>(object: I): DiceResult_OddsEntry {
    const message = createBaseDiceResult_OddsEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseGetDiceStatsRes(): GetDiceStatsRes {
  return { diceProbabilities: {}, recentResults: [] };
}

export const GetDiceStatsRes = {
  encode(message: GetDiceStatsRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    Object.entries(message.diceProbabilities).forEach(([key, value]) => {
      GetDiceStatsRes_DiceProbabilitiesEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).ldelim();
    });
    for (const v of message.recentResults) {
      DiceResult.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDiceStatsRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDiceStatsRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          const entry1 = GetDiceStatsRes_DiceProbabilitiesEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.diceProbabilities[entry1.key] = entry1.value;
          }
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.recentResults.push(DiceResult.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDiceStatsRes {
    return {
      diceProbabilities: isObject(object.diceProbabilities)
        ? Object.entries(object.diceProbabilities).reduce<{ [key: number]: number }>((acc, [key, value]) => {
          acc[globalThis.Number(key)] = Number(value);
          return acc;
        }, {})
        : {},
      recentResults: globalThis.Array.isArray(object?.recentResults)
        ? object.recentResults.map((e: any) => DiceResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetDiceStatsRes): unknown {
    const obj: any = {};
    if (message.diceProbabilities) {
      const entries = Object.entries(message.diceProbabilities);
      if (entries.length > 0) {
        obj.diceProbabilities = {};
        entries.forEach(([k, v]) => {
          obj.diceProbabilities[k] = Math.round(v);
        });
      }
    }
    if (message.recentResults?.length) {
      obj.recentResults = message.recentResults.map((e) => DiceResult.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDiceStatsRes>, I>>(base?: I): GetDiceStatsRes {
    return GetDiceStatsRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDiceStatsRes>, I>>(object: I): GetDiceStatsRes {
    const message = createBaseGetDiceStatsRes();
    message.diceProbabilities = Object.entries(object.diceProbabilities ?? {}).reduce<{ [key: number]: number }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.recentResults = object.recentResults?.map((e) => DiceResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetDiceStatsRes_DiceProbabilitiesEntry(): GetDiceStatsRes_DiceProbabilitiesEntry {
  return { key: 0, value: 0 };
}

export const GetDiceStatsRes_DiceProbabilitiesEntry = {
  encode(message: GetDiceStatsRes_DiceProbabilitiesEntry, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.key !== 0) {
      writer.uint32(8).int32(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDiceStatsRes_DiceProbabilitiesEntry {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDiceStatsRes_DiceProbabilitiesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.key = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDiceStatsRes_DiceProbabilitiesEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: GetDiceStatsRes_DiceProbabilitiesEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDiceStatsRes_DiceProbabilitiesEntry>, I>>(
    base?: I,
  ): GetDiceStatsRes_DiceProbabilitiesEntry {
    return GetDiceStatsRes_DiceProbabilitiesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDiceStatsRes_DiceProbabilitiesEntry>, I>>(
    object: I,
  ): GetDiceStatsRes_DiceProbabilitiesEntry {
    const message = createBaseGetDiceStatsRes_DiceProbabilitiesEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseGetColorPlayerHistoryReq(): GetColorPlayerHistoryReq {
  return { startTime: 0, endTime: 0, pageSize: 0 };
}

export const GetColorPlayerHistoryReq = {
  encode(message: GetColorPlayerHistoryReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.startTime !== 0) {
      writer.uint32(8).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetColorPlayerHistoryReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetColorPlayerHistoryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.startTime = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetColorPlayerHistoryReq {
    return {
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
    };
  },

  toJSON(message: GetColorPlayerHistoryReq): unknown {
    const obj: any = {};
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetColorPlayerHistoryReq>, I>>(base?: I): GetColorPlayerHistoryReq {
    return GetColorPlayerHistoryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetColorPlayerHistoryReq>, I>>(object: I): GetColorPlayerHistoryReq {
    const message = createBaseGetColorPlayerHistoryReq();
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    message.pageSize = object.pageSize ?? 0;
    return message;
  },
};

function createBaseColorPlayerHistory(): ColorPlayerHistory {
  return { gameCode: "", endTime: 0, diceInfos: [], betInfos: [], cost: 0, reward: 0, result: 0 };
}

export const ColorPlayerHistory = {
  encode(message: ColorPlayerHistory, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameCode !== "") {
      writer.uint32(10).string(message.gameCode);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    writer.uint32(26).fork();
    for (const v of message.diceInfos) {
      writer.int32(v);
    }
    writer.ldelim();
    for (const v of message.betInfos) {
      BetInfo.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    if (message.cost !== 0) {
      writer.uint32(40).int32(message.cost);
    }
    if (message.reward !== 0) {
      writer.uint32(48).int32(message.reward);
    }
    if (message.result !== 0) {
      writer.uint32(56).int32(message.result);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ColorPlayerHistory {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseColorPlayerHistory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameCode = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag === 24) {
            message.diceInfos.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.diceInfos.push(reader.int32());
            }

            continue;
          }

          break;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.betInfos.push(BetInfo.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.cost = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.reward = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.result = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ColorPlayerHistory {
    return {
      gameCode: isSet(object.gameCode) ? globalThis.String(object.gameCode) : "",
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      diceInfos: globalThis.Array.isArray(object?.diceInfos)
        ? object.diceInfos.map((e: any) => globalThis.Number(e))
        : [],
      betInfos: globalThis.Array.isArray(object?.betInfos) ? object.betInfos.map((e: any) => BetInfo.fromJSON(e)) : [],
      cost: isSet(object.cost) ? globalThis.Number(object.cost) : 0,
      reward: isSet(object.reward) ? globalThis.Number(object.reward) : 0,
      result: isSet(object.result) ? globalThis.Number(object.result) : 0,
    };
  },

  toJSON(message: ColorPlayerHistory): unknown {
    const obj: any = {};
    if (message.gameCode !== "") {
      obj.gameCode = message.gameCode;
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.diceInfos?.length) {
      obj.diceInfos = message.diceInfos.map((e) => Math.round(e));
    }
    if (message.betInfos?.length) {
      obj.betInfos = message.betInfos.map((e) => BetInfo.toJSON(e));
    }
    if (message.cost !== 0) {
      obj.cost = Math.round(message.cost);
    }
    if (message.reward !== 0) {
      obj.reward = Math.round(message.reward);
    }
    if (message.result !== 0) {
      obj.result = Math.round(message.result);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ColorPlayerHistory>, I>>(base?: I): ColorPlayerHistory {
    return ColorPlayerHistory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ColorPlayerHistory>, I>>(object: I): ColorPlayerHistory {
    const message = createBaseColorPlayerHistory();
    message.gameCode = object.gameCode ?? "";
    message.endTime = object.endTime ?? 0;
    message.diceInfos = object.diceInfos?.map((e) => e) || [];
    message.betInfos = object.betInfos?.map((e) => BetInfo.fromPartial(e)) || [];
    message.cost = object.cost ?? 0;
    message.reward = object.reward ?? 0;
    message.result = object.result ?? 0;
    return message;
  },
};

function createBaseGetColorPlayerHistoryRes(): GetColorPlayerHistoryRes {
  return { colorPlayerHistory: [] };
}

export const GetColorPlayerHistoryRes = {
  encode(message: GetColorPlayerHistoryRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.colorPlayerHistory) {
      ColorPlayerHistory.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetColorPlayerHistoryRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetColorPlayerHistoryRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.colorPlayerHistory.push(ColorPlayerHistory.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetColorPlayerHistoryRes {
    return {
      colorPlayerHistory: globalThis.Array.isArray(object?.colorPlayerHistory)
        ? object.colorPlayerHistory.map((e: any) => ColorPlayerHistory.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetColorPlayerHistoryRes): unknown {
    const obj: any = {};
    if (message.colorPlayerHistory?.length) {
      obj.colorPlayerHistory = message.colorPlayerHistory.map((e) => ColorPlayerHistory.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetColorPlayerHistoryRes>, I>>(base?: I): GetColorPlayerHistoryRes {
    return GetColorPlayerHistoryRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetColorPlayerHistoryRes>, I>>(object: I): GetColorPlayerHistoryRes {
    const message = createBaseGetColorPlayerHistoryRes();
    message.colorPlayerHistory = object.colorPlayerHistory?.map((e) => ColorPlayerHistory.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
