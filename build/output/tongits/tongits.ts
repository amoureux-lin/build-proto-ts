// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: tongits/tongits.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { PlayerInfo } from "./game_common_room";

export const protobufPackage = "tongits.v1";

/** 空消息 */
export interface EmptyRes {
}

/** 房间信息 */
export interface RoomInfo {
  roomId: number;
  roomName: string;
  /** 房间状态 */
  roomStatus: number;
  /** 最大座位数 */
  maxSeat: number;
}

/** 玩家在游戏中的完整信息 */
export interface TongitsPlayerInfo {
  /** 基础玩家信息 */
  playerInfo:
    | PlayerInfo
    | undefined;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  /** 是否是庄家 */
  isDealer: boolean;
  /** 已经打出的牌组 (明牌) */
  displayedMelds: Meld[];
  /** 自己的手牌 */
  handCards: number[];
  /** 是否可以发起挑战 */
  isFight: boolean;
  /** 时间戳 */
  countdown: number;
  /** 1:默认待选择 2:发起 3:接受 4:拒绝 5:烧死 */
  changeStatus: number;
  /** 1:不可操作, 2:select, 3:Action */
  status: number;
  /** 是否赢得比赛 */
  isWin: boolean;
  /** 点数 */
  cardPoint: number;
}

export interface TongitsBasePlayerInfo {
  /** 玩家ID */
  playerId: number;
  /** 1:默认待选择 2:发起 3:接受 4:拒绝 5:烧死 */
  changeStatus: number;
  /** 剩余时间 */
  countdown: number;
}

/** 牌组结构 */
export interface Meld {
  /** 组成牌组的牌 */
  cards: number[];
  /** 高亮的牌 (可见) */
  highlightCards: number;
  /** 这个牌组属于哪个玩家 */
  ownerId: number;
  /** 牌组的唯一ID (例如，玩家Melds数组的索引) */
  meldId: number;
  /** 牌组是否锁定 (动画表现) */
  locked: boolean;
}

/** 游戏状态的核心信息 */
export interface GameInfo {
  /** 当前操作玩家ID */
  actionPlayerId: number;
  /** 游戏阶段 1:初始 2:游戏中  3: 挑战中 4: 结算前  5:结算中 */
  status: number;
  /** 牌堆剩余牌数 */
  deckCardCount: number;
  /** 弃牌堆的牌 从下到上 */
  discardPile: number[];
  /** 底池信息 */
  pot:
    | PotInfo
    | undefined;
  /** 弃牌显示牌 */
  discardCard: number;
  /** 胜利类型  1 tongits 2 挑战 3 时间结束比大小: */
  winType: number;
  /** 视角玩家id */
  perspectiveId: number;
  /** 当前下注币值 */
  betAmount: number;
  /** 币值列表 */
  scores: number[];
}

/** 底池信息 */
export interface PotInfo {
  /** 底池金额 */
  base: number;
  /** 连续未被赢取的次数 */
  winCount: number;
  /** 当前所属玩家id */
  useId: number;
}

export interface PlayerResult {
  playerInfo:
    | TongitsPlayerInfo
    | undefined;
  /** 总输赢获得的钱 */
  sumWinBonus: number;
  /** 正常输赢获得的钱 */
  normalWinBonus: number;
  /** Tongits输赢获得的钱 */
  tongitsWinBonus: number;
  /** 牌型奖励获得的钱 */
  cardTypeBonus: number;
  /** 奖励点 宝牌获得的钱 */
  bonusBonus: number;
  /** 烧牌获得的钱 */
  burnedBonus: number;
  /** 赢得挑战获得的钱 */
  winChallengeBonus: number;
  /** 底池获得的钱 */
  potBonus: number;
  /** 点数 */
  cardPoint: number;
}

/**
 * 进入房间
 * MessageType: TONGITS_JOIN_ROOM_REQ (3001)
 */
export interface JoinRoomReq {
  roomId: number;
}

/**
 * 进入房间
 * MessageType: TONGITS_JOIN_ROOM_RES (3002)
 */
export interface JoinRoomRes {
  /** 房间信息 */
  roomInfo: RoomInfo | undefined;
  players: TongitsPlayerInfo[];
  /** 旁观者信息 */
  watchers: PlayerInfo[];
  /** 旁观者数量 */
  playersCount: number;
  /** 开麦用户列表 */
  speakers: PlayerInfo[];
  /** 自己信息 */
  self:
    | TongitsPlayerInfo
    | undefined;
  /** 游戏信息 */
  gameInfo: GameInfo | undefined;
}

/**
 * 开始游戏
 * MessageType: TONGITS_ROOM_OWNER_START_GAME_REQ (3019)
 */
export interface StartGameReq {
}

/**
 * 开始游戏
 * MessageType: TONGITS_ROOM_OWNER_START_GAME_RES (3020)
 */
export interface StartGameRes {
}

/**
 * 游戏开始广播
 * MessageType: TONGITS_START_GAME_BROADCAST (3015)
 */
export interface GameStartBroadcast {
  /** 游戏初始状态 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 座位上的玩家信息 */
  players: TongitsPlayerInfo[];
  userId: number;
}

/**
 * tongits 点击请求
 * MessageType: TONGITS_WIN_CLICK_REQ (3031)
 */
export interface TongitsReq {
}

/**
 * tongits 点击响应
 * MessageType: TONGITS_WIN_CLICK_RES (3032)
 */
export interface TongitsRes {
}

/**
 * 抽牌
 * MessageType: TONGITS_DRAW_REQ (3003)
 */
export interface DrawCardReq {
}

/**
 * 抽牌
 * MessageType: TONGITS_DRAW_RES (3004)
 */
export interface DrawCardRes {
  /** 抽到的牌，只有自己知道 */
  drawnCard: number;
  /** 是否有Tongits */
  hasTongits: boolean;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
}

/**
 * 抽牌广播
 * MessageType: TONGITS_DRAW_BROADCAST (3021)
 */
export interface DrawCardBroadcast {
  playerId: number;
  userId: number;
  /** 当自动抽牌时  自己会有这个值 */
  drawnCard: number;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
}

/**
 * 出牌
 * MessageType: TONGITS_MELD_REQ (3005)
 */
export interface MeldCardReq {
  cards: number[];
}

/**
 * 出牌
 * MessageType: TONGITS_MELD_RES (3006)
 */
export interface MeldCardRes {
  /** 新打出的牌组 */
  newMeld:
    | Meld
    | undefined;
  /** 是否有Tongits */
  hasTongits: boolean;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
}

/**
 * 出牌广播
 * MessageType: TONGITS_MELD_BROADCAST (3022)
 */
export interface MeldCardBroadcast {
  playerId: number;
  /** 新打出的牌组 */
  newMeld:
    | Meld
    | undefined;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  userId: number;
}

/**
 * 补牌/压牌
 * MessageType: TONGITS_LAYOFF_REQ (3007)
 */
export interface LayOffCardReq {
  /** 要补的牌 */
  card: number;
  /** 目标牌组的拥有者ID */
  targetPlayerId: number;
  /** 目标牌组的ID */
  targetMeldId: number;
}

/**
 * 补牌/压牌
 * MessageType: TONGITS_LAYOFF_RES (3008)
 */
export interface LayOffCardRes {
  /** 要补的牌 */
  cardAdded: number;
  /** 目标牌组的拥有者ID */
  targetPlayerId: number;
  /** 目标牌组的ID */
  targetMeldId: number;
  /** 是否有Tongits */
  hasTongits: boolean;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
}

/**
 * 补牌/压牌广播
 * MessageType: TONGITS_LAYOFF_BROADCAST (3023)
 */
export interface LayOffCardBroadcast {
  /** 操作者 */
  actionPlayerId: number;
  /** 补的牌 */
  cardAdded: number;
  /** 目标玩家 */
  targetPlayerId: number;
  /** 目标牌组 */
  targetMeldId: number;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  userId: number;
}

/**
 * 打牌
 * MessageType: TONGITS_DISCARD_REQ (3009)
 */
export interface DiscardCardReq {
  card: number;
}

/**
 * 打牌
 * MessageType: TONGITS_DISCARD_RES (3010)
 */
export interface DiscardCardRes {
  discardedCard: number;
  /** 解锁的牌组ID列表 */
  unlockMelds: number[];
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  /** 弃牌堆的牌 从下到上 */
  discardPile: number[];
}

/**
 * 打牌广播
 * MessageType: TONGITS_DISCARD_BROADCAST (3024)
 */
export interface DiscardCardBroadcast {
  playerId: number;
  discardedCard: number;
  /** 解锁的牌组ID列表 */
  unlockMelds: number[];
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  /** 弃牌堆的牌 从下到上 */
  discardPile: number[];
  userId: number;
}

/**
 * 吃牌
 * MessageType: TONGITS_TAKE_REQ (3011)
 */
export interface TakeCardReq {
  /** 手中用来组合的牌 */
  cardsFromHand: number[];
}

/**
 * 吃牌
 * MessageType: TONGITS_TAKE_RES (3012)
 */
export interface TakeCardRes {
  /** 吃牌后形成的新牌组 */
  newMeld:
    | Meld
    | undefined;
  /** 是否有Tongits */
  hasTongits: boolean;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  /** 弃牌显示牌 */
  discard: number;
}

/**
 * 吃牌广播
 * MessageType: TONGITS_TAKE_BROADCAST (3025)
 */
export interface TakeCardBroadcast {
  playerId: number;
  /** 吃牌后形成的新牌组 */
  newMeld:
    | Meld
    | undefined;
  /** 手牌数量 (对其他玩家可见) */
  handCardCount: number;
  /** 弃牌显示牌 */
  discard: number;
  userId: number;
}

/**
 * 挑战/pk
 * MessageType: TONGITS_CHALLENGE_ACTION_REQ (3013)
 */
export interface ChallengeReq {
  /** 2:发起 3:接受 4:拒绝 */
  changeStatus: number;
}

/**
 * 挑战/pk
 * MessageType: TONGITS_CHALLENGE_ACTION_RES (3014)
 */
export interface ChallengeRes {
  basePlayers: TongitsBasePlayerInfo[];
}

/**
 * 挑战广播
 * MessageType: TONGITS_CHALLENGE_BROADCAST (3026)
 */
export interface ChallengeBroadcast {
  playerId: number;
  basePlayers: TongitsBasePlayerInfo[];
  userId: number;
}

/**
 * PK广播
 * MessageType: TONGITS_PK_BROADCAST (3027)
 */
export interface PKBroadcast {
  playerId: number;
  /** 2:发起 3:接受 4:拒绝 */
  changeStatus: number;
  userId: number;
}

/**
 * 游戏结算广播
 * MessageType: TONGITS_GAME_RESULT_BROADCAST (3016)
 */
export interface GameResultBroadcast {
  winnerId: number;
  playerResults: PlayerResult[];
  /** 时间戳 */
  countdown: number;
  userId: number;
}

/**
 * 房间重置广播
 * MessageType: TONGITS_ROOM_RESET_BROADCAST (3017)
 */
export interface RoomResetBroadcast {
  players: TongitsPlayerInfo[];
  /** 自己信息 */
  self:
    | TongitsPlayerInfo
    | undefined;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 通知玩家id */
  userId: number;
}

/**
 * 操作变动广播
 * MessageType: TONGITS_ACTION_CHANGE_BROADCAST (3018)
 */
export interface ActionChangeBroadcast {
  /** 操作玩家 */
  actionPlayerId: number;
  /** 倒计时 */
  countdown: number;
  /** 是否可以发起挑战 */
  isFight: boolean;
  /** 1:不可操作, 2:select, 3:Action */
  status: number;
  /** 通知玩家id */
  userId: number;
}

/**
 * 结算前比牌 广播
 * MessageType: TONGITS_GAME_WIN_BROADCAST (3028)
 */
export interface BeforeResultBroadcast {
  winnerId: number;
  /** 胜利类型  1 tongits 2 挑战 3 时间结束比大小: */
  winType: number;
  players: TongitsPlayerInfo[];
  /** 时间戳 */
  countdown: number;
  /** 底池信息 */
  pot:
    | PotInfo
    | undefined;
  /** 通知玩家id */
  userId: number;
}

/**
 * 获取游戏结算详情数据
 * MessageType: TONGITS_GAME_RESULT_DETAILS_REQ (3029)
 */
export interface GameResultDetailsReq {
}

/**
 * 获取游戏结算详情数据
 * MessageType: TONGITS_GAME_RESULT_DETAILS_RES (3030)
 */
export interface GameResultDetailsRes {
  winnerId: number;
  playerResults: PlayerResult[];
}

/**
 * 获取玩家历史记录请求
 * MessageType: TONGITS_GET_PLAYER_HISTORY_REQ
 */
export interface GetPlayerHistoryReq {
  /** 页数，默认第一页 */
  page: number;
  /** 开始时间（Unix时间戳），默认当天开始 */
  startTime: number;
  /** 结束时间（Unix时间戳），默认当天结束 */
  endTime: number;
}

/**
 * 获取玩家历史记录响应
 * MessageType: TONGITS_GET_PLAYER_HISTORY_RES
 */
export interface GetPlayerHistoryRes {
  /** 历史记录列表 */
  records: TongitsHistoryRecord[];
}

/** 单条历史记录 */
export interface TongitsHistoryRecord {
  /** 玩家列表 */
  players: TongitsHistoryInfo[];
}

/** 每条数据内的tongits对局玩家信息 */
export interface TongitsHistoryInfo {
  /** 游戏局号 */
  gameCode: string;
  /** 结束时间 */
  endTime: number;
  userId: number;
  nickname: string;
  avatar: string;
  /** 实际扣费 */
  profit: number;
  /** 手牌点数 */
  score: number;
  /** 剩余手牌 */
  cards: number[];
  /** 1 普通赢  2 普通输 3 挑战认输 4 tongits胜利 5 挑战胜利 6 挑战失败 7 接受挑战胜利 8 接受挑战失败 9 烧死 */
  gameResult: number;
  /** 扣费 */
  cost: number;
  /** 普通奖励 */
  normalReward: number;
  /** pot池 */
  potReward: number;
  /** 总计 */
  totalReward: number;
}

function createBaseEmptyRes(): EmptyRes {
  return {};
}

export const EmptyRes = {
  encode(_: EmptyRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EmptyRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmptyRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): EmptyRes {
    return {};
  },

  toJSON(_: EmptyRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<EmptyRes>, I>>(base?: I): EmptyRes {
    return EmptyRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmptyRes>, I>>(_: I): EmptyRes {
    const message = createBaseEmptyRes();
    return message;
  },
};

function createBaseRoomInfo(): RoomInfo {
  return { roomId: 0, roomName: "", roomStatus: 0, maxSeat: 0 };
}

export const RoomInfo = {
  encode(message: RoomInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    if (message.roomName !== "") {
      writer.uint32(18).string(message.roomName);
    }
    if (message.roomStatus !== 0) {
      writer.uint32(24).int32(message.roomStatus);
    }
    if (message.maxSeat !== 0) {
      writer.uint32(32).int32(message.maxSeat);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoomInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomName = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.roomStatus = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.maxSeat = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomInfo {
    return {
      roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0,
      roomName: isSet(object.roomName) ? globalThis.String(object.roomName) : "",
      roomStatus: isSet(object.roomStatus) ? globalThis.Number(object.roomStatus) : 0,
      maxSeat: isSet(object.maxSeat) ? globalThis.Number(object.maxSeat) : 0,
    };
  },

  toJSON(message: RoomInfo): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    if (message.roomName !== "") {
      obj.roomName = message.roomName;
    }
    if (message.roomStatus !== 0) {
      obj.roomStatus = Math.round(message.roomStatus);
    }
    if (message.maxSeat !== 0) {
      obj.maxSeat = Math.round(message.maxSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomInfo>, I>>(base?: I): RoomInfo {
    return RoomInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomInfo>, I>>(object: I): RoomInfo {
    const message = createBaseRoomInfo();
    message.roomId = object.roomId ?? 0;
    message.roomName = object.roomName ?? "";
    message.roomStatus = object.roomStatus ?? 0;
    message.maxSeat = object.maxSeat ?? 0;
    return message;
  },
};

function createBaseTongitsPlayerInfo(): TongitsPlayerInfo {
  return {
    playerInfo: undefined,
    handCardCount: 0,
    isDealer: false,
    displayedMelds: [],
    handCards: [],
    isFight: false,
    countdown: 0,
    changeStatus: 0,
    status: 0,
    isWin: false,
    cardPoint: 0,
  };
}

export const TongitsPlayerInfo = {
  encode(message: TongitsPlayerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerInfo !== undefined) {
      PlayerInfo.encode(message.playerInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.handCardCount !== 0) {
      writer.uint32(16).int32(message.handCardCount);
    }
    if (message.isDealer !== false) {
      writer.uint32(24).bool(message.isDealer);
    }
    for (const v of message.displayedMelds) {
      Meld.encode(v!, writer.uint32(34).fork()).ldelim();
    }
    writer.uint32(42).fork();
    for (const v of message.handCards) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.isFight !== false) {
      writer.uint32(48).bool(message.isFight);
    }
    if (message.countdown !== 0) {
      writer.uint32(56).int32(message.countdown);
    }
    if (message.changeStatus !== 0) {
      writer.uint32(64).int32(message.changeStatus);
    }
    if (message.status !== 0) {
      writer.uint32(72).int32(message.status);
    }
    if (message.isWin !== false) {
      writer.uint32(80).bool(message.isWin);
    }
    if (message.cardPoint !== 0) {
      writer.uint32(88).int32(message.cardPoint);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsPlayerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsPlayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerInfo = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isDealer = reader.bool();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.displayedMelds.push(Meld.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag === 40) {
            message.handCards.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.handCards.push(reader.int32());
            }

            continue;
          }

          break;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.isFight = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.countdown = reader.int32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.changeStatus = reader.int32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.status = reader.int32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.isWin = reader.bool();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.cardPoint = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TongitsPlayerInfo {
    return {
      playerInfo: isSet(object.playerInfo) ? PlayerInfo.fromJSON(object.playerInfo) : undefined,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      isDealer: isSet(object.isDealer) ? globalThis.Boolean(object.isDealer) : false,
      displayedMelds: globalThis.Array.isArray(object?.displayedMelds)
        ? object.displayedMelds.map((e: any) => Meld.fromJSON(e))
        : [],
      handCards: globalThis.Array.isArray(object?.handCards)
        ? object.handCards.map((e: any) => globalThis.Number(e))
        : [],
      isFight: isSet(object.isFight) ? globalThis.Boolean(object.isFight) : false,
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
      changeStatus: isSet(object.changeStatus) ? globalThis.Number(object.changeStatus) : 0,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      isWin: isSet(object.isWin) ? globalThis.Boolean(object.isWin) : false,
      cardPoint: isSet(object.cardPoint) ? globalThis.Number(object.cardPoint) : 0,
    };
  },

  toJSON(message: TongitsPlayerInfo): unknown {
    const obj: any = {};
    if (message.playerInfo !== undefined) {
      obj.playerInfo = PlayerInfo.toJSON(message.playerInfo);
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.isDealer !== false) {
      obj.isDealer = message.isDealer;
    }
    if (message.displayedMelds?.length) {
      obj.displayedMelds = message.displayedMelds.map((e) => Meld.toJSON(e));
    }
    if (message.handCards?.length) {
      obj.handCards = message.handCards.map((e) => Math.round(e));
    }
    if (message.isFight !== false) {
      obj.isFight = message.isFight;
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    if (message.changeStatus !== 0) {
      obj.changeStatus = Math.round(message.changeStatus);
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.isWin !== false) {
      obj.isWin = message.isWin;
    }
    if (message.cardPoint !== 0) {
      obj.cardPoint = Math.round(message.cardPoint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsPlayerInfo>, I>>(base?: I): TongitsPlayerInfo {
    return TongitsPlayerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsPlayerInfo>, I>>(object: I): TongitsPlayerInfo {
    const message = createBaseTongitsPlayerInfo();
    message.playerInfo = (object.playerInfo !== undefined && object.playerInfo !== null)
      ? PlayerInfo.fromPartial(object.playerInfo)
      : undefined;
    message.handCardCount = object.handCardCount ?? 0;
    message.isDealer = object.isDealer ?? false;
    message.displayedMelds = object.displayedMelds?.map((e) => Meld.fromPartial(e)) || [];
    message.handCards = object.handCards?.map((e) => e) || [];
    message.isFight = object.isFight ?? false;
    message.countdown = object.countdown ?? 0;
    message.changeStatus = object.changeStatus ?? 0;
    message.status = object.status ?? 0;
    message.isWin = object.isWin ?? false;
    message.cardPoint = object.cardPoint ?? 0;
    return message;
  },
};

function createBaseTongitsBasePlayerInfo(): TongitsBasePlayerInfo {
  return { playerId: 0, changeStatus: 0, countdown: 0 };
}

export const TongitsBasePlayerInfo = {
  encode(message: TongitsBasePlayerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.changeStatus !== 0) {
      writer.uint32(16).int32(message.changeStatus);
    }
    if (message.countdown !== 0) {
      writer.uint32(24).int32(message.countdown);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsBasePlayerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsBasePlayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.changeStatus = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.countdown = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TongitsBasePlayerInfo {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      changeStatus: isSet(object.changeStatus) ? globalThis.Number(object.changeStatus) : 0,
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
    };
  },

  toJSON(message: TongitsBasePlayerInfo): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.changeStatus !== 0) {
      obj.changeStatus = Math.round(message.changeStatus);
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsBasePlayerInfo>, I>>(base?: I): TongitsBasePlayerInfo {
    return TongitsBasePlayerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsBasePlayerInfo>, I>>(object: I): TongitsBasePlayerInfo {
    const message = createBaseTongitsBasePlayerInfo();
    message.playerId = object.playerId ?? 0;
    message.changeStatus = object.changeStatus ?? 0;
    message.countdown = object.countdown ?? 0;
    return message;
  },
};

function createBaseMeld(): Meld {
  return { cards: [], highlightCards: 0, ownerId: 0, meldId: 0, locked: false };
}

export const Meld = {
  encode(message: Meld, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.cards) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.highlightCards !== 0) {
      writer.uint32(16).int32(message.highlightCards);
    }
    if (message.ownerId !== 0) {
      writer.uint32(24).int64(message.ownerId);
    }
    if (message.meldId !== 0) {
      writer.uint32(32).int32(message.meldId);
    }
    if (message.locked !== false) {
      writer.uint32(40).bool(message.locked);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Meld {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeld();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.cards.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.int32());
            }

            continue;
          }

          break;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.highlightCards = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.ownerId = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.meldId = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.locked = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Meld {
    return {
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
      highlightCards: isSet(object.highlightCards) ? globalThis.Number(object.highlightCards) : 0,
      ownerId: isSet(object.ownerId) ? globalThis.Number(object.ownerId) : 0,
      meldId: isSet(object.meldId) ? globalThis.Number(object.meldId) : 0,
      locked: isSet(object.locked) ? globalThis.Boolean(object.locked) : false,
    };
  },

  toJSON(message: Meld): unknown {
    const obj: any = {};
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    if (message.highlightCards !== 0) {
      obj.highlightCards = Math.round(message.highlightCards);
    }
    if (message.ownerId !== 0) {
      obj.ownerId = Math.round(message.ownerId);
    }
    if (message.meldId !== 0) {
      obj.meldId = Math.round(message.meldId);
    }
    if (message.locked !== false) {
      obj.locked = message.locked;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Meld>, I>>(base?: I): Meld {
    return Meld.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Meld>, I>>(object: I): Meld {
    const message = createBaseMeld();
    message.cards = object.cards?.map((e) => e) || [];
    message.highlightCards = object.highlightCards ?? 0;
    message.ownerId = object.ownerId ?? 0;
    message.meldId = object.meldId ?? 0;
    message.locked = object.locked ?? false;
    return message;
  },
};

function createBaseGameInfo(): GameInfo {
  return {
    actionPlayerId: 0,
    status: 0,
    deckCardCount: 0,
    discardPile: [],
    pot: undefined,
    discardCard: 0,
    winType: 0,
    perspectiveId: 0,
    betAmount: 0,
    scores: [],
  };
}

export const GameInfo = {
  encode(message: GameInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionPlayerId !== 0) {
      writer.uint32(8).int64(message.actionPlayerId);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.deckCardCount !== 0) {
      writer.uint32(32).int32(message.deckCardCount);
    }
    writer.uint32(50).fork();
    for (const v of message.discardPile) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.pot !== undefined) {
      PotInfo.encode(message.pot, writer.uint32(58).fork()).ldelim();
    }
    if (message.discardCard !== 0) {
      writer.uint32(64).int32(message.discardCard);
    }
    if (message.winType !== 0) {
      writer.uint32(72).int32(message.winType);
    }
    if (message.perspectiveId !== 0) {
      writer.uint32(80).int64(message.perspectiveId);
    }
    if (message.betAmount !== 0) {
      writer.uint32(88).int32(message.betAmount);
    }
    writer.uint32(98).fork();
    for (const v of message.scores) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.deckCardCount = reader.int32();
          continue;
        case 6:
          if (tag === 48) {
            message.discardPile.push(reader.int32());

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.discardPile.push(reader.int32());
            }

            continue;
          }

          break;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.pot = PotInfo.decode(reader, reader.uint32());
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.discardCard = reader.int32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.winType = reader.int32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.perspectiveId = longToNumber(reader.int64() as Long);
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.betAmount = reader.int32();
          continue;
        case 12:
          if (tag === 96) {
            message.scores.push(reader.int32());

            continue;
          }

          if (tag === 98) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.scores.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameInfo {
    return {
      actionPlayerId: isSet(object.actionPlayerId) ? globalThis.Number(object.actionPlayerId) : 0,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      deckCardCount: isSet(object.deckCardCount) ? globalThis.Number(object.deckCardCount) : 0,
      discardPile: globalThis.Array.isArray(object?.discardPile)
        ? object.discardPile.map((e: any) => globalThis.Number(e))
        : [],
      pot: isSet(object.pot) ? PotInfo.fromJSON(object.pot) : undefined,
      discardCard: isSet(object.discardCard) ? globalThis.Number(object.discardCard) : 0,
      winType: isSet(object.winType) ? globalThis.Number(object.winType) : 0,
      perspectiveId: isSet(object.perspectiveId) ? globalThis.Number(object.perspectiveId) : 0,
      betAmount: isSet(object.betAmount) ? globalThis.Number(object.betAmount) : 0,
      scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: GameInfo): unknown {
    const obj: any = {};
    if (message.actionPlayerId !== 0) {
      obj.actionPlayerId = Math.round(message.actionPlayerId);
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.deckCardCount !== 0) {
      obj.deckCardCount = Math.round(message.deckCardCount);
    }
    if (message.discardPile?.length) {
      obj.discardPile = message.discardPile.map((e) => Math.round(e));
    }
    if (message.pot !== undefined) {
      obj.pot = PotInfo.toJSON(message.pot);
    }
    if (message.discardCard !== 0) {
      obj.discardCard = Math.round(message.discardCard);
    }
    if (message.winType !== 0) {
      obj.winType = Math.round(message.winType);
    }
    if (message.perspectiveId !== 0) {
      obj.perspectiveId = Math.round(message.perspectiveId);
    }
    if (message.betAmount !== 0) {
      obj.betAmount = Math.round(message.betAmount);
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameInfo>, I>>(base?: I): GameInfo {
    return GameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameInfo>, I>>(object: I): GameInfo {
    const message = createBaseGameInfo();
    message.actionPlayerId = object.actionPlayerId ?? 0;
    message.status = object.status ?? 0;
    message.deckCardCount = object.deckCardCount ?? 0;
    message.discardPile = object.discardPile?.map((e) => e) || [];
    message.pot = (object.pot !== undefined && object.pot !== null) ? PotInfo.fromPartial(object.pot) : undefined;
    message.discardCard = object.discardCard ?? 0;
    message.winType = object.winType ?? 0;
    message.perspectiveId = object.perspectiveId ?? 0;
    message.betAmount = object.betAmount ?? 0;
    message.scores = object.scores?.map((e) => e) || [];
    return message;
  },
};

function createBasePotInfo(): PotInfo {
  return { base: 0, winCount: 0, useId: 0 };
}

export const PotInfo = {
  encode(message: PotInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.base !== 0) {
      writer.uint32(8).int32(message.base);
    }
    if (message.winCount !== 0) {
      writer.uint32(16).int32(message.winCount);
    }
    if (message.useId !== 0) {
      writer.uint32(24).int64(message.useId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PotInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePotInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.base = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.winCount = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.useId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PotInfo {
    return {
      base: isSet(object.base) ? globalThis.Number(object.base) : 0,
      winCount: isSet(object.winCount) ? globalThis.Number(object.winCount) : 0,
      useId: isSet(object.useId) ? globalThis.Number(object.useId) : 0,
    };
  },

  toJSON(message: PotInfo): unknown {
    const obj: any = {};
    if (message.base !== 0) {
      obj.base = Math.round(message.base);
    }
    if (message.winCount !== 0) {
      obj.winCount = Math.round(message.winCount);
    }
    if (message.useId !== 0) {
      obj.useId = Math.round(message.useId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PotInfo>, I>>(base?: I): PotInfo {
    return PotInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PotInfo>, I>>(object: I): PotInfo {
    const message = createBasePotInfo();
    message.base = object.base ?? 0;
    message.winCount = object.winCount ?? 0;
    message.useId = object.useId ?? 0;
    return message;
  },
};

function createBasePlayerResult(): PlayerResult {
  return {
    playerInfo: undefined,
    sumWinBonus: 0,
    normalWinBonus: 0,
    tongitsWinBonus: 0,
    cardTypeBonus: 0,
    bonusBonus: 0,
    burnedBonus: 0,
    winChallengeBonus: 0,
    potBonus: 0,
    cardPoint: 0,
  };
}

export const PlayerResult = {
  encode(message: PlayerResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerInfo !== undefined) {
      TongitsPlayerInfo.encode(message.playerInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.sumWinBonus !== 0) {
      writer.uint32(16).int32(message.sumWinBonus);
    }
    if (message.normalWinBonus !== 0) {
      writer.uint32(24).int32(message.normalWinBonus);
    }
    if (message.tongitsWinBonus !== 0) {
      writer.uint32(32).int32(message.tongitsWinBonus);
    }
    if (message.cardTypeBonus !== 0) {
      writer.uint32(40).int32(message.cardTypeBonus);
    }
    if (message.bonusBonus !== 0) {
      writer.uint32(48).int32(message.bonusBonus);
    }
    if (message.burnedBonus !== 0) {
      writer.uint32(56).int32(message.burnedBonus);
    }
    if (message.winChallengeBonus !== 0) {
      writer.uint32(64).int32(message.winChallengeBonus);
    }
    if (message.potBonus !== 0) {
      writer.uint32(72).int32(message.potBonus);
    }
    if (message.cardPoint !== 0) {
      writer.uint32(80).int32(message.cardPoint);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PlayerResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerInfo = TongitsPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.sumWinBonus = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.normalWinBonus = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.tongitsWinBonus = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.cardTypeBonus = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.bonusBonus = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.burnedBonus = reader.int32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.winChallengeBonus = reader.int32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.potBonus = reader.int32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.cardPoint = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerResult {
    return {
      playerInfo: isSet(object.playerInfo) ? TongitsPlayerInfo.fromJSON(object.playerInfo) : undefined,
      sumWinBonus: isSet(object.sumWinBonus) ? globalThis.Number(object.sumWinBonus) : 0,
      normalWinBonus: isSet(object.normalWinBonus) ? globalThis.Number(object.normalWinBonus) : 0,
      tongitsWinBonus: isSet(object.tongitsWinBonus) ? globalThis.Number(object.tongitsWinBonus) : 0,
      cardTypeBonus: isSet(object.cardTypeBonus) ? globalThis.Number(object.cardTypeBonus) : 0,
      bonusBonus: isSet(object.bonusBonus) ? globalThis.Number(object.bonusBonus) : 0,
      burnedBonus: isSet(object.burnedBonus) ? globalThis.Number(object.burnedBonus) : 0,
      winChallengeBonus: isSet(object.winChallengeBonus) ? globalThis.Number(object.winChallengeBonus) : 0,
      potBonus: isSet(object.potBonus) ? globalThis.Number(object.potBonus) : 0,
      cardPoint: isSet(object.cardPoint) ? globalThis.Number(object.cardPoint) : 0,
    };
  },

  toJSON(message: PlayerResult): unknown {
    const obj: any = {};
    if (message.playerInfo !== undefined) {
      obj.playerInfo = TongitsPlayerInfo.toJSON(message.playerInfo);
    }
    if (message.sumWinBonus !== 0) {
      obj.sumWinBonus = Math.round(message.sumWinBonus);
    }
    if (message.normalWinBonus !== 0) {
      obj.normalWinBonus = Math.round(message.normalWinBonus);
    }
    if (message.tongitsWinBonus !== 0) {
      obj.tongitsWinBonus = Math.round(message.tongitsWinBonus);
    }
    if (message.cardTypeBonus !== 0) {
      obj.cardTypeBonus = Math.round(message.cardTypeBonus);
    }
    if (message.bonusBonus !== 0) {
      obj.bonusBonus = Math.round(message.bonusBonus);
    }
    if (message.burnedBonus !== 0) {
      obj.burnedBonus = Math.round(message.burnedBonus);
    }
    if (message.winChallengeBonus !== 0) {
      obj.winChallengeBonus = Math.round(message.winChallengeBonus);
    }
    if (message.potBonus !== 0) {
      obj.potBonus = Math.round(message.potBonus);
    }
    if (message.cardPoint !== 0) {
      obj.cardPoint = Math.round(message.cardPoint);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerResult>, I>>(base?: I): PlayerResult {
    return PlayerResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerResult>, I>>(object: I): PlayerResult {
    const message = createBasePlayerResult();
    message.playerInfo = (object.playerInfo !== undefined && object.playerInfo !== null)
      ? TongitsPlayerInfo.fromPartial(object.playerInfo)
      : undefined;
    message.sumWinBonus = object.sumWinBonus ?? 0;
    message.normalWinBonus = object.normalWinBonus ?? 0;
    message.tongitsWinBonus = object.tongitsWinBonus ?? 0;
    message.cardTypeBonus = object.cardTypeBonus ?? 0;
    message.bonusBonus = object.bonusBonus ?? 0;
    message.burnedBonus = object.burnedBonus ?? 0;
    message.winChallengeBonus = object.winChallengeBonus ?? 0;
    message.potBonus = object.potBonus ?? 0;
    message.cardPoint = object.cardPoint ?? 0;
    return message;
  },
};

function createBaseJoinRoomReq(): JoinRoomReq {
  return { roomId: 0 };
}

export const JoinRoomReq = {
  encode(message: JoinRoomReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomReq {
    return { roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0 };
  },

  toJSON(message: JoinRoomReq): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomReq>, I>>(base?: I): JoinRoomReq {
    return JoinRoomReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomReq>, I>>(object: I): JoinRoomReq {
    const message = createBaseJoinRoomReq();
    message.roomId = object.roomId ?? 0;
    return message;
  },
};

function createBaseJoinRoomRes(): JoinRoomRes {
  return {
    roomInfo: undefined,
    players: [],
    watchers: [],
    playersCount: 0,
    speakers: [],
    self: undefined,
    gameInfo: undefined,
  };
}

export const JoinRoomRes = {
  encode(message: JoinRoomRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomInfo !== undefined) {
      RoomInfo.encode(message.roomInfo, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.players) {
      TongitsPlayerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.watchers) {
      PlayerInfo.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(32).int32(message.playersCount);
    }
    for (const v of message.speakers) {
      PlayerInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.self !== undefined) {
      TongitsPlayerInfo.encode(message.self, writer.uint32(50).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomInfo = RoomInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.players.push(TongitsPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.watchers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.speakers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.self = TongitsPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRes {
    return {
      roomInfo: isSet(object.roomInfo) ? RoomInfo.fromJSON(object.roomInfo) : undefined,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => TongitsPlayerInfo.fromJSON(e))
        : [],
      watchers: globalThis.Array.isArray(object?.watchers)
        ? object.watchers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
      speakers: globalThis.Array.isArray(object?.speakers)
        ? object.speakers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      self: isSet(object.self) ? TongitsPlayerInfo.fromJSON(object.self) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
    };
  },

  toJSON(message: JoinRoomRes): unknown {
    const obj: any = {};
    if (message.roomInfo !== undefined) {
      obj.roomInfo = RoomInfo.toJSON(message.roomInfo);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => TongitsPlayerInfo.toJSON(e));
    }
    if (message.watchers?.length) {
      obj.watchers = message.watchers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    if (message.speakers?.length) {
      obj.speakers = message.speakers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.self !== undefined) {
      obj.self = TongitsPlayerInfo.toJSON(message.self);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomRes>, I>>(base?: I): JoinRoomRes {
    return JoinRoomRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomRes>, I>>(object: I): JoinRoomRes {
    const message = createBaseJoinRoomRes();
    message.roomInfo = (object.roomInfo !== undefined && object.roomInfo !== null)
      ? RoomInfo.fromPartial(object.roomInfo)
      : undefined;
    message.players = object.players?.map((e) => TongitsPlayerInfo.fromPartial(e)) || [];
    message.watchers = object.watchers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.playersCount = object.playersCount ?? 0;
    message.speakers = object.speakers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.self = (object.self !== undefined && object.self !== null)
      ? TongitsPlayerInfo.fromPartial(object.self)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    return message;
  },
};

function createBaseStartGameReq(): StartGameReq {
  return {};
}

export const StartGameReq = {
  encode(_: StartGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameReq {
    return {};
  },

  toJSON(_: StartGameReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameReq>, I>>(base?: I): StartGameReq {
    return StartGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameReq>, I>>(_: I): StartGameReq {
    const message = createBaseStartGameReq();
    return message;
  },
};

function createBaseStartGameRes(): StartGameRes {
  return {};
}

export const StartGameRes = {
  encode(_: StartGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameRes {
    return {};
  },

  toJSON(_: StartGameRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameRes>, I>>(base?: I): StartGameRes {
    return StartGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameRes>, I>>(_: I): StartGameRes {
    const message = createBaseStartGameRes();
    return message;
  },
};

function createBaseGameStartBroadcast(): GameStartBroadcast {
  return { gameInfo: undefined, players: [], userId: 0 };
}

export const GameStartBroadcast = {
  encode(message: GameStartBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.players) {
      TongitsPlayerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameStartBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStartBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.players.push(TongitsPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStartBroadcast {
    return {
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => TongitsPlayerInfo.fromJSON(e))
        : [],
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: GameStartBroadcast): unknown {
    const obj: any = {};
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => TongitsPlayerInfo.toJSON(e));
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStartBroadcast>, I>>(base?: I): GameStartBroadcast {
    return GameStartBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStartBroadcast>, I>>(object: I): GameStartBroadcast {
    const message = createBaseGameStartBroadcast();
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.players = object.players?.map((e) => TongitsPlayerInfo.fromPartial(e)) || [];
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseTongitsReq(): TongitsReq {
  return {};
}

export const TongitsReq = {
  encode(_: TongitsReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TongitsReq {
    return {};
  },

  toJSON(_: TongitsReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsReq>, I>>(base?: I): TongitsReq {
    return TongitsReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsReq>, I>>(_: I): TongitsReq {
    const message = createBaseTongitsReq();
    return message;
  },
};

function createBaseTongitsRes(): TongitsRes {
  return {};
}

export const TongitsRes = {
  encode(_: TongitsRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TongitsRes {
    return {};
  },

  toJSON(_: TongitsRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsRes>, I>>(base?: I): TongitsRes {
    return TongitsRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsRes>, I>>(_: I): TongitsRes {
    const message = createBaseTongitsRes();
    return message;
  },
};

function createBaseDrawCardReq(): DrawCardReq {
  return {};
}

export const DrawCardReq = {
  encode(_: DrawCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DrawCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDrawCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DrawCardReq {
    return {};
  },

  toJSON(_: DrawCardReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DrawCardReq>, I>>(base?: I): DrawCardReq {
    return DrawCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DrawCardReq>, I>>(_: I): DrawCardReq {
    const message = createBaseDrawCardReq();
    return message;
  },
};

function createBaseDrawCardRes(): DrawCardRes {
  return { drawnCard: 0, hasTongits: false, handCardCount: 0 };
}

export const DrawCardRes = {
  encode(message: DrawCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.drawnCard !== 0) {
      writer.uint32(8).int32(message.drawnCard);
    }
    if (message.hasTongits !== false) {
      writer.uint32(16).bool(message.hasTongits);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DrawCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDrawCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.drawnCard = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.hasTongits = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DrawCardRes {
    return {
      drawnCard: isSet(object.drawnCard) ? globalThis.Number(object.drawnCard) : 0,
      hasTongits: isSet(object.hasTongits) ? globalThis.Boolean(object.hasTongits) : false,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
    };
  },

  toJSON(message: DrawCardRes): unknown {
    const obj: any = {};
    if (message.drawnCard !== 0) {
      obj.drawnCard = Math.round(message.drawnCard);
    }
    if (message.hasTongits !== false) {
      obj.hasTongits = message.hasTongits;
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DrawCardRes>, I>>(base?: I): DrawCardRes {
    return DrawCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DrawCardRes>, I>>(object: I): DrawCardRes {
    const message = createBaseDrawCardRes();
    message.drawnCard = object.drawnCard ?? 0;
    message.hasTongits = object.hasTongits ?? false;
    message.handCardCount = object.handCardCount ?? 0;
    return message;
  },
};

function createBaseDrawCardBroadcast(): DrawCardBroadcast {
  return { playerId: 0, userId: 0, drawnCard: 0, handCardCount: 0 };
}

export const DrawCardBroadcast = {
  encode(message: DrawCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.userId !== 0) {
      writer.uint32(16).int64(message.userId);
    }
    if (message.drawnCard !== 0) {
      writer.uint32(24).int32(message.drawnCard);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(32).int32(message.handCardCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DrawCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDrawCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.drawnCard = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DrawCardBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      drawnCard: isSet(object.drawnCard) ? globalThis.Number(object.drawnCard) : 0,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
    };
  },

  toJSON(message: DrawCardBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.drawnCard !== 0) {
      obj.drawnCard = Math.round(message.drawnCard);
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DrawCardBroadcast>, I>>(base?: I): DrawCardBroadcast {
    return DrawCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DrawCardBroadcast>, I>>(object: I): DrawCardBroadcast {
    const message = createBaseDrawCardBroadcast();
    message.playerId = object.playerId ?? 0;
    message.userId = object.userId ?? 0;
    message.drawnCard = object.drawnCard ?? 0;
    message.handCardCount = object.handCardCount ?? 0;
    return message;
  },
};

function createBaseMeldCardReq(): MeldCardReq {
  return { cards: [] };
}

export const MeldCardReq = {
  encode(message: MeldCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.cards) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MeldCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeldCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.cards.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeldCardReq {
    return { cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: MeldCardReq): unknown {
    const obj: any = {};
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeldCardReq>, I>>(base?: I): MeldCardReq {
    return MeldCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeldCardReq>, I>>(object: I): MeldCardReq {
    const message = createBaseMeldCardReq();
    message.cards = object.cards?.map((e) => e) || [];
    return message;
  },
};

function createBaseMeldCardRes(): MeldCardRes {
  return { newMeld: undefined, hasTongits: false, handCardCount: 0 };
}

export const MeldCardRes = {
  encode(message: MeldCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.newMeld !== undefined) {
      Meld.encode(message.newMeld, writer.uint32(10).fork()).ldelim();
    }
    if (message.hasTongits !== false) {
      writer.uint32(16).bool(message.hasTongits);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MeldCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeldCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.newMeld = Meld.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.hasTongits = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeldCardRes {
    return {
      newMeld: isSet(object.newMeld) ? Meld.fromJSON(object.newMeld) : undefined,
      hasTongits: isSet(object.hasTongits) ? globalThis.Boolean(object.hasTongits) : false,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
    };
  },

  toJSON(message: MeldCardRes): unknown {
    const obj: any = {};
    if (message.newMeld !== undefined) {
      obj.newMeld = Meld.toJSON(message.newMeld);
    }
    if (message.hasTongits !== false) {
      obj.hasTongits = message.hasTongits;
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeldCardRes>, I>>(base?: I): MeldCardRes {
    return MeldCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeldCardRes>, I>>(object: I): MeldCardRes {
    const message = createBaseMeldCardRes();
    message.newMeld = (object.newMeld !== undefined && object.newMeld !== null)
      ? Meld.fromPartial(object.newMeld)
      : undefined;
    message.hasTongits = object.hasTongits ?? false;
    message.handCardCount = object.handCardCount ?? 0;
    return message;
  },
};

function createBaseMeldCardBroadcast(): MeldCardBroadcast {
  return { playerId: 0, newMeld: undefined, handCardCount: 0, userId: 0 };
}

export const MeldCardBroadcast = {
  encode(message: MeldCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.newMeld !== undefined) {
      Meld.encode(message.newMeld, writer.uint32(18).fork()).ldelim();
    }
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): MeldCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeldCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.newMeld = Meld.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeldCardBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      newMeld: isSet(object.newMeld) ? Meld.fromJSON(object.newMeld) : undefined,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: MeldCardBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.newMeld !== undefined) {
      obj.newMeld = Meld.toJSON(message.newMeld);
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeldCardBroadcast>, I>>(base?: I): MeldCardBroadcast {
    return MeldCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeldCardBroadcast>, I>>(object: I): MeldCardBroadcast {
    const message = createBaseMeldCardBroadcast();
    message.playerId = object.playerId ?? 0;
    message.newMeld = (object.newMeld !== undefined && object.newMeld !== null)
      ? Meld.fromPartial(object.newMeld)
      : undefined;
    message.handCardCount = object.handCardCount ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseLayOffCardReq(): LayOffCardReq {
  return { card: 0, targetPlayerId: 0, targetMeldId: 0 };
}

export const LayOffCardReq = {
  encode(message: LayOffCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.card !== 0) {
      writer.uint32(8).int32(message.card);
    }
    if (message.targetPlayerId !== 0) {
      writer.uint32(16).int64(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      writer.uint32(24).int32(message.targetMeldId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LayOffCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayOffCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.card = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.targetPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.targetMeldId = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LayOffCardReq {
    return {
      card: isSet(object.card) ? globalThis.Number(object.card) : 0,
      targetPlayerId: isSet(object.targetPlayerId) ? globalThis.Number(object.targetPlayerId) : 0,
      targetMeldId: isSet(object.targetMeldId) ? globalThis.Number(object.targetMeldId) : 0,
    };
  },

  toJSON(message: LayOffCardReq): unknown {
    const obj: any = {};
    if (message.card !== 0) {
      obj.card = Math.round(message.card);
    }
    if (message.targetPlayerId !== 0) {
      obj.targetPlayerId = Math.round(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      obj.targetMeldId = Math.round(message.targetMeldId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LayOffCardReq>, I>>(base?: I): LayOffCardReq {
    return LayOffCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LayOffCardReq>, I>>(object: I): LayOffCardReq {
    const message = createBaseLayOffCardReq();
    message.card = object.card ?? 0;
    message.targetPlayerId = object.targetPlayerId ?? 0;
    message.targetMeldId = object.targetMeldId ?? 0;
    return message;
  },
};

function createBaseLayOffCardRes(): LayOffCardRes {
  return { cardAdded: 0, targetPlayerId: 0, targetMeldId: 0, hasTongits: false, handCardCount: 0 };
}

export const LayOffCardRes = {
  encode(message: LayOffCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cardAdded !== 0) {
      writer.uint32(8).int32(message.cardAdded);
    }
    if (message.targetPlayerId !== 0) {
      writer.uint32(16).int64(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      writer.uint32(24).int32(message.targetMeldId);
    }
    if (message.hasTongits !== false) {
      writer.uint32(32).bool(message.hasTongits);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(40).int32(message.handCardCount);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LayOffCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayOffCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.cardAdded = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.targetPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.targetMeldId = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.hasTongits = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LayOffCardRes {
    return {
      cardAdded: isSet(object.cardAdded) ? globalThis.Number(object.cardAdded) : 0,
      targetPlayerId: isSet(object.targetPlayerId) ? globalThis.Number(object.targetPlayerId) : 0,
      targetMeldId: isSet(object.targetMeldId) ? globalThis.Number(object.targetMeldId) : 0,
      hasTongits: isSet(object.hasTongits) ? globalThis.Boolean(object.hasTongits) : false,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
    };
  },

  toJSON(message: LayOffCardRes): unknown {
    const obj: any = {};
    if (message.cardAdded !== 0) {
      obj.cardAdded = Math.round(message.cardAdded);
    }
    if (message.targetPlayerId !== 0) {
      obj.targetPlayerId = Math.round(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      obj.targetMeldId = Math.round(message.targetMeldId);
    }
    if (message.hasTongits !== false) {
      obj.hasTongits = message.hasTongits;
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LayOffCardRes>, I>>(base?: I): LayOffCardRes {
    return LayOffCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LayOffCardRes>, I>>(object: I): LayOffCardRes {
    const message = createBaseLayOffCardRes();
    message.cardAdded = object.cardAdded ?? 0;
    message.targetPlayerId = object.targetPlayerId ?? 0;
    message.targetMeldId = object.targetMeldId ?? 0;
    message.hasTongits = object.hasTongits ?? false;
    message.handCardCount = object.handCardCount ?? 0;
    return message;
  },
};

function createBaseLayOffCardBroadcast(): LayOffCardBroadcast {
  return { actionPlayerId: 0, cardAdded: 0, targetPlayerId: 0, targetMeldId: 0, handCardCount: 0, userId: 0 };
}

export const LayOffCardBroadcast = {
  encode(message: LayOffCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionPlayerId !== 0) {
      writer.uint32(8).int64(message.actionPlayerId);
    }
    if (message.cardAdded !== 0) {
      writer.uint32(16).int32(message.cardAdded);
    }
    if (message.targetPlayerId !== 0) {
      writer.uint32(24).int64(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      writer.uint32(32).int32(message.targetMeldId);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(40).int32(message.handCardCount);
    }
    if (message.userId !== 0) {
      writer.uint32(48).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LayOffCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayOffCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.cardAdded = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.targetPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.targetMeldId = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LayOffCardBroadcast {
    return {
      actionPlayerId: isSet(object.actionPlayerId) ? globalThis.Number(object.actionPlayerId) : 0,
      cardAdded: isSet(object.cardAdded) ? globalThis.Number(object.cardAdded) : 0,
      targetPlayerId: isSet(object.targetPlayerId) ? globalThis.Number(object.targetPlayerId) : 0,
      targetMeldId: isSet(object.targetMeldId) ? globalThis.Number(object.targetMeldId) : 0,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: LayOffCardBroadcast): unknown {
    const obj: any = {};
    if (message.actionPlayerId !== 0) {
      obj.actionPlayerId = Math.round(message.actionPlayerId);
    }
    if (message.cardAdded !== 0) {
      obj.cardAdded = Math.round(message.cardAdded);
    }
    if (message.targetPlayerId !== 0) {
      obj.targetPlayerId = Math.round(message.targetPlayerId);
    }
    if (message.targetMeldId !== 0) {
      obj.targetMeldId = Math.round(message.targetMeldId);
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LayOffCardBroadcast>, I>>(base?: I): LayOffCardBroadcast {
    return LayOffCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LayOffCardBroadcast>, I>>(object: I): LayOffCardBroadcast {
    const message = createBaseLayOffCardBroadcast();
    message.actionPlayerId = object.actionPlayerId ?? 0;
    message.cardAdded = object.cardAdded ?? 0;
    message.targetPlayerId = object.targetPlayerId ?? 0;
    message.targetMeldId = object.targetMeldId ?? 0;
    message.handCardCount = object.handCardCount ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseDiscardCardReq(): DiscardCardReq {
  return { card: 0 };
}

export const DiscardCardReq = {
  encode(message: DiscardCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.card !== 0) {
      writer.uint32(8).int32(message.card);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DiscardCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscardCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.card = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscardCardReq {
    return { card: isSet(object.card) ? globalThis.Number(object.card) : 0 };
  },

  toJSON(message: DiscardCardReq): unknown {
    const obj: any = {};
    if (message.card !== 0) {
      obj.card = Math.round(message.card);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscardCardReq>, I>>(base?: I): DiscardCardReq {
    return DiscardCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscardCardReq>, I>>(object: I): DiscardCardReq {
    const message = createBaseDiscardCardReq();
    message.card = object.card ?? 0;
    return message;
  },
};

function createBaseDiscardCardRes(): DiscardCardRes {
  return { discardedCard: 0, unlockMelds: [], handCardCount: 0, discardPile: [] };
}

export const DiscardCardRes = {
  encode(message: DiscardCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.discardedCard !== 0) {
      writer.uint32(8).int32(message.discardedCard);
    }
    writer.uint32(18).fork();
    for (const v of message.unlockMelds) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    writer.uint32(42).fork();
    for (const v of message.discardPile) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DiscardCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscardCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.discardedCard = reader.int32();
          continue;
        case 2:
          if (tag === 16) {
            message.unlockMelds.push(reader.int32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.unlockMelds.push(reader.int32());
            }

            continue;
          }

          break;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 5:
          if (tag === 40) {
            message.discardPile.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.discardPile.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscardCardRes {
    return {
      discardedCard: isSet(object.discardedCard) ? globalThis.Number(object.discardedCard) : 0,
      unlockMelds: globalThis.Array.isArray(object?.unlockMelds)
        ? object.unlockMelds.map((e: any) => globalThis.Number(e))
        : [],
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      discardPile: globalThis.Array.isArray(object?.discardPile)
        ? object.discardPile.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: DiscardCardRes): unknown {
    const obj: any = {};
    if (message.discardedCard !== 0) {
      obj.discardedCard = Math.round(message.discardedCard);
    }
    if (message.unlockMelds?.length) {
      obj.unlockMelds = message.unlockMelds.map((e) => Math.round(e));
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.discardPile?.length) {
      obj.discardPile = message.discardPile.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscardCardRes>, I>>(base?: I): DiscardCardRes {
    return DiscardCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscardCardRes>, I>>(object: I): DiscardCardRes {
    const message = createBaseDiscardCardRes();
    message.discardedCard = object.discardedCard ?? 0;
    message.unlockMelds = object.unlockMelds?.map((e) => e) || [];
    message.handCardCount = object.handCardCount ?? 0;
    message.discardPile = object.discardPile?.map((e) => e) || [];
    return message;
  },
};

function createBaseDiscardCardBroadcast(): DiscardCardBroadcast {
  return { playerId: 0, discardedCard: 0, unlockMelds: [], handCardCount: 0, discardPile: [], userId: 0 };
}

export const DiscardCardBroadcast = {
  encode(message: DiscardCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.discardedCard !== 0) {
      writer.uint32(16).int32(message.discardedCard);
    }
    writer.uint32(26).fork();
    for (const v of message.unlockMelds) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.handCardCount !== 0) {
      writer.uint32(32).int32(message.handCardCount);
    }
    writer.uint32(42).fork();
    for (const v of message.discardPile) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.userId !== 0) {
      writer.uint32(88).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DiscardCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscardCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.discardedCard = reader.int32();
          continue;
        case 3:
          if (tag === 24) {
            message.unlockMelds.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.unlockMelds.push(reader.int32());
            }

            continue;
          }

          break;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 5:
          if (tag === 40) {
            message.discardPile.push(reader.int32());

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.discardPile.push(reader.int32());
            }

            continue;
          }

          break;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscardCardBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      discardedCard: isSet(object.discardedCard) ? globalThis.Number(object.discardedCard) : 0,
      unlockMelds: globalThis.Array.isArray(object?.unlockMelds)
        ? object.unlockMelds.map((e: any) => globalThis.Number(e))
        : [],
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      discardPile: globalThis.Array.isArray(object?.discardPile)
        ? object.discardPile.map((e: any) => globalThis.Number(e))
        : [],
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: DiscardCardBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.discardedCard !== 0) {
      obj.discardedCard = Math.round(message.discardedCard);
    }
    if (message.unlockMelds?.length) {
      obj.unlockMelds = message.unlockMelds.map((e) => Math.round(e));
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.discardPile?.length) {
      obj.discardPile = message.discardPile.map((e) => Math.round(e));
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscardCardBroadcast>, I>>(base?: I): DiscardCardBroadcast {
    return DiscardCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscardCardBroadcast>, I>>(object: I): DiscardCardBroadcast {
    const message = createBaseDiscardCardBroadcast();
    message.playerId = object.playerId ?? 0;
    message.discardedCard = object.discardedCard ?? 0;
    message.unlockMelds = object.unlockMelds?.map((e) => e) || [];
    message.handCardCount = object.handCardCount ?? 0;
    message.discardPile = object.discardPile?.map((e) => e) || [];
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseTakeCardReq(): TakeCardReq {
  return { cardsFromHand: [] };
}

export const TakeCardReq = {
  encode(message: TakeCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    writer.uint32(10).fork();
    for (const v of message.cardsFromHand) {
      writer.int32(v);
    }
    writer.ldelim();
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TakeCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTakeCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.cardsFromHand.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cardsFromHand.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TakeCardReq {
    return {
      cardsFromHand: globalThis.Array.isArray(object?.cardsFromHand)
        ? object.cardsFromHand.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: TakeCardReq): unknown {
    const obj: any = {};
    if (message.cardsFromHand?.length) {
      obj.cardsFromHand = message.cardsFromHand.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TakeCardReq>, I>>(base?: I): TakeCardReq {
    return TakeCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TakeCardReq>, I>>(object: I): TakeCardReq {
    const message = createBaseTakeCardReq();
    message.cardsFromHand = object.cardsFromHand?.map((e) => e) || [];
    return message;
  },
};

function createBaseTakeCardRes(): TakeCardRes {
  return { newMeld: undefined, hasTongits: false, handCardCount: 0, discard: 0 };
}

export const TakeCardRes = {
  encode(message: TakeCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.newMeld !== undefined) {
      Meld.encode(message.newMeld, writer.uint32(10).fork()).ldelim();
    }
    if (message.hasTongits !== false) {
      writer.uint32(16).bool(message.hasTongits);
    }
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    if (message.discard !== 0) {
      writer.uint32(32).int32(message.discard);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TakeCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTakeCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.newMeld = Meld.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.hasTongits = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.discard = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TakeCardRes {
    return {
      newMeld: isSet(object.newMeld) ? Meld.fromJSON(object.newMeld) : undefined,
      hasTongits: isSet(object.hasTongits) ? globalThis.Boolean(object.hasTongits) : false,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      discard: isSet(object.discard) ? globalThis.Number(object.discard) : 0,
    };
  },

  toJSON(message: TakeCardRes): unknown {
    const obj: any = {};
    if (message.newMeld !== undefined) {
      obj.newMeld = Meld.toJSON(message.newMeld);
    }
    if (message.hasTongits !== false) {
      obj.hasTongits = message.hasTongits;
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.discard !== 0) {
      obj.discard = Math.round(message.discard);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TakeCardRes>, I>>(base?: I): TakeCardRes {
    return TakeCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TakeCardRes>, I>>(object: I): TakeCardRes {
    const message = createBaseTakeCardRes();
    message.newMeld = (object.newMeld !== undefined && object.newMeld !== null)
      ? Meld.fromPartial(object.newMeld)
      : undefined;
    message.hasTongits = object.hasTongits ?? false;
    message.handCardCount = object.handCardCount ?? 0;
    message.discard = object.discard ?? 0;
    return message;
  },
};

function createBaseTakeCardBroadcast(): TakeCardBroadcast {
  return { playerId: 0, newMeld: undefined, handCardCount: 0, discard: 0, userId: 0 };
}

export const TakeCardBroadcast = {
  encode(message: TakeCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.newMeld !== undefined) {
      Meld.encode(message.newMeld, writer.uint32(18).fork()).ldelim();
    }
    if (message.handCardCount !== 0) {
      writer.uint32(24).int32(message.handCardCount);
    }
    if (message.discard !== 0) {
      writer.uint32(32).int32(message.discard);
    }
    if (message.userId !== 0) {
      writer.uint32(40).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TakeCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTakeCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.newMeld = Meld.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.handCardCount = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.discard = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TakeCardBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      newMeld: isSet(object.newMeld) ? Meld.fromJSON(object.newMeld) : undefined,
      handCardCount: isSet(object.handCardCount) ? globalThis.Number(object.handCardCount) : 0,
      discard: isSet(object.discard) ? globalThis.Number(object.discard) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: TakeCardBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.newMeld !== undefined) {
      obj.newMeld = Meld.toJSON(message.newMeld);
    }
    if (message.handCardCount !== 0) {
      obj.handCardCount = Math.round(message.handCardCount);
    }
    if (message.discard !== 0) {
      obj.discard = Math.round(message.discard);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TakeCardBroadcast>, I>>(base?: I): TakeCardBroadcast {
    return TakeCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TakeCardBroadcast>, I>>(object: I): TakeCardBroadcast {
    const message = createBaseTakeCardBroadcast();
    message.playerId = object.playerId ?? 0;
    message.newMeld = (object.newMeld !== undefined && object.newMeld !== null)
      ? Meld.fromPartial(object.newMeld)
      : undefined;
    message.handCardCount = object.handCardCount ?? 0;
    message.discard = object.discard ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseChallengeReq(): ChallengeReq {
  return { changeStatus: 0 };
}

export const ChallengeReq = {
  encode(message: ChallengeReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.changeStatus !== 0) {
      writer.uint32(8).int32(message.changeStatus);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ChallengeReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallengeReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.changeStatus = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChallengeReq {
    return { changeStatus: isSet(object.changeStatus) ? globalThis.Number(object.changeStatus) : 0 };
  },

  toJSON(message: ChallengeReq): unknown {
    const obj: any = {};
    if (message.changeStatus !== 0) {
      obj.changeStatus = Math.round(message.changeStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChallengeReq>, I>>(base?: I): ChallengeReq {
    return ChallengeReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChallengeReq>, I>>(object: I): ChallengeReq {
    const message = createBaseChallengeReq();
    message.changeStatus = object.changeStatus ?? 0;
    return message;
  },
};

function createBaseChallengeRes(): ChallengeRes {
  return { basePlayers: [] };
}

export const ChallengeRes = {
  encode(message: ChallengeRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.basePlayers) {
      TongitsBasePlayerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ChallengeRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallengeRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.basePlayers.push(TongitsBasePlayerInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChallengeRes {
    return {
      basePlayers: globalThis.Array.isArray(object?.basePlayers)
        ? object.basePlayers.map((e: any) => TongitsBasePlayerInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ChallengeRes): unknown {
    const obj: any = {};
    if (message.basePlayers?.length) {
      obj.basePlayers = message.basePlayers.map((e) => TongitsBasePlayerInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChallengeRes>, I>>(base?: I): ChallengeRes {
    return ChallengeRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChallengeRes>, I>>(object: I): ChallengeRes {
    const message = createBaseChallengeRes();
    message.basePlayers = object.basePlayers?.map((e) => TongitsBasePlayerInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseChallengeBroadcast(): ChallengeBroadcast {
  return { playerId: 0, basePlayers: [], userId: 0 };
}

export const ChallengeBroadcast = {
  encode(message: ChallengeBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    for (const v of message.basePlayers) {
      TongitsBasePlayerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ChallengeBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallengeBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.basePlayers.push(TongitsBasePlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChallengeBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      basePlayers: globalThis.Array.isArray(object?.basePlayers)
        ? object.basePlayers.map((e: any) => TongitsBasePlayerInfo.fromJSON(e))
        : [],
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: ChallengeBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.basePlayers?.length) {
      obj.basePlayers = message.basePlayers.map((e) => TongitsBasePlayerInfo.toJSON(e));
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChallengeBroadcast>, I>>(base?: I): ChallengeBroadcast {
    return ChallengeBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChallengeBroadcast>, I>>(object: I): ChallengeBroadcast {
    const message = createBaseChallengeBroadcast();
    message.playerId = object.playerId ?? 0;
    message.basePlayers = object.basePlayers?.map((e) => TongitsBasePlayerInfo.fromPartial(e)) || [];
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBasePKBroadcast(): PKBroadcast {
  return { playerId: 0, changeStatus: 0, userId: 0 };
}

export const PKBroadcast = {
  encode(message: PKBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerId !== 0) {
      writer.uint32(8).int64(message.playerId);
    }
    if (message.changeStatus !== 0) {
      writer.uint32(16).int32(message.changeStatus);
    }
    if (message.userId !== 0) {
      writer.uint32(24).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PKBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePKBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.playerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.changeStatus = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PKBroadcast {
    return {
      playerId: isSet(object.playerId) ? globalThis.Number(object.playerId) : 0,
      changeStatus: isSet(object.changeStatus) ? globalThis.Number(object.changeStatus) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: PKBroadcast): unknown {
    const obj: any = {};
    if (message.playerId !== 0) {
      obj.playerId = Math.round(message.playerId);
    }
    if (message.changeStatus !== 0) {
      obj.changeStatus = Math.round(message.changeStatus);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PKBroadcast>, I>>(base?: I): PKBroadcast {
    return PKBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PKBroadcast>, I>>(object: I): PKBroadcast {
    const message = createBasePKBroadcast();
    message.playerId = object.playerId ?? 0;
    message.changeStatus = object.changeStatus ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseGameResultBroadcast(): GameResultBroadcast {
  return { winnerId: 0, playerResults: [], countdown: 0, userId: 0 };
}

export const GameResultBroadcast = {
  encode(message: GameResultBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.winnerId !== 0) {
      writer.uint32(8).int64(message.winnerId);
    }
    for (const v of message.playerResults) {
      PlayerResult.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.countdown !== 0) {
      writer.uint32(24).int32(message.countdown);
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameResultBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameResultBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.winnerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerResults.push(PlayerResult.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.countdown = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameResultBroadcast {
    return {
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      playerResults: globalThis.Array.isArray(object?.playerResults)
        ? object.playerResults.map((e: any) => PlayerResult.fromJSON(e))
        : [],
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: GameResultBroadcast): unknown {
    const obj: any = {};
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.playerResults?.length) {
      obj.playerResults = message.playerResults.map((e) => PlayerResult.toJSON(e));
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameResultBroadcast>, I>>(base?: I): GameResultBroadcast {
    return GameResultBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameResultBroadcast>, I>>(object: I): GameResultBroadcast {
    const message = createBaseGameResultBroadcast();
    message.winnerId = object.winnerId ?? 0;
    message.playerResults = object.playerResults?.map((e) => PlayerResult.fromPartial(e)) || [];
    message.countdown = object.countdown ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseRoomResetBroadcast(): RoomResetBroadcast {
  return { players: [], self: undefined, gameInfo: undefined, userId: 0 };
}

export const RoomResetBroadcast = {
  encode(message: RoomResetBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.players) {
      TongitsPlayerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.self !== undefined) {
      TongitsPlayerInfo.encode(message.self, writer.uint32(18).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(26).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoomResetBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomResetBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.players.push(TongitsPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.self = TongitsPlayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomResetBroadcast {
    return {
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => TongitsPlayerInfo.fromJSON(e))
        : [],
      self: isSet(object.self) ? TongitsPlayerInfo.fromJSON(object.self) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: RoomResetBroadcast): unknown {
    const obj: any = {};
    if (message.players?.length) {
      obj.players = message.players.map((e) => TongitsPlayerInfo.toJSON(e));
    }
    if (message.self !== undefined) {
      obj.self = TongitsPlayerInfo.toJSON(message.self);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomResetBroadcast>, I>>(base?: I): RoomResetBroadcast {
    return RoomResetBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomResetBroadcast>, I>>(object: I): RoomResetBroadcast {
    const message = createBaseRoomResetBroadcast();
    message.players = object.players?.map((e) => TongitsPlayerInfo.fromPartial(e)) || [];
    message.self = (object.self !== undefined && object.self !== null)
      ? TongitsPlayerInfo.fromPartial(object.self)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseActionChangeBroadcast(): ActionChangeBroadcast {
  return { actionPlayerId: 0, countdown: 0, isFight: false, status: 0, userId: 0 };
}

export const ActionChangeBroadcast = {
  encode(message: ActionChangeBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionPlayerId !== 0) {
      writer.uint32(8).int64(message.actionPlayerId);
    }
    if (message.countdown !== 0) {
      writer.uint32(16).int32(message.countdown);
    }
    if (message.isFight !== false) {
      writer.uint32(24).bool(message.isFight);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.userId !== 0) {
      writer.uint32(88).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ActionChangeBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionChangeBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionPlayerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.countdown = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isFight = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionChangeBroadcast {
    return {
      actionPlayerId: isSet(object.actionPlayerId) ? globalThis.Number(object.actionPlayerId) : 0,
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
      isFight: isSet(object.isFight) ? globalThis.Boolean(object.isFight) : false,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: ActionChangeBroadcast): unknown {
    const obj: any = {};
    if (message.actionPlayerId !== 0) {
      obj.actionPlayerId = Math.round(message.actionPlayerId);
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    if (message.isFight !== false) {
      obj.isFight = message.isFight;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionChangeBroadcast>, I>>(base?: I): ActionChangeBroadcast {
    return ActionChangeBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionChangeBroadcast>, I>>(object: I): ActionChangeBroadcast {
    const message = createBaseActionChangeBroadcast();
    message.actionPlayerId = object.actionPlayerId ?? 0;
    message.countdown = object.countdown ?? 0;
    message.isFight = object.isFight ?? false;
    message.status = object.status ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseBeforeResultBroadcast(): BeforeResultBroadcast {
  return { winnerId: 0, winType: 0, players: [], countdown: 0, pot: undefined, userId: 0 };
}

export const BeforeResultBroadcast = {
  encode(message: BeforeResultBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.winnerId !== 0) {
      writer.uint32(8).int64(message.winnerId);
    }
    if (message.winType !== 0) {
      writer.uint32(16).int32(message.winType);
    }
    for (const v of message.players) {
      TongitsPlayerInfo.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.countdown !== 0) {
      writer.uint32(32).int32(message.countdown);
    }
    if (message.pot !== undefined) {
      PotInfo.encode(message.pot, writer.uint32(42).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(88).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): BeforeResultBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeforeResultBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.winnerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.winType = reader.int32();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.players.push(TongitsPlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.countdown = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.pot = PotInfo.decode(reader, reader.uint32());
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BeforeResultBroadcast {
    return {
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      winType: isSet(object.winType) ? globalThis.Number(object.winType) : 0,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => TongitsPlayerInfo.fromJSON(e))
        : [],
      countdown: isSet(object.countdown) ? globalThis.Number(object.countdown) : 0,
      pot: isSet(object.pot) ? PotInfo.fromJSON(object.pot) : undefined,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: BeforeResultBroadcast): unknown {
    const obj: any = {};
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.winType !== 0) {
      obj.winType = Math.round(message.winType);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => TongitsPlayerInfo.toJSON(e));
    }
    if (message.countdown !== 0) {
      obj.countdown = Math.round(message.countdown);
    }
    if (message.pot !== undefined) {
      obj.pot = PotInfo.toJSON(message.pot);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeforeResultBroadcast>, I>>(base?: I): BeforeResultBroadcast {
    return BeforeResultBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeforeResultBroadcast>, I>>(object: I): BeforeResultBroadcast {
    const message = createBaseBeforeResultBroadcast();
    message.winnerId = object.winnerId ?? 0;
    message.winType = object.winType ?? 0;
    message.players = object.players?.map((e) => TongitsPlayerInfo.fromPartial(e)) || [];
    message.countdown = object.countdown ?? 0;
    message.pot = (object.pot !== undefined && object.pot !== null) ? PotInfo.fromPartial(object.pot) : undefined;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseGameResultDetailsReq(): GameResultDetailsReq {
  return {};
}

export const GameResultDetailsReq = {
  encode(_: GameResultDetailsReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameResultDetailsReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameResultDetailsReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GameResultDetailsReq {
    return {};
  },

  toJSON(_: GameResultDetailsReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GameResultDetailsReq>, I>>(base?: I): GameResultDetailsReq {
    return GameResultDetailsReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameResultDetailsReq>, I>>(_: I): GameResultDetailsReq {
    const message = createBaseGameResultDetailsReq();
    return message;
  },
};

function createBaseGameResultDetailsRes(): GameResultDetailsRes {
  return { winnerId: 0, playerResults: [] };
}

export const GameResultDetailsRes = {
  encode(message: GameResultDetailsRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.winnerId !== 0) {
      writer.uint32(8).int64(message.winnerId);
    }
    for (const v of message.playerResults) {
      PlayerResult.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameResultDetailsRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameResultDetailsRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.winnerId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.playerResults.push(PlayerResult.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameResultDetailsRes {
    return {
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      playerResults: globalThis.Array.isArray(object?.playerResults)
        ? object.playerResults.map((e: any) => PlayerResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GameResultDetailsRes): unknown {
    const obj: any = {};
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.playerResults?.length) {
      obj.playerResults = message.playerResults.map((e) => PlayerResult.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameResultDetailsRes>, I>>(base?: I): GameResultDetailsRes {
    return GameResultDetailsRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameResultDetailsRes>, I>>(object: I): GameResultDetailsRes {
    const message = createBaseGameResultDetailsRes();
    message.winnerId = object.winnerId ?? 0;
    message.playerResults = object.playerResults?.map((e) => PlayerResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetPlayerHistoryReq(): GetPlayerHistoryReq {
  return { page: 0, startTime: 0, endTime: 0 };
}

export const GetPlayerHistoryReq = {
  encode(message: GetPlayerHistoryReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.page !== 0) {
      writer.uint32(8).int32(message.page);
    }
    if (message.startTime !== 0) {
      writer.uint32(16).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(24).int64(message.endTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerHistoryReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerHistoryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.page = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.startTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerHistoryReq {
    return {
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
    };
  },

  toJSON(message: GetPlayerHistoryReq): unknown {
    const obj: any = {};
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerHistoryReq>, I>>(base?: I): GetPlayerHistoryReq {
    return GetPlayerHistoryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerHistoryReq>, I>>(object: I): GetPlayerHistoryReq {
    const message = createBaseGetPlayerHistoryReq();
    message.page = object.page ?? 0;
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    return message;
  },
};

function createBaseGetPlayerHistoryRes(): GetPlayerHistoryRes {
  return { records: [] };
}

export const GetPlayerHistoryRes = {
  encode(message: GetPlayerHistoryRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.records) {
      TongitsHistoryRecord.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerHistoryRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerHistoryRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.records.push(TongitsHistoryRecord.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerHistoryRes {
    return {
      records: globalThis.Array.isArray(object?.records)
        ? object.records.map((e: any) => TongitsHistoryRecord.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPlayerHistoryRes): unknown {
    const obj: any = {};
    if (message.records?.length) {
      obj.records = message.records.map((e) => TongitsHistoryRecord.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerHistoryRes>, I>>(base?: I): GetPlayerHistoryRes {
    return GetPlayerHistoryRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerHistoryRes>, I>>(object: I): GetPlayerHistoryRes {
    const message = createBaseGetPlayerHistoryRes();
    message.records = object.records?.map((e) => TongitsHistoryRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTongitsHistoryRecord(): TongitsHistoryRecord {
  return { players: [] };
}

export const TongitsHistoryRecord = {
  encode(message: TongitsHistoryRecord, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.players) {
      TongitsHistoryInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsHistoryRecord {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsHistoryRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.players.push(TongitsHistoryInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TongitsHistoryRecord {
    return {
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => TongitsHistoryInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TongitsHistoryRecord): unknown {
    const obj: any = {};
    if (message.players?.length) {
      obj.players = message.players.map((e) => TongitsHistoryInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsHistoryRecord>, I>>(base?: I): TongitsHistoryRecord {
    return TongitsHistoryRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsHistoryRecord>, I>>(object: I): TongitsHistoryRecord {
    const message = createBaseTongitsHistoryRecord();
    message.players = object.players?.map((e) => TongitsHistoryInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTongitsHistoryInfo(): TongitsHistoryInfo {
  return {
    gameCode: "",
    endTime: 0,
    userId: 0,
    nickname: "",
    avatar: "",
    profit: 0,
    score: 0,
    cards: [],
    gameResult: 0,
    cost: 0,
    normalReward: 0,
    potReward: 0,
    totalReward: 0,
  };
}

export const TongitsHistoryInfo = {
  encode(message: TongitsHistoryInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameCode !== "") {
      writer.uint32(10).string(message.gameCode);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.userId !== 0) {
      writer.uint32(24).int64(message.userId);
    }
    if (message.nickname !== "") {
      writer.uint32(34).string(message.nickname);
    }
    if (message.avatar !== "") {
      writer.uint32(42).string(message.avatar);
    }
    if (message.profit !== 0) {
      writer.uint32(48).int32(message.profit);
    }
    if (message.score !== 0) {
      writer.uint32(56).int32(message.score);
    }
    writer.uint32(66).fork();
    for (const v of message.cards) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.gameResult !== 0) {
      writer.uint32(72).int32(message.gameResult);
    }
    if (message.cost !== 0) {
      writer.uint32(80).int32(message.cost);
    }
    if (message.normalReward !== 0) {
      writer.uint32(88).int32(message.normalReward);
    }
    if (message.potReward !== 0) {
      writer.uint32(96).int32(message.potReward);
    }
    if (message.totalReward !== 0) {
      writer.uint32(104).int32(message.totalReward);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TongitsHistoryInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTongitsHistoryInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameCode = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.nickname = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.avatar = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.profit = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.score = reader.int32();
          continue;
        case 8:
          if (tag === 64) {
            message.cards.push(reader.int32());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.cards.push(reader.int32());
            }

            continue;
          }

          break;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.gameResult = reader.int32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.cost = reader.int32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.normalReward = reader.int32();
          continue;
        case 12:
          if (tag !== 96) {
            break;
          }

          message.potReward = reader.int32();
          continue;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.totalReward = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TongitsHistoryInfo {
    return {
      gameCode: isSet(object.gameCode) ? globalThis.String(object.gameCode) : "",
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : "",
      avatar: isSet(object.avatar) ? globalThis.String(object.avatar) : "",
      profit: isSet(object.profit) ? globalThis.Number(object.profit) : 0,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => globalThis.Number(e)) : [],
      gameResult: isSet(object.gameResult) ? globalThis.Number(object.gameResult) : 0,
      cost: isSet(object.cost) ? globalThis.Number(object.cost) : 0,
      normalReward: isSet(object.normalReward) ? globalThis.Number(object.normalReward) : 0,
      potReward: isSet(object.potReward) ? globalThis.Number(object.potReward) : 0,
      totalReward: isSet(object.totalReward) ? globalThis.Number(object.totalReward) : 0,
    };
  },

  toJSON(message: TongitsHistoryInfo): unknown {
    const obj: any = {};
    if (message.gameCode !== "") {
      obj.gameCode = message.gameCode;
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.nickname !== "") {
      obj.nickname = message.nickname;
    }
    if (message.avatar !== "") {
      obj.avatar = message.avatar;
    }
    if (message.profit !== 0) {
      obj.profit = Math.round(message.profit);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => Math.round(e));
    }
    if (message.gameResult !== 0) {
      obj.gameResult = Math.round(message.gameResult);
    }
    if (message.cost !== 0) {
      obj.cost = Math.round(message.cost);
    }
    if (message.normalReward !== 0) {
      obj.normalReward = Math.round(message.normalReward);
    }
    if (message.potReward !== 0) {
      obj.potReward = Math.round(message.potReward);
    }
    if (message.totalReward !== 0) {
      obj.totalReward = Math.round(message.totalReward);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TongitsHistoryInfo>, I>>(base?: I): TongitsHistoryInfo {
    return TongitsHistoryInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TongitsHistoryInfo>, I>>(object: I): TongitsHistoryInfo {
    const message = createBaseTongitsHistoryInfo();
    message.gameCode = object.gameCode ?? "";
    message.endTime = object.endTime ?? 0;
    message.userId = object.userId ?? 0;
    message.nickname = object.nickname ?? "";
    message.avatar = object.avatar ?? "";
    message.profit = object.profit ?? 0;
    message.score = object.score ?? 0;
    message.cards = object.cards?.map((e) => e) || [];
    message.gameResult = object.gameResult ?? 0;
    message.cost = object.cost ?? 0;
    message.normalReward = object.normalReward ?? 0;
    message.potReward = object.potReward ?? 0;
    message.totalReward = object.totalReward ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
