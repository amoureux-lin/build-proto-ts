// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.34.0
// source: solitaire/solitaire.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";
import { PlayerInfo } from "./game_common_room";

export const protobufPackage = "solitaire.v1";

/** 空消息 */
export interface EmptyRes {
}

/**
 * 进入房间请求
 * MessageType: SOLITAIRE_JOIN_ROOM_REQ (1500)
 */
export interface JoinRoomReq {
  roomId: number;
}

/**
 * 进入房间响应
 * MessageType: SOLITAIRE_JOIN_ROOM_RES (1501)
 */
export interface JoinRoomRes {
  /** 房间信息 */
  roomInfo: RoomInfo | undefined;
  players: SolitairePayerInfo[];
  /** 旁观者信息 */
  watchers: PlayerInfo[];
  /** 旁观者数量 */
  playersCount: number;
  /** 开麦用户列表 */
  speakers: PlayerInfo[];
  /** 自己信息 */
  self:
    | SolitairePayerInfo
    | undefined;
  /** 游戏信息 */
  gameInfo: GameInfo | undefined;
}

/**
 * 房主开始游戏
 * MessageType: SOLITAIRE_ROOM_OWNER_START_GAME_REQ (1504)
 */
export interface StartGameReq {
}

/**
 * 房主开始游戏响应
 * MessageType: SOLITAIRE_ROOM_OWNER_START_GAME_RES (1505)
 */
export interface StartGameRes {
}

/**
 * 连牌请求
 * MessageType: SOLITAIRE_CONNECTOR_CARD_REQ (1508)
 */
export interface ConnectorCardReq {
  /** 0 牌桌 1 牌堆 */
  actionType: number;
  /** 牌的索引 */
  cardId: number;
}

/**
 * 连牌响应
 * MessageType: SOLITAIRE_CONNECTOR_CARD_RES (1509)
 */
export interface ConnectorCardRes {
  /** 0 牌桌 1 牌堆 */
  actionType: number;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 牌堆剩余牌数 */
  cardNum: number;
}

/**
 * 游戏开始广播
 * MessageType: SOLITAIRE_START_GAME_BROADCAST (1510)
 */
export interface StartGameBroadcast {
  /**  */
  userId: number;
  /** 游戏信息 */
  gameInfo: GameInfo | undefined;
}

/**
 * 连牌广播
 * MessageType: SOLITAIRE_CONNECTOR_CARD_BROADCAST (1511)
 */
export interface ConnectorCardBroadcast {
  /** 发起连牌的玩家id */
  gameUserId: number;
  /** 0 牌桌 1 牌堆 */
  actionType: number;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 牌堆剩余牌数 */
  cardNum: number;
  /** 通知的玩家id */
  userId: number;
}

/**
 * 玩家完成自己游戏
 * MessageType: SOLITAIRE_CONNECTOR_FINISHED_BROADCAST (1512)
 */
export interface ConnectorFinishedBroadcast {
  /** 胜利玩家id */
  finishedId: number;
  /** 时间戳 */
  timer: number;
  /** 玩家id */
  userId: number;
}

/**
 * 结算通知广播
 * MessageType: SOLITAIRE_GAME_RESULT_BROADCAST (1513)
 */
export interface GameResultBroadcast {
  /**  */
  userId: number;
  /** 游戏结果 */
  gameResult: GameResult | undefined;
}

/**
 * 房间重置广播
 * MessageType: SOLITAIRE_ROOM_RESET_BROADCAST (1514)
 */
export interface RoomResetBroadcast {
  players: SolitairePayerInfo[];
  /** 自己信息 */
  self:
    | SolitairePayerInfo
    | undefined;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 通知玩家id */
  userId: number;
}

/**
 * 视角切换广播
 * MessageType: SOLITAIRE_VIEW_CHANGE_BROADCAST (1517)
 */
export interface ViewChangeBroadcast {
  /** 观看玩家信息 */
  viewPlayer:
    | SolitairePayerInfo
    | undefined;
  /** 游戏信息 */
  gameInfo:
    | GameInfo
    | undefined;
  /** 通知玩家id */
  userId: number;
}

export interface RoomInfo {
  roomId: number;
  roomName: string;
  /** 房间状态 0 空房间 1 等待中 2 开始游戏 3 游戏结束 */
  roomStatus: number;
  /** 房间模式 0: 游戏模式 1: 房主模式 */
  roomMode: number;
  /** 最大座位数 */
  maxSeat: number;
}

/** 玩家信息 */
export interface SolitairePayerInfo {
  /** 玩家信息 */
  playerInfo:
    | PlayerInfo
    | undefined;
  /** 得分 */
  score: number;
  /** 连击 */
  combo: number;
  /** 牌堆剩余牌数 */
  cardNum: number;
}

export interface GameInfo {
  /** 牌局信息  连牌下 为显示牌以及消牌 */
  pokers: PokerInfo[];
  /** 消除的牌 */
  clearPokers: PokerInfo[];
  /** 手牌数值 */
  headValue: number;
  /** 0 普通牌 999 万能牌 */
  poolPokers: number[];
  /** 游戏状态 0 游戏中 1 已完成 */
  state: number;
  /** 时间戳 */
  timer: number;
  /** 得分 */
  score: number;
  /** 连击 */
  combo: number;
  /** 扣分 */
  deduction: number;
  /** 点击的牌的id */
  clickId: number;
  /** 当前下注币值 */
  betAmount: number;
  /** 币值列表 */
  scores: number[];
  /** 视角玩家id */
  perspectiveId: number;
  /** 当前变化分值 */
  changeScore: number;
}

export interface GameResult {
  /** 玩家变化 */
  players: SolitairePayerInfo[];
  /** 获胜者id */
  winnerId: number;
  /** 消耗牌数 */
  count: number;
}

export interface PokerInfo {
  /** 牌的索引 */
  id: number;
  /** 牌的数值 */
  value: number;
  /** 盖牌索引 */
  covers: number[];
  /** 被盖牌索引 */
  coveredBy: number[];
  /** 牌的状态 0 盖牌 1 可点击  2 消除 */
  state: number;
  /** 牌的x坐标 */
  x: number;
  /** 牌的y坐标 */
  y: number;
}

/**
 * 获取玩家历史记录请求
 * MessageType: SOLITAIRE_GET_PLAYER_HISTORY_REQ
 */
export interface GetPlayerHistoryReq {
  /** 页数，默认第一页 */
  page: number;
  /** 开始时间（Unix时间戳），默认当天开始 */
  startTime: number;
  /** 结束时间（Unix时间戳），默认当天结束 */
  endTime: number;
}

/**
 * 获取玩家历史记录响应
 * MessageType: SOLITAIRE_GET_PLAYER_HISTORY_RES
 */
export interface GetPlayerHistoryRes {
  /** 历史记录列表 */
  records: SolitaireHistoryRecord[];
}

/** 单条历史记录 */
export interface SolitaireHistoryRecord {
  /** 游戏局号 */
  gameCode: string;
  /** 结束时间 */
  endTime: number;
  cost: number;
  reward: number;
  cards: SolitaireCardInfo[];
  /** 自己分数 */
  score: number;
  /** 对方分数 */
  youScore: number;
}

export interface SolitaireCardInfo {
  cardId: number;
  /** 1 连牌 2 翻牌 */
  cardType: number;
}

function createBaseEmptyRes(): EmptyRes {
  return {};
}

export const EmptyRes = {
  encode(_: EmptyRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): EmptyRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmptyRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): EmptyRes {
    return {};
  },

  toJSON(_: EmptyRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<EmptyRes>, I>>(base?: I): EmptyRes {
    return EmptyRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EmptyRes>, I>>(_: I): EmptyRes {
    const message = createBaseEmptyRes();
    return message;
  },
};

function createBaseJoinRoomReq(): JoinRoomReq {
  return { roomId: 0 };
}

export const JoinRoomReq = {
  encode(message: JoinRoomReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomReq {
    return { roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0 };
  },

  toJSON(message: JoinRoomReq): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomReq>, I>>(base?: I): JoinRoomReq {
    return JoinRoomReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomReq>, I>>(object: I): JoinRoomReq {
    const message = createBaseJoinRoomReq();
    message.roomId = object.roomId ?? 0;
    return message;
  },
};

function createBaseJoinRoomRes(): JoinRoomRes {
  return {
    roomInfo: undefined,
    players: [],
    watchers: [],
    playersCount: 0,
    speakers: [],
    self: undefined,
    gameInfo: undefined,
  };
}

export const JoinRoomRes = {
  encode(message: JoinRoomRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomInfo !== undefined) {
      RoomInfo.encode(message.roomInfo, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.players) {
      SolitairePayerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.watchers) {
      PlayerInfo.encode(v!, writer.uint32(26).fork()).ldelim();
    }
    if (message.playersCount !== 0) {
      writer.uint32(32).int32(message.playersCount);
    }
    for (const v of message.speakers) {
      PlayerInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.self !== undefined) {
      SolitairePayerInfo.encode(message.self, writer.uint32(50).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(58).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): JoinRoomRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJoinRoomRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.roomInfo = RoomInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.players.push(SolitairePayerInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.watchers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.playersCount = reader.int32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.speakers.push(PlayerInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.self = SolitairePayerInfo.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JoinRoomRes {
    return {
      roomInfo: isSet(object.roomInfo) ? RoomInfo.fromJSON(object.roomInfo) : undefined,
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => SolitairePayerInfo.fromJSON(e))
        : [],
      watchers: globalThis.Array.isArray(object?.watchers)
        ? object.watchers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      playersCount: isSet(object.playersCount) ? globalThis.Number(object.playersCount) : 0,
      speakers: globalThis.Array.isArray(object?.speakers)
        ? object.speakers.map((e: any) => PlayerInfo.fromJSON(e))
        : [],
      self: isSet(object.self) ? SolitairePayerInfo.fromJSON(object.self) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
    };
  },

  toJSON(message: JoinRoomRes): unknown {
    const obj: any = {};
    if (message.roomInfo !== undefined) {
      obj.roomInfo = RoomInfo.toJSON(message.roomInfo);
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => SolitairePayerInfo.toJSON(e));
    }
    if (message.watchers?.length) {
      obj.watchers = message.watchers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.playersCount !== 0) {
      obj.playersCount = Math.round(message.playersCount);
    }
    if (message.speakers?.length) {
      obj.speakers = message.speakers.map((e) => PlayerInfo.toJSON(e));
    }
    if (message.self !== undefined) {
      obj.self = SolitairePayerInfo.toJSON(message.self);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinRoomRes>, I>>(base?: I): JoinRoomRes {
    return JoinRoomRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinRoomRes>, I>>(object: I): JoinRoomRes {
    const message = createBaseJoinRoomRes();
    message.roomInfo = (object.roomInfo !== undefined && object.roomInfo !== null)
      ? RoomInfo.fromPartial(object.roomInfo)
      : undefined;
    message.players = object.players?.map((e) => SolitairePayerInfo.fromPartial(e)) || [];
    message.watchers = object.watchers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.playersCount = object.playersCount ?? 0;
    message.speakers = object.speakers?.map((e) => PlayerInfo.fromPartial(e)) || [];
    message.self = (object.self !== undefined && object.self !== null)
      ? SolitairePayerInfo.fromPartial(object.self)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    return message;
  },
};

function createBaseStartGameReq(): StartGameReq {
  return {};
}

export const StartGameReq = {
  encode(_: StartGameReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameReq {
    return {};
  },

  toJSON(_: StartGameReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameReq>, I>>(base?: I): StartGameReq {
    return StartGameReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameReq>, I>>(_: I): StartGameReq {
    const message = createBaseStartGameReq();
    return message;
  },
};

function createBaseStartGameRes(): StartGameRes {
  return {};
}

export const StartGameRes = {
  encode(_: StartGameRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StartGameRes {
    return {};
  },

  toJSON(_: StartGameRes): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameRes>, I>>(base?: I): StartGameRes {
    return StartGameRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameRes>, I>>(_: I): StartGameRes {
    const message = createBaseStartGameRes();
    return message;
  },
};

function createBaseConnectorCardReq(): ConnectorCardReq {
  return { actionType: 0, cardId: 0 };
}

export const ConnectorCardReq = {
  encode(message: ConnectorCardReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionType !== 0) {
      writer.uint32(8).int32(message.actionType);
    }
    if (message.cardId !== 0) {
      writer.uint32(16).int32(message.cardId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConnectorCardReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorCardReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionType = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.cardId = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorCardReq {
    return {
      actionType: isSet(object.actionType) ? globalThis.Number(object.actionType) : 0,
      cardId: isSet(object.cardId) ? globalThis.Number(object.cardId) : 0,
    };
  },

  toJSON(message: ConnectorCardReq): unknown {
    const obj: any = {};
    if (message.actionType !== 0) {
      obj.actionType = Math.round(message.actionType);
    }
    if (message.cardId !== 0) {
      obj.cardId = Math.round(message.cardId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorCardReq>, I>>(base?: I): ConnectorCardReq {
    return ConnectorCardReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorCardReq>, I>>(object: I): ConnectorCardReq {
    const message = createBaseConnectorCardReq();
    message.actionType = object.actionType ?? 0;
    message.cardId = object.cardId ?? 0;
    return message;
  },
};

function createBaseConnectorCardRes(): ConnectorCardRes {
  return { actionType: 0, gameInfo: undefined, cardNum: 0 };
}

export const ConnectorCardRes = {
  encode(message: ConnectorCardRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.actionType !== 0) {
      writer.uint32(8).int32(message.actionType);
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(18).fork()).ldelim();
    }
    if (message.cardNum !== 0) {
      writer.uint32(24).int32(message.cardNum);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConnectorCardRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorCardRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.actionType = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.cardNum = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorCardRes {
    return {
      actionType: isSet(object.actionType) ? globalThis.Number(object.actionType) : 0,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      cardNum: isSet(object.cardNum) ? globalThis.Number(object.cardNum) : 0,
    };
  },

  toJSON(message: ConnectorCardRes): unknown {
    const obj: any = {};
    if (message.actionType !== 0) {
      obj.actionType = Math.round(message.actionType);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.cardNum !== 0) {
      obj.cardNum = Math.round(message.cardNum);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorCardRes>, I>>(base?: I): ConnectorCardRes {
    return ConnectorCardRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorCardRes>, I>>(object: I): ConnectorCardRes {
    const message = createBaseConnectorCardRes();
    message.actionType = object.actionType ?? 0;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.cardNum = object.cardNum ?? 0;
    return message;
  },
};

function createBaseStartGameBroadcast(): StartGameBroadcast {
  return { userId: 0, gameInfo: undefined };
}

export const StartGameBroadcast = {
  encode(message: StartGameBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): StartGameBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartGameBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartGameBroadcast {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
    };
  },

  toJSON(message: StartGameBroadcast): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartGameBroadcast>, I>>(base?: I): StartGameBroadcast {
    return StartGameBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartGameBroadcast>, I>>(object: I): StartGameBroadcast {
    const message = createBaseStartGameBroadcast();
    message.userId = object.userId ?? 0;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    return message;
  },
};

function createBaseConnectorCardBroadcast(): ConnectorCardBroadcast {
  return { gameUserId: 0, actionType: 0, gameInfo: undefined, cardNum: 0, userId: 0 };
}

export const ConnectorCardBroadcast = {
  encode(message: ConnectorCardBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameUserId !== 0) {
      writer.uint32(8).int64(message.gameUserId);
    }
    if (message.actionType !== 0) {
      writer.uint32(16).int32(message.actionType);
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(26).fork()).ldelim();
    }
    if (message.cardNum !== 0) {
      writer.uint32(40).int32(message.cardNum);
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConnectorCardBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorCardBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.gameUserId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.actionType = reader.int32();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.cardNum = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorCardBroadcast {
    return {
      gameUserId: isSet(object.gameUserId) ? globalThis.Number(object.gameUserId) : 0,
      actionType: isSet(object.actionType) ? globalThis.Number(object.actionType) : 0,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      cardNum: isSet(object.cardNum) ? globalThis.Number(object.cardNum) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: ConnectorCardBroadcast): unknown {
    const obj: any = {};
    if (message.gameUserId !== 0) {
      obj.gameUserId = Math.round(message.gameUserId);
    }
    if (message.actionType !== 0) {
      obj.actionType = Math.round(message.actionType);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.cardNum !== 0) {
      obj.cardNum = Math.round(message.cardNum);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorCardBroadcast>, I>>(base?: I): ConnectorCardBroadcast {
    return ConnectorCardBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorCardBroadcast>, I>>(object: I): ConnectorCardBroadcast {
    const message = createBaseConnectorCardBroadcast();
    message.gameUserId = object.gameUserId ?? 0;
    message.actionType = object.actionType ?? 0;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.cardNum = object.cardNum ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseConnectorFinishedBroadcast(): ConnectorFinishedBroadcast {
  return { finishedId: 0, timer: 0, userId: 0 };
}

export const ConnectorFinishedBroadcast = {
  encode(message: ConnectorFinishedBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.finishedId !== 0) {
      writer.uint32(8).int64(message.finishedId);
    }
    if (message.timer !== 0) {
      writer.uint32(16).int32(message.timer);
    }
    if (message.userId !== 0) {
      writer.uint32(24).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ConnectorFinishedBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConnectorFinishedBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.finishedId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.timer = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConnectorFinishedBroadcast {
    return {
      finishedId: isSet(object.finishedId) ? globalThis.Number(object.finishedId) : 0,
      timer: isSet(object.timer) ? globalThis.Number(object.timer) : 0,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: ConnectorFinishedBroadcast): unknown {
    const obj: any = {};
    if (message.finishedId !== 0) {
      obj.finishedId = Math.round(message.finishedId);
    }
    if (message.timer !== 0) {
      obj.timer = Math.round(message.timer);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConnectorFinishedBroadcast>, I>>(base?: I): ConnectorFinishedBroadcast {
    return ConnectorFinishedBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConnectorFinishedBroadcast>, I>>(object: I): ConnectorFinishedBroadcast {
    const message = createBaseConnectorFinishedBroadcast();
    message.finishedId = object.finishedId ?? 0;
    message.timer = object.timer ?? 0;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseGameResultBroadcast(): GameResultBroadcast {
  return { userId: 0, gameResult: undefined };
}

export const GameResultBroadcast = {
  encode(message: GameResultBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.userId !== 0) {
      writer.uint32(8).int64(message.userId);
    }
    if (message.gameResult !== undefined) {
      GameResult.encode(message.gameResult, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameResultBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameResultBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameResult = GameResult.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameResultBroadcast {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      gameResult: isSet(object.gameResult) ? GameResult.fromJSON(object.gameResult) : undefined,
    };
  },

  toJSON(message: GameResultBroadcast): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.gameResult !== undefined) {
      obj.gameResult = GameResult.toJSON(message.gameResult);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameResultBroadcast>, I>>(base?: I): GameResultBroadcast {
    return GameResultBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameResultBroadcast>, I>>(object: I): GameResultBroadcast {
    const message = createBaseGameResultBroadcast();
    message.userId = object.userId ?? 0;
    message.gameResult = (object.gameResult !== undefined && object.gameResult !== null)
      ? GameResult.fromPartial(object.gameResult)
      : undefined;
    return message;
  },
};

function createBaseRoomResetBroadcast(): RoomResetBroadcast {
  return { players: [], self: undefined, gameInfo: undefined, userId: 0 };
}

export const RoomResetBroadcast = {
  encode(message: RoomResetBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.players) {
      SolitairePayerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.self !== undefined) {
      SolitairePayerInfo.encode(message.self, writer.uint32(18).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(26).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(32).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoomResetBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomResetBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.players.push(SolitairePayerInfo.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.self = SolitairePayerInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomResetBroadcast {
    return {
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => SolitairePayerInfo.fromJSON(e))
        : [],
      self: isSet(object.self) ? SolitairePayerInfo.fromJSON(object.self) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: RoomResetBroadcast): unknown {
    const obj: any = {};
    if (message.players?.length) {
      obj.players = message.players.map((e) => SolitairePayerInfo.toJSON(e));
    }
    if (message.self !== undefined) {
      obj.self = SolitairePayerInfo.toJSON(message.self);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomResetBroadcast>, I>>(base?: I): RoomResetBroadcast {
    return RoomResetBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomResetBroadcast>, I>>(object: I): RoomResetBroadcast {
    const message = createBaseRoomResetBroadcast();
    message.players = object.players?.map((e) => SolitairePayerInfo.fromPartial(e)) || [];
    message.self = (object.self !== undefined && object.self !== null)
      ? SolitairePayerInfo.fromPartial(object.self)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseViewChangeBroadcast(): ViewChangeBroadcast {
  return { viewPlayer: undefined, gameInfo: undefined, userId: 0 };
}

export const ViewChangeBroadcast = {
  encode(message: ViewChangeBroadcast, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.viewPlayer !== undefined) {
      SolitairePayerInfo.encode(message.viewPlayer, writer.uint32(10).fork()).ldelim();
    }
    if (message.gameInfo !== undefined) {
      GameInfo.encode(message.gameInfo, writer.uint32(18).fork()).ldelim();
    }
    if (message.userId !== 0) {
      writer.uint32(24).int64(message.userId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ViewChangeBroadcast {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseViewChangeBroadcast();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.viewPlayer = SolitairePayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gameInfo = GameInfo.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.userId = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ViewChangeBroadcast {
    return {
      viewPlayer: isSet(object.viewPlayer) ? SolitairePayerInfo.fromJSON(object.viewPlayer) : undefined,
      gameInfo: isSet(object.gameInfo) ? GameInfo.fromJSON(object.gameInfo) : undefined,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
    };
  },

  toJSON(message: ViewChangeBroadcast): unknown {
    const obj: any = {};
    if (message.viewPlayer !== undefined) {
      obj.viewPlayer = SolitairePayerInfo.toJSON(message.viewPlayer);
    }
    if (message.gameInfo !== undefined) {
      obj.gameInfo = GameInfo.toJSON(message.gameInfo);
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ViewChangeBroadcast>, I>>(base?: I): ViewChangeBroadcast {
    return ViewChangeBroadcast.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ViewChangeBroadcast>, I>>(object: I): ViewChangeBroadcast {
    const message = createBaseViewChangeBroadcast();
    message.viewPlayer = (object.viewPlayer !== undefined && object.viewPlayer !== null)
      ? SolitairePayerInfo.fromPartial(object.viewPlayer)
      : undefined;
    message.gameInfo = (object.gameInfo !== undefined && object.gameInfo !== null)
      ? GameInfo.fromPartial(object.gameInfo)
      : undefined;
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseRoomInfo(): RoomInfo {
  return { roomId: 0, roomName: "", roomStatus: 0, roomMode: 0, maxSeat: 0 };
}

export const RoomInfo = {
  encode(message: RoomInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.roomId !== 0) {
      writer.uint32(8).int64(message.roomId);
    }
    if (message.roomName !== "") {
      writer.uint32(18).string(message.roomName);
    }
    if (message.roomStatus !== 0) {
      writer.uint32(24).int32(message.roomStatus);
    }
    if (message.roomMode !== 0) {
      writer.uint32(32).int32(message.roomMode);
    }
    if (message.maxSeat !== 0) {
      writer.uint32(40).int32(message.maxSeat);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RoomInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.roomId = longToNumber(reader.int64() as Long);
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.roomName = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.roomStatus = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.roomMode = reader.int32();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.maxSeat = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomInfo {
    return {
      roomId: isSet(object.roomId) ? globalThis.Number(object.roomId) : 0,
      roomName: isSet(object.roomName) ? globalThis.String(object.roomName) : "",
      roomStatus: isSet(object.roomStatus) ? globalThis.Number(object.roomStatus) : 0,
      roomMode: isSet(object.roomMode) ? globalThis.Number(object.roomMode) : 0,
      maxSeat: isSet(object.maxSeat) ? globalThis.Number(object.maxSeat) : 0,
    };
  },

  toJSON(message: RoomInfo): unknown {
    const obj: any = {};
    if (message.roomId !== 0) {
      obj.roomId = Math.round(message.roomId);
    }
    if (message.roomName !== "") {
      obj.roomName = message.roomName;
    }
    if (message.roomStatus !== 0) {
      obj.roomStatus = Math.round(message.roomStatus);
    }
    if (message.roomMode !== 0) {
      obj.roomMode = Math.round(message.roomMode);
    }
    if (message.maxSeat !== 0) {
      obj.maxSeat = Math.round(message.maxSeat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomInfo>, I>>(base?: I): RoomInfo {
    return RoomInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomInfo>, I>>(object: I): RoomInfo {
    const message = createBaseRoomInfo();
    message.roomId = object.roomId ?? 0;
    message.roomName = object.roomName ?? "";
    message.roomStatus = object.roomStatus ?? 0;
    message.roomMode = object.roomMode ?? 0;
    message.maxSeat = object.maxSeat ?? 0;
    return message;
  },
};

function createBaseSolitairePayerInfo(): SolitairePayerInfo {
  return { playerInfo: undefined, score: 0, combo: 0, cardNum: 0 };
}

export const SolitairePayerInfo = {
  encode(message: SolitairePayerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.playerInfo !== undefined) {
      PlayerInfo.encode(message.playerInfo, writer.uint32(10).fork()).ldelim();
    }
    if (message.score !== 0) {
      writer.uint32(16).int32(message.score);
    }
    if (message.combo !== 0) {
      writer.uint32(24).int32(message.combo);
    }
    if (message.cardNum !== 0) {
      writer.uint32(32).int32(message.cardNum);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SolitairePayerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolitairePayerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playerInfo = PlayerInfo.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.score = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.combo = reader.int32();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.cardNum = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolitairePayerInfo {
    return {
      playerInfo: isSet(object.playerInfo) ? PlayerInfo.fromJSON(object.playerInfo) : undefined,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      combo: isSet(object.combo) ? globalThis.Number(object.combo) : 0,
      cardNum: isSet(object.cardNum) ? globalThis.Number(object.cardNum) : 0,
    };
  },

  toJSON(message: SolitairePayerInfo): unknown {
    const obj: any = {};
    if (message.playerInfo !== undefined) {
      obj.playerInfo = PlayerInfo.toJSON(message.playerInfo);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.combo !== 0) {
      obj.combo = Math.round(message.combo);
    }
    if (message.cardNum !== 0) {
      obj.cardNum = Math.round(message.cardNum);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SolitairePayerInfo>, I>>(base?: I): SolitairePayerInfo {
    return SolitairePayerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SolitairePayerInfo>, I>>(object: I): SolitairePayerInfo {
    const message = createBaseSolitairePayerInfo();
    message.playerInfo = (object.playerInfo !== undefined && object.playerInfo !== null)
      ? PlayerInfo.fromPartial(object.playerInfo)
      : undefined;
    message.score = object.score ?? 0;
    message.combo = object.combo ?? 0;
    message.cardNum = object.cardNum ?? 0;
    return message;
  },
};

function createBaseGameInfo(): GameInfo {
  return {
    pokers: [],
    clearPokers: [],
    headValue: 0,
    poolPokers: [],
    state: 0,
    timer: 0,
    score: 0,
    combo: 0,
    deduction: 0,
    clickId: 0,
    betAmount: 0,
    scores: [],
    perspectiveId: 0,
    changeScore: 0,
  };
}

export const GameInfo = {
  encode(message: GameInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.pokers) {
      PokerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.clearPokers) {
      PokerInfo.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.headValue !== 0) {
      writer.uint32(24).int32(message.headValue);
    }
    writer.uint32(34).fork();
    for (const v of message.poolPokers) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.state !== 0) {
      writer.uint32(40).int32(message.state);
    }
    if (message.timer !== 0) {
      writer.uint32(48).int32(message.timer);
    }
    if (message.score !== 0) {
      writer.uint32(56).int32(message.score);
    }
    if (message.combo !== 0) {
      writer.uint32(64).int32(message.combo);
    }
    if (message.deduction !== 0) {
      writer.uint32(72).int32(message.deduction);
    }
    if (message.clickId !== 0) {
      writer.uint32(80).int32(message.clickId);
    }
    if (message.betAmount !== 0) {
      writer.uint32(88).int32(message.betAmount);
    }
    writer.uint32(98).fork();
    for (const v of message.scores) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.perspectiveId !== 0) {
      writer.uint32(104).int64(message.perspectiveId);
    }
    if (message.changeScore !== 0) {
      writer.uint32(112).int32(message.changeScore);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.pokers.push(PokerInfo.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.clearPokers.push(PokerInfo.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.headValue = reader.int32();
          continue;
        case 4:
          if (tag === 32) {
            message.poolPokers.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.poolPokers.push(reader.int32());
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.state = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.timer = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.score = reader.int32();
          continue;
        case 8:
          if (tag !== 64) {
            break;
          }

          message.combo = reader.int32();
          continue;
        case 9:
          if (tag !== 72) {
            break;
          }

          message.deduction = reader.int32();
          continue;
        case 10:
          if (tag !== 80) {
            break;
          }

          message.clickId = reader.int32();
          continue;
        case 11:
          if (tag !== 88) {
            break;
          }

          message.betAmount = reader.int32();
          continue;
        case 12:
          if (tag === 96) {
            message.scores.push(reader.int32());

            continue;
          }

          if (tag === 98) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.scores.push(reader.int32());
            }

            continue;
          }

          break;
        case 13:
          if (tag !== 104) {
            break;
          }

          message.perspectiveId = longToNumber(reader.int64() as Long);
          continue;
        case 14:
          if (tag !== 112) {
            break;
          }

          message.changeScore = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameInfo {
    return {
      pokers: globalThis.Array.isArray(object?.pokers) ? object.pokers.map((e: any) => PokerInfo.fromJSON(e)) : [],
      clearPokers: globalThis.Array.isArray(object?.clearPokers)
        ? object.clearPokers.map((e: any) => PokerInfo.fromJSON(e))
        : [],
      headValue: isSet(object.headValue) ? globalThis.Number(object.headValue) : 0,
      poolPokers: globalThis.Array.isArray(object?.poolPokers)
        ? object.poolPokers.map((e: any) => globalThis.Number(e))
        : [],
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      timer: isSet(object.timer) ? globalThis.Number(object.timer) : 0,
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      combo: isSet(object.combo) ? globalThis.Number(object.combo) : 0,
      deduction: isSet(object.deduction) ? globalThis.Number(object.deduction) : 0,
      clickId: isSet(object.clickId) ? globalThis.Number(object.clickId) : 0,
      betAmount: isSet(object.betAmount) ? globalThis.Number(object.betAmount) : 0,
      scores: globalThis.Array.isArray(object?.scores) ? object.scores.map((e: any) => globalThis.Number(e)) : [],
      perspectiveId: isSet(object.perspectiveId) ? globalThis.Number(object.perspectiveId) : 0,
      changeScore: isSet(object.changeScore) ? globalThis.Number(object.changeScore) : 0,
    };
  },

  toJSON(message: GameInfo): unknown {
    const obj: any = {};
    if (message.pokers?.length) {
      obj.pokers = message.pokers.map((e) => PokerInfo.toJSON(e));
    }
    if (message.clearPokers?.length) {
      obj.clearPokers = message.clearPokers.map((e) => PokerInfo.toJSON(e));
    }
    if (message.headValue !== 0) {
      obj.headValue = Math.round(message.headValue);
    }
    if (message.poolPokers?.length) {
      obj.poolPokers = message.poolPokers.map((e) => Math.round(e));
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.timer !== 0) {
      obj.timer = Math.round(message.timer);
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.combo !== 0) {
      obj.combo = Math.round(message.combo);
    }
    if (message.deduction !== 0) {
      obj.deduction = Math.round(message.deduction);
    }
    if (message.clickId !== 0) {
      obj.clickId = Math.round(message.clickId);
    }
    if (message.betAmount !== 0) {
      obj.betAmount = Math.round(message.betAmount);
    }
    if (message.scores?.length) {
      obj.scores = message.scores.map((e) => Math.round(e));
    }
    if (message.perspectiveId !== 0) {
      obj.perspectiveId = Math.round(message.perspectiveId);
    }
    if (message.changeScore !== 0) {
      obj.changeScore = Math.round(message.changeScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameInfo>, I>>(base?: I): GameInfo {
    return GameInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameInfo>, I>>(object: I): GameInfo {
    const message = createBaseGameInfo();
    message.pokers = object.pokers?.map((e) => PokerInfo.fromPartial(e)) || [];
    message.clearPokers = object.clearPokers?.map((e) => PokerInfo.fromPartial(e)) || [];
    message.headValue = object.headValue ?? 0;
    message.poolPokers = object.poolPokers?.map((e) => e) || [];
    message.state = object.state ?? 0;
    message.timer = object.timer ?? 0;
    message.score = object.score ?? 0;
    message.combo = object.combo ?? 0;
    message.deduction = object.deduction ?? 0;
    message.clickId = object.clickId ?? 0;
    message.betAmount = object.betAmount ?? 0;
    message.scores = object.scores?.map((e) => e) || [];
    message.perspectiveId = object.perspectiveId ?? 0;
    message.changeScore = object.changeScore ?? 0;
    return message;
  },
};

function createBaseGameResult(): GameResult {
  return { players: [], winnerId: 0, count: 0 };
}

export const GameResult = {
  encode(message: GameResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.players) {
      SolitairePayerInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    if (message.winnerId !== 0) {
      writer.uint32(16).int32(message.winnerId);
    }
    if (message.count !== 0) {
      writer.uint32(24).int32(message.count);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GameResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.players.push(SolitairePayerInfo.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.winnerId = reader.int32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.count = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameResult {
    return {
      players: globalThis.Array.isArray(object?.players)
        ? object.players.map((e: any) => SolitairePayerInfo.fromJSON(e))
        : [],
      winnerId: isSet(object.winnerId) ? globalThis.Number(object.winnerId) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: GameResult): unknown {
    const obj: any = {};
    if (message.players?.length) {
      obj.players = message.players.map((e) => SolitairePayerInfo.toJSON(e));
    }
    if (message.winnerId !== 0) {
      obj.winnerId = Math.round(message.winnerId);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameResult>, I>>(base?: I): GameResult {
    return GameResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameResult>, I>>(object: I): GameResult {
    const message = createBaseGameResult();
    message.players = object.players?.map((e) => SolitairePayerInfo.fromPartial(e)) || [];
    message.winnerId = object.winnerId ?? 0;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBasePokerInfo(): PokerInfo {
  return { id: 0, value: 0, covers: [], coveredBy: [], state: 0, x: 0, y: 0 };
}

export const PokerInfo = {
  encode(message: PokerInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.value !== 0) {
      writer.uint32(16).int32(message.value);
    }
    writer.uint32(26).fork();
    for (const v of message.covers) {
      writer.int32(v);
    }
    writer.ldelim();
    writer.uint32(34).fork();
    for (const v of message.coveredBy) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.state !== 0) {
      writer.uint32(40).int32(message.state);
    }
    if (message.x !== 0) {
      writer.uint32(48).int32(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(56).int32(message.y);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): PokerInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePokerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.value = reader.int32();
          continue;
        case 3:
          if (tag === 24) {
            message.covers.push(reader.int32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.covers.push(reader.int32());
            }

            continue;
          }

          break;
        case 4:
          if (tag === 32) {
            message.coveredBy.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.coveredBy.push(reader.int32());
            }

            continue;
          }

          break;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.state = reader.int32();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.x = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.y = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PokerInfo {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
      covers: globalThis.Array.isArray(object?.covers) ? object.covers.map((e: any) => globalThis.Number(e)) : [],
      coveredBy: globalThis.Array.isArray(object?.coveredBy)
        ? object.coveredBy.map((e: any) => globalThis.Number(e))
        : [],
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
    };
  },

  toJSON(message: PokerInfo): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    if (message.covers?.length) {
      obj.covers = message.covers.map((e) => Math.round(e));
    }
    if (message.coveredBy?.length) {
      obj.coveredBy = message.coveredBy.map((e) => Math.round(e));
    }
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.x !== 0) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== 0) {
      obj.y = Math.round(message.y);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PokerInfo>, I>>(base?: I): PokerInfo {
    return PokerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PokerInfo>, I>>(object: I): PokerInfo {
    const message = createBasePokerInfo();
    message.id = object.id ?? 0;
    message.value = object.value ?? 0;
    message.covers = object.covers?.map((e) => e) || [];
    message.coveredBy = object.coveredBy?.map((e) => e) || [];
    message.state = object.state ?? 0;
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    return message;
  },
};

function createBaseGetPlayerHistoryReq(): GetPlayerHistoryReq {
  return { page: 0, startTime: 0, endTime: 0 };
}

export const GetPlayerHistoryReq = {
  encode(message: GetPlayerHistoryReq, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.page !== 0) {
      writer.uint32(8).int32(message.page);
    }
    if (message.startTime !== 0) {
      writer.uint32(16).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(24).int64(message.endTime);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerHistoryReq {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerHistoryReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.page = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.startTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerHistoryReq {
    return {
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
    };
  },

  toJSON(message: GetPlayerHistoryReq): unknown {
    const obj: any = {};
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerHistoryReq>, I>>(base?: I): GetPlayerHistoryReq {
    return GetPlayerHistoryReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerHistoryReq>, I>>(object: I): GetPlayerHistoryReq {
    const message = createBaseGetPlayerHistoryReq();
    message.page = object.page ?? 0;
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    return message;
  },
};

function createBaseGetPlayerHistoryRes(): GetPlayerHistoryRes {
  return { records: [] };
}

export const GetPlayerHistoryRes = {
  encode(message: GetPlayerHistoryRes, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.records) {
      SolitaireHistoryRecord.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetPlayerHistoryRes {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlayerHistoryRes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.records.push(SolitaireHistoryRecord.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlayerHistoryRes {
    return {
      records: globalThis.Array.isArray(object?.records)
        ? object.records.map((e: any) => SolitaireHistoryRecord.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPlayerHistoryRes): unknown {
    const obj: any = {};
    if (message.records?.length) {
      obj.records = message.records.map((e) => SolitaireHistoryRecord.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlayerHistoryRes>, I>>(base?: I): GetPlayerHistoryRes {
    return GetPlayerHistoryRes.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlayerHistoryRes>, I>>(object: I): GetPlayerHistoryRes {
    const message = createBaseGetPlayerHistoryRes();
    message.records = object.records?.map((e) => SolitaireHistoryRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSolitaireHistoryRecord(): SolitaireHistoryRecord {
  return { gameCode: "", endTime: 0, cost: 0, reward: 0, cards: [], score: 0, youScore: 0 };
}

export const SolitaireHistoryRecord = {
  encode(message: SolitaireHistoryRecord, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.gameCode !== "") {
      writer.uint32(10).string(message.gameCode);
    }
    if (message.endTime !== 0) {
      writer.uint32(16).int64(message.endTime);
    }
    if (message.cost !== 0) {
      writer.uint32(24).int64(message.cost);
    }
    if (message.reward !== 0) {
      writer.uint32(32).int64(message.reward);
    }
    for (const v of message.cards) {
      SolitaireCardInfo.encode(v!, writer.uint32(42).fork()).ldelim();
    }
    if (message.score !== 0) {
      writer.uint32(48).int32(message.score);
    }
    if (message.youScore !== 0) {
      writer.uint32(56).int32(message.youScore);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SolitaireHistoryRecord {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolitaireHistoryRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.gameCode = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.endTime = longToNumber(reader.int64() as Long);
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.cost = longToNumber(reader.int64() as Long);
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.reward = longToNumber(reader.int64() as Long);
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.cards.push(SolitaireCardInfo.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.score = reader.int32();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.youScore = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolitaireHistoryRecord {
    return {
      gameCode: isSet(object.gameCode) ? globalThis.String(object.gameCode) : "",
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      cost: isSet(object.cost) ? globalThis.Number(object.cost) : 0,
      reward: isSet(object.reward) ? globalThis.Number(object.reward) : 0,
      cards: globalThis.Array.isArray(object?.cards) ? object.cards.map((e: any) => SolitaireCardInfo.fromJSON(e)) : [],
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      youScore: isSet(object.youScore) ? globalThis.Number(object.youScore) : 0,
    };
  },

  toJSON(message: SolitaireHistoryRecord): unknown {
    const obj: any = {};
    if (message.gameCode !== "") {
      obj.gameCode = message.gameCode;
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.cost !== 0) {
      obj.cost = Math.round(message.cost);
    }
    if (message.reward !== 0) {
      obj.reward = Math.round(message.reward);
    }
    if (message.cards?.length) {
      obj.cards = message.cards.map((e) => SolitaireCardInfo.toJSON(e));
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.youScore !== 0) {
      obj.youScore = Math.round(message.youScore);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SolitaireHistoryRecord>, I>>(base?: I): SolitaireHistoryRecord {
    return SolitaireHistoryRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SolitaireHistoryRecord>, I>>(object: I): SolitaireHistoryRecord {
    const message = createBaseSolitaireHistoryRecord();
    message.gameCode = object.gameCode ?? "";
    message.endTime = object.endTime ?? 0;
    message.cost = object.cost ?? 0;
    message.reward = object.reward ?? 0;
    message.cards = object.cards?.map((e) => SolitaireCardInfo.fromPartial(e)) || [];
    message.score = object.score ?? 0;
    message.youScore = object.youScore ?? 0;
    return message;
  },
};

function createBaseSolitaireCardInfo(): SolitaireCardInfo {
  return { cardId: 0, cardType: 0 };
}

export const SolitaireCardInfo = {
  encode(message: SolitaireCardInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.cardId !== 0) {
      writer.uint32(8).int32(message.cardId);
    }
    if (message.cardType !== 0) {
      writer.uint32(16).int32(message.cardType);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): SolitaireCardInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolitaireCardInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.cardId = reader.int32();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.cardType = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SolitaireCardInfo {
    return {
      cardId: isSet(object.cardId) ? globalThis.Number(object.cardId) : 0,
      cardType: isSet(object.cardType) ? globalThis.Number(object.cardType) : 0,
    };
  },

  toJSON(message: SolitaireCardInfo): unknown {
    const obj: any = {};
    if (message.cardId !== 0) {
      obj.cardId = Math.round(message.cardId);
    }
    if (message.cardType !== 0) {
      obj.cardType = Math.round(message.cardType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SolitaireCardInfo>, I>>(base?: I): SolitaireCardInfo {
    return SolitaireCardInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SolitaireCardInfo>, I>>(object: I): SolitaireCardInfo {
    const message = createBaseSolitaireCardInfo();
    message.cardId = object.cardId ?? 0;
    message.cardType = object.cardType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
